# Support AI Agent — Контекст проекта

> Этот файл поддерживается Claude Code и обновляется в начале каждой сессии.
> **Последнее обновление:** 2026-08-02 (сессия 26 — C1 пройдена, П1 починена, задеплоена и проверена на проде, коммит `0aa641f`. На проде кончилось место — почищено, поставлены лимиты на логи)

---

## С чего начать сессию 27

**П1 на проде** (коммит `0aa641f`, фронт-бандл `index-R0CK9rwL.js` отдаётся). Что починено — в [ПРОВЕРКА_РАЗБОРА.md](ПРОВЕРКА_РАЗБОРА.md), раздел «П1». Проверено на живых заявках после деплоя: 4576 ПЛ 10 км → «Данные верны», 4538/О174ТХ56 ПЛ 43 → «Данные верны», 4674 — вторая строка из комментария клиента, 3498 — моточасы 7 м/ч отдельным полем, 4537 — «Нет даты» вместо вердикта по чужому дню, 3704 — дата из «с 02.07», 4015 — регион `О613ТТ56` из темы.

**Не проверено человеком:** карточка глазами — как выглядят моточасы (`1 м/ч` в колонке ПЛ) и строка с пометкой `(из комментария клиента)`.

Дальше по методу: **C2** — Ульяновские РС ×4 (115 заявок) + Чувашэнерго ×3 (5). Ульяновским Барышское/Димитровградское/Южное выдан нейтральный профиль «формат не подтверждён примерами» — формат надо наконец увидеть; у Чувашэнерго профиля нет вовсе.

**Инфраструктура (2026-08-02):** на сервере кончилось место (0 байт свободно, запись в SQLite не шла). Виноват `/var/log/syslog` 1.7 ГБ — `danted` (SOCKS-прокси, к проекту не относится) логирует каждое соединение. Почищено (syslog, старые ротации, `/root/.vscode-server`), стало 3.6 ГБ свободно. Поставлены лимиты: logrotate для syslog `daily` + `maxsize 200M` + ежечасный запуск через `/etc/cron.hourly/logrotate-size`, journald `SystemMaxUse=200M`. Если место снова кончится — смотреть `danted`.

**Крупнейшая находка C1 — потеря пробега по путевому листу в актах ЦПО ССМ**: во всех 12 строках 4 актов `ПЛ Nкм` не распознан, из-за этого минимум 3 строки получили «Проверить» там, где телеметрия совпадает с путевым листом и годился автоответ «Данные верны».

**Правки сессии 25 закоммичены и уже на проде** (`34584dc`, сервис `active`, HTTP 200 — проверено 2026-07-31 перед прогоном C1). Что в них было:
- `POST /issues/{id}/parameters` в Okdesk вместо `PATCH /issues/{id}` — из-за этого правки «Местоположение техники / Контактное лицо / Номер телефона» вообще не доезжали (Okdesk отвечал 200 и молча игнорировал). Проверено на живой 65910.
- `editable_parameters` в карточке: форма правки и счётчик `параметры N/3` читают сырые значения по кодам, а не «витрину» `_build_parameters` (та прячет мусор и подставляет телефон из «Контактного лица» — поле выглядело заполненным при пустом атрибуте).
- Оценку разбора можно ставить любому разбору, не только платному прогону ИИ; источник пишется в `ai_feedback.verdict_source` (`rules`/`ai`/`operator`), в БД добавлена колонка через лёгкую миграцию.
- Срок / приоритет / плановая продолжительность в блоке «Поля Okdesk» переведены НА ПРОСМОТР: у API-ключа нет прав (403 на `/deadlines` и `/priorities`), поле правки только обманывало. Бэкенд эти поля сохранять умеет — когда права выдадут, менять только фронт.
- Фронт собран в `app/static` (старые бандлы удалены, новые не добавлены в индекс git).
- В живой заявке 65910 в «Местоположение техники» стоит `-` — это след диагностики, Okdesk не даёт стереть значение обязательного атрибута.

## С чего начинали сессию 25 (история)

**Всё из сессии 24 задеплоено и проверено на живых данных** (коммиты `4b6fd12`, `0c58857`, `b58574b`, `3562abd`). Прод здоров: сервис `active`, HTTP 200, ошибок в журнале нет, ни одного падения под нагрузкой прогонов.

**Что осталось непроверенным — начать с этого:**
1. **Карточка глазами.** Ни одна из новых кнопок не нажата человеком: добавление строки, удаление с подтверждением, правка пробега, правка вердикта в одиночной заявке и — обязательно — **F5 после правки** (флаг `operator_touched`). Бэкенд по этим путям проверен, UI — нет.
2. **Модалка предупреждения о потере правок** перед «Обновить разбор».
3. **Предложение «ИИ ошибся? Отметить»** — появляется после правки вердикта, у которого источник был `ai`.

**Открытые вопросы, решать не срочно:**
- `issue_cache.description` ПУСТ у всех 4642 заявок, хотя в живом Okdesk описание есть. Разбор это переживает (тянет вживую), но список, поиск и ИИ-чат работают вслепую. Решить, чинить ли синхронизацию.
- Доверие к ИИ: уверенность модели бесполезна как сигнал (см. ниже) — вернуться, когда наберётся статистика `ai_feedback`.
- `В152ТУ 53 км` → `В152ТУ53`: единственный известный ложный класс у обеих регулярок номера. На 5406 боевых записях не встретился ни разу; лечить — только обе сразу, одним лукахедом `(?!\s*(?:км|м\b|%))`.

---

## ПЛАН СЕССИИ 25 — прочесать разбор по остальным ПО

> ⚠️ **УСТАРЕЛО, см. [ПРОВЕРКА_РАЗБОРА.md](ПРОВЕРКА_РАЗБОРА.md)** — 2026-07-31 план пересобран по
> живой базе и по интервью с пользователем. Числа заявок ниже НЕВЕРНЫ (в базе 4867 заявок, а не
> 4642: Волжское 948, а не 852; Прихоперское 281, а не 266). Главное упущение этого раздела —
> **Оренбургэнерго вообще отсутствует, хотя это крупнейший филиал: 988 заявок в 7 ПО**. Ещё одна
> ошибка методики: выбирать заявки с вложениями по теме нельзя — у Самарского, Саратов Северного,
> Пензы и большей части Оренбурга «актоподобных» тем НОЛЬ. Ниже оставлено как история решений.

**Зачем.** Сессия 24 нашла 10 дефектов, посмотрев ровно 26 заявок ОДНОГО оператора — это 0.6% базы, почти целиком Оренбургэнерго и Ульяновские. Плотность находок (10 дефектов на 26 заявок) говорит, что на других ПО их не меньше. Плюс в этой сессии тронуты вещи, которые могли задеть чужие форматы: обязательная единица у заявленного пробега, регион через пробел в ОБЕИХ регулярках номера, переименование профилей, кламп года по `created_at`.

**Методика (она сработала, повторить её же):**
1. Выборка заявок ПО из `issue_cache`, обязательно **с вложениями** — текстовый путь уже прочёсан, а актовый почти нет.
2. Снять `result_cache` (kind `parse`/`batch`) ДО прогона — это состояние «до».
3. `POST /parse`, а где есть вложения — `POST /automate_batch`. Оба БЕСПЛАТНЫ (DeepSeek не зовут), но `automate_batch` тянет OCR: гонять ПО ОДНОЙ, между прогонами проверять `systemctl is-active` и HTTP 200.
4. Сверить результат с ИСХОДНИКОМ (`GET /extracted`, `GET /attachments`), а не с ожиданием «в голове».
5. Классифицировать по тем же осям: номер (OCR-искажение, латиница, спецтехника, регион), дата (отчёта вместо неисправности, опечатка года, ISO), пробег (ПЛ и заявленный, путаница с датой/телефоном/номером акта), состав строк (потеряны, задвоены), вердикт против фактов.
6. Регресс: старый модуль (`git show HEAD:app/services/issue_automation.py`) против нового на локальной базе + контрольная выборка ПО, которых правка не касалась.

**Порядок ПО — сначала риск регрессии, потом объём.** Числа — заявок в `issue_cache`.

| Очередь | ПО | Заявок | Почему именно оно |
|---|---|---|---|
| 1 | **Саратовские РС Северное** | 456 | Профиль переименован в составной ключ. Если что-то сломано переименованием — здесь и вылезет |
| 2 | **Ульяновские РС** (Ульяновское 91 + Барышское 14 + Димитровградское 5 + Южное 1) | 111 | Заведён НЕЙТРАЛЬНЫЙ профиль, формат акта примерами не подтверждён. **Нужна хоть одна заявка с вложением**, чтобы понять реальный формат и дать нормальную стратегию |
| 3 | **Чувашэнерго** (Алатырское/Южное/Северное) | 5 | Раньше «Северное» ошибочно ловилось саратовским профилем, теперь профиля нет вовсе. Проверить, что стало не хуже |
| 4 | **Волжское ПО** | 852 | Самое большое ПО базы, актовые PDF. Главная проверка обязательной единицы у заявленного пробега |
| 5 | **Самарское ПО** | 525 | То же: объём + акты |
| 6 | **Прихоперское** 266, **Чапаевское** 160, **Жигулевское** 155, **Северо-Восточное** 84 | 665 | Профили есть, переименованы в составные |
| 7 | **Пензаэнерго** (Нижнеломовское 125, Сердобское 102, Кузнецкое 89, Пензенское 49, Каменское 36) | 401 | Один широкий профиль на 5 разных ПО — вероятно, форматы различаются |
| 8 | **Саратовские прочие** (Правобережное 26, Приволжское 9, Центральное 7, Заволжское 1) | 43 | Профиля нет ни у одного |
| 9 | **Мордовэнерго** 49 | 49 | Широкий профиль, форматов не знаем |

**На что смотреть особо (наследие сессии 24):**
- **Заявленный пробег и единица измерения.** Самый вероятный источник регрессии. Ищем акты, где «км» стоит в шапке таблицы далеко от числа: значение перестанет извлекаться. Принимаются «52,57 км», «(км) 20», «км\n318», «, км | 51» — всё, что вне этого, недоберётся.
- **Регион в номере.** Теперь берётся и через пробел. Проверить, что объекты по полному номеру в гео находятся (у части объектов гео заведён БЕЗ региона; страхует fuzzy `find_object_by_plate`).
- **Кламп года по `created_at`.** Порог «не опечатка» — 10 лет вперёд. Проверить, что настоящие старые заявки (разбор за прошлый год) не прижались.
- **Ключ «1С» и «гл»** — самые короткие, самые рискованные. Искать ложные срабатывания в текстах, где «1С» это название программы, а «гл» — часть слова.

**Что делать с находками.** Тот же порядок, что сработал: сначала диагностика БЕЗ патчей (отдельный проход), потом починка пачкой по классам дефектов, потом регресс-прогон старый-против-нового на всём корпусе, и только потом деплой. Продуктовые развилки (лестница вердиктов, стратегии профилей) — выносить пользователю с ЦИФРАМИ по `training_samples`, а не с рассуждениями: именно так была поймана ветка с 44% ложных закрытий.

**Бюджет ИИ.** Прочёсывание бесплатное. Платные `/batch/ai` — только точечно, там, где надо сравнить вердикт правил с мнением модели.

**Ближайшая крупная задача — объединить проект с okdesk-console** (Приоритет 5 в `ПЛАН_РАЗВИТИЯ.md`): начинать с РАЗВЕДКИ, а не с переноса данных. Мы уже читаем её SQLite напрямую (`/home/okdesk/okdesk-console/app.db`, таблицы `templates`+`categories`), но не знаем, что в консоли есть сверх шаблонов и кто ей ещё пользуется.

**Найдено попутно, не чинилось:** `issue_cache.description` ПУСТ у всех 4642 заявок, хотя в живом Okdesk описание есть. Разбор это переживает (тянет вживую), но список, поиск и ИИ-чат работают вслепую. Решить, чинить ли синхронизацию.

---

## Сессия 24 — правка строк разбора и 10 дефектов по боевой выборке

**Задача 1 — оператор правит таблицу разбора.**

1. **Правка вердикта в ОДИНОЧНОЙ заявке.** Бэкенд умел это с сессии 22 (`_objects_doc` фолбэчится на kind=parse) — не хватало UI: вердикт рисовался read-only пилюлей. Select-оверлей вынесен в общий `VerdictCell`, правка номера/даты — в общий `ParseEditCell` (были две копии по ~130 строк).
2. **Добавление строки** — `POST /batch/row` (`plate`, `date`, `file?`). Оператор задаёт только номер и дату, телеметрию и вердикт по правилам считает система (`verdict_source` остаётся `rules` — решение приняла машина, пилюля «✎ оператор» была бы обманом). Строка помечается `manual_row`.
3. **Удаление строки** — `POST /batch/row/delete` с модалкой подтверждения. В теле идут `index` + снимок (`plate`/`date`/`file`): расхождение → 409, чтобы не снести соседнюю строку, если список перестроился в другой вкладке. **Единственную строку удалить нельзя** (400): `_objects_doc` считает пустой `objects` отсутствующим разбором, и добавить строку обратно было бы уже некуда. Удаление НЕ запоминается — решение пользователя, форс-прогон вернёт строку.
4. **Правка пробега** — `POST /batch/mileage` (ПЛ и «ГЛОНАСС заявл.»). **Гео не дёргается**: пробег из акта — данные клиента, телеметрия от них не зависит. Вердикт пересчитывается из уже сохранённой телеметрии через `_verdict_from_facts`; `heuristic_category` пересчитывается тоже (она зависит от ПЛ и уходит в промпт как «подсказка_эвристики» — стухшая подсказка испортила бы следующий платный прогон). Строку с `verdict_source=operator` пересчёт не трогает. Пустой ввод = стереть (`clear_sheet`/`clear_declared`: `None` означает «не менять»).
5. **Правки больше не пропадают после F5** — найдено при реализации, оказалось дороже самой задачи. Одиночная карточка рисует кэш `automate` (он богаче), а все ручные правки пишутся в `parse`: до перезагрузки правка видна, после — таблица снова берёт `automate`. Касалось не только вердикта, но и правок номера и даты. Решение: бэкенд ставит документу флаг `operator_touched`, фронт с ним считает главным `parse`. Альтернативы хуже — дублировать правки в оба кэша (в `automate` вердикт зовётся `category`) или гонять разбор при каждом открытии карточки.
6. **Предупреждение перед переразбором.** «Обновить разбор» перезаписывает кэш ЦЕЛИКОМ, ручные правки терялись молча. Теперь при наличии правок — модалка со списком, что именно будет потеряно. Сохранение правок при переразборе НЕ делаем — осознанный выбор пользователя.
7. **«ИИ ошибся? Отметить»** — после правки вердикта, который поставил ИИ, рядом со строкой появляется тихое предложение записать `ai_feedback` (`bad` + `wrong_verdict` + правильная категория). Только по клику: автоматически не пишем.

**Задача 2 — боевая диагностика.** 26 открытых заявок Рогозина, бесплатный разбор (`/parse` ×26, `automate_batch` ×4), ноль вызовов DeepSeek, сервис всё время `active`. Найдено 10 дефектов, починены все.

| # | Дефект | Заявок | Как починено |
|---|---|---|---|
| 1 | ПЛ не читается: «пут,л», «пут л.», «по 1С», «по спидометру» | 8 | `_WAYBILL_KEY_FULL`/`_WAYBILL_KEY_SHORT` — одна константа на оба места разбора |
| 2 | «Заявлено клиентом» не читается: «данные гл», «по Галактике», «в ПК» в актах | 8+ | `_DECLARED_KEY_*`, `_GLONASS_KM_RE` → `_DECLARED_KM_RE` (имя врало после слияния наборов) |
| 3 | Дата «за 27.07» без года | 2 | год из `created_at`, с поправкой на переход через Новый год |
| 4 | Год из будущего («с 01.07.2028») | 1 | кламп к году создания; порог 10 лет, дальше — не опечатка, а промах разбора |
| 5 | Фантомное ТС `2393СО` из «внут . 23-93 **сот**» | 1 | граница слова справа у обратной ветки спецномера |
| 6 | Общая дата не раздаётся строкам; спецномер двоится | 2 | раздача при ЛЮБОМ числе строк (своя дата не перебивается, 63617); дедуп через `_special_candidates` |
| 7 | Номер заявки как пробег: «ССМ № _169_» | 1 | единица обязательна, «№» запрещён |
| 8 | Регион теряется при пробеле: «А 908 АУ 73» | 4 | необязательный пробел перед блоком региона |
| 9 | Трёхзначный год «22.07.226» | 1 | `_norm_typo_year`, только для «3 цифры, начинается с 2» |
| 10 | «требуется удалённая диагностика» в описании не поднимает флаг | 2 | детектор принимает `*texts` (тема + описание + комментарии); `_POWER_OK_RE` расширена наречиями |

**Профили ПО перестроены** (решение пользователя): ключи составные («Саратовские РС Северное ПО»), выбор по САМОМУ ДЛИННОМУ совпадению, а не по первому в списке. Чинит два класса ошибок: «Оренбургэнерго Северное ПО» и «Чувашэнерго Северное ПО» больше не ловятся САРАТОВСКИМ профилем (117+1 заявка), «Ульяновские РС Барышское/Димитровградское/Южное ПО» перестали быть без профиля (20 заявок) — им дан НЕЙТРАЛЬНЫЙ профиль с пометкой «формат не подтверждён примерами», чужую стратегию не приписывали. Всего 13 компаний сменили профиль, 62 без изменений.

**Главное решение сессии — ветка лестницы «заявлено ≈ система → Данные верны» развёрнута в «Проверить».** Совпадение заявленного клиентом с нашей системой доказывает лишь, что клиент ПРАВИЛЬНО ПРОЧИТАЛ наш экран, — а жалоба-то на разрыв ПЛ↔система. Проверка на 27 боевых заявках из `training_samples`: из 9 «Данные верны» **ложны 4** (64873 — ПЛ 61 при системе 3.3, оператор ответил про глушение; 64185 и 64181 — тоже глушение; 65268 — не было питания). 44% ложных закрытий на ветке, ведущей к автоответу клиенту. Цена решения: теряем ~5 верных автоответов из 27. В коде оставлено обоснование с цифрами и пометка «не восстанавливать». Приоритет «Глушение» над совпадением пробега пользователь решил ОСТАВИТЬ как есть.

**Проверено на прод-данных после деплоя:** 17 из 20 дефектов подтверждены починенными сразу, 2 хвоста найдены и добиты (см. ниже), регрессий на контрольной выборке 22 заявок других ПО — **ноль** (ни одна строка не пропала, ни одно значение не обнулилось, ни один вердикт не изменился). Главный риск НЕ реализовался: на боевом PDF 65920 все три значения «в ПК» извлеклись до сотых (3.59 / 100.52 / 67.6) там, где раньше было пусто.

**Два хвоста, найденные боевой проверкой** (коммит `3562abd`):
- **Регион был в шапке разбора, но пропадал в СТРОКАХ.** Строки берут номера через `extract_all_plates` → `_PLATE_STD_RE`, а она принимала регион только слитно; `_PLATE_RE` (шапка) пробел уже умела. Хвост региона у обеих регулярок сведён к одному.
- **Дата «за 22.07.226г» из комментария** не читалась не из-за трёхзначного года (`_norm_typo_year` работает), а потому что комментарии доходили до парсера только при ПУСТОМ описании (64871, форвард-письма). Теперь добирают ТОЛЬКО пустую дату.
- Попутно сужено против выдумывания данных: дата из переписки едет к строкам параметром `fallback_date`, а не подмешиванием дайджеста в текст. Иначе построчный движок заводил ТС по номеру из ОТВЕТА ОПЕРАТОРА и склеивал его с датой из комментария про ДРУГУЮ машину (65847) — телеметрия запрашивалась для чужой пары «номер + дата», и бессмысленный вердикт выглядел достоверным. Дата у заявки общая, список машин — нет. Проверено: 65847 и 65851 больше не получают выдуманных строк.

**Первый реальный платный прогон `/batch/ai`** (65915, 17 строк) — состоялся, и сразу нашёл бага, из-за которого фича не работала вообще: `max_tokens=2048` был зашит в `LLMClient` жёстко и общий на все вызовы. Пакетный разбор пишет по абзацу на объект плюс сводный текст — JSON обрывался недописанным, вызов уходил впустую, а надпись «модель вернула не тот формат» была неотличима от сбоя сети. Починено (коммит `b58574b`): потолок считается от числа строк `min(8192, 600 + 260*N)`, `finish_reason` и хвост непарсящегося ответа пишутся в лог, сбой связи отделён от неверного формата.

После починки прогон отработал за 24 с: **объекты не перепутаны местами** (главный страх — на этом ломался `compose_aggregate_answer`), сводный текст сгруппировал 11 глушений и одну исправную, валидатор чужих гос.номеров дал `summary_note: None`, **5 строк с ручным вердиктом модель не получила и не тронула**. Но: уверенность 0.96–0.99 у ВСЕХ 12 строк (как сигнал бесполезна, а на ней висит порог `auto_eligible` conf≥0.8), и все 12 категорий совпали с вердиктом правил — модель не даёт второго мнения, а рационализирует готовый ответ (по А499НТ назвала признаком глушения «среднее спутников 12.1», что есть норма). Решено вернуться к этому, когда наберётся статистика оценок.

---

**Сделано в сессии 23, часть 3 — два явных источника ответа и правка полей Okdesk** (коммит `e0f5a1c`, задеплоен):

1. **Найдена причина путаницы «почему ответ такой, если ИИ не вызывался».** Чип «✦ черновик ИИ» в баре для заявок с вложениями звал `/compose_answer` → `compose_aggregate_answer`, а тот собирает текст **детерминированно в коде** (группировка гос.номеров по вердикту + готовые формулировки; модель оттуда убрали ещё в 64435, она путала, какой ТС в какой группе). Бейдж «ИИ не вызывался» не врал — врал чип. Кнопки «Один ответ по всем объектам» на 65915 не было вовсе: она показывалась только агрегатным (ОДКР) заявкам.
2. **Теперь два источника, оба в липком баре** (решение пользователя): «по правилам ▾» — бесплатно, охват на выбор (все объекты / выбранный ТС; для одной строки берётся шаблон её категории из `_CATEGORY_CATALOG` с датой и реальным пробегом), доступно ЛЮБОЙ заявке — `POST /compose_answer?scope=all|object`. «✦ ИИ ▾» ничего не запрашивает: тексты приносит единственный платный вызов `/batch/ai` — черновик по строке и **сводный по всей заявке, который модель пишет сама** (новое поле `ai_summary_answer`, тот же один вызов). До прогона чип погашен и подсказывает, куда нажать. Кнопка из «Разбора» убрана — одно действие не живёт в двух местах.
3. **Сводный текст модели проверяется КОДОМ**: упомянула гос.номер, которого в заявке нет — текст выбрасывается целиком (`summary_note` объясняет почему), остаётся сводник по правилам. Лучше без текста, чем с чужой машиной в письме клиенту.
4. **Правка полей Okdesk из карточки** — `PATCH /issues/{id}/fields` с белым списком: тема, `deadline_at`, `priority`, `planned_execution_in_hours`. Приоритеты из справочника `GET /issues/meta/priorities` (проверено на проде: `low`/`normal`/`high`), коды не хардкодим. Правка тихая и по ОДНОМУ полю: Enter сохраняет, Esc отменяет, ошибка в одном не откатывает остальные. После сохранения кэш заявки обновляется полным `cache_single_issue` (у `refresh_single_issue` только статус и ответственный). **Не даём менять компанию и контакт** — подмену клиента у живой заявки не откатить; наблюдатели/оборудование/объект обслуживания требуют пикеров по справочникам Okdesk, это отдельный шаг.
5. Удалена эвристика `isBatchIssue` («есть вложения ИЛИ ≥2 номера в тексте») — карточка знает фактический состав разбора из `onParse`.

**Сделано в сессии 23, часть 2 — правки по замечаниям с прода** (коммит `05d504b`, задеплоен):

1. **Ширина карточки — процент вместо жёстких 680px**: `clamp(680px,46%,1100px)`. Оператор проводит в карточке больше времени, чем в трек-панели, и на широком мониторе половина экрана простаивала. Границы: 680 — все 8 колонок разбора без скролла, 1100 — дальше строки неудобно длинные. Одно правило на все разрешения (1366→680, 1600→736, 1920→858, 2560→1100).
2. **Панели открываются на всю ширину.** При открытом треке от списка оставалась полоса 280px с порогом 1600px, а кнопка ⤢ прятала список, но карточка оставалась 680px — справа зияла пустота. Теперь список уходит целиком, развёрнутая карточка получает `flex-1`. Треку хватает 460px (было 520): иначе на 1366 сумма с карточкой уезжала в горизонтальный скролл.
3. **Поле ответа на всю ширину своей строкой**, кнопки уехали в ряд ниже и стали иконками (☰ шаблон, ⋯ ещё, ⌄ свернуть); «Ответить и решить» остался с текстом. Высота растёт под текст до 240px вместо фиксированных 84px. Место под скроллбар зарезервировано (`scrollbar-gutter: stable`) — из-за него правый скруглённый угол выглядел обрезанным.
4. **Список выбора вердикта был НЕВИДИМ** (чёрный по чёрному): у прозрачного `select`-оверлея стоял `text-transparent`, а `option` наследуют цвет от select. Плюс класс `text-primary` на пунктах не работал вовсе — в проекте это имя ЦВЕТА (`text-text-primary`), а не утилита. Цвет пунктов задан инлайном.
5. **Строки таблицы разбора разжаты** (`py-1`→`py-2`, разделитель `border-line`): высота строки 30→38px, 20 строк больше не слипаются в полосу.

---

**Сделано в сессии 23, часть 1** (коммит `152f911`, задеплоен):

1. **Один платный вызов ИИ на всю заявку — `POST /issues/{id}/batch/ai`.** Главный разрыв закрыт: `analyze_batch` считал строки только правилами, а `automate` умел один ТС и на пакетной заявке возвращал `null` — уверенности, обоснования и черновика по объектам не было вообще. Теперь модель получает факты ВСЕХ строк разом (до 25 за вызов, дальше строки остаются с вердиктом правил и об этом печатается `ai_note`) и отвечает по каждой: категория из каталога, `confidence`, `reasoning`, `draft_answer`. Ручные правки оператора не перезаписываются — строки с `verdict_source=operator` в модель не уходят; строки без телеметрии тоже (обосновывать нечего, а выдумывать факты модель не должна). Категорию вне каталога отбрасываем: вердикт правил надёжнее выдумки.
2. **Платная кнопка одна на карточку и переехала в «Телеметрию»** — туда, где появляются её результаты. Для мультиобъектной заявки жмёт `/batch/ai`, для одиночной — `automate` (он богаче: few-shot, проверка возобновления данных, разбор комментариев). Обоснование, доверие и черновик читаются ИЗ СТРОКИ разбора, у каждого ТС свои. Чип «черновик ИИ» в баре теперь ВСТАВЛЯЕТ готовый текст без запроса (раньше всегда стрелял платным вызовом); сводный ответ по ОДКР — отдельной кнопкой в «Разборе».
3. **Смена статуса всегда через модалку.** «В работе» и «Ожидание ответа» уходили из меню молча, причём дата возврата подставлялась сама (+3 дня), а обязательный для Okdesk комментарий мог оказаться пустым. Добавлено «Нет времени». Гард «Сначала укажите тип заявки» висит в баре ПОСТОЯННО (был тост уже после отказа) и дублируется в модалке.
4. **Пер-файловый статус OCR** в `GET /attachments` (`done` / `partial` + `pages_done` / `queued` / `unavailable`) — данные лежали в кэше `ocr:<att_id>` и наружу не выходили: у любого извлекаемого файла писалось «ИИ читает», в том числе у PDF, прочитанного на 4 страницы из 6. В секции «Вложения» — плитки с типом файла и агрегатный прогресс; ДЕЙСТВИЕ «Продолжить распознавание» осталось в «Разборе» (это тот же прогон, что «Обновить разбор»).
5. **Правка даты в строке разбора** — `POST /issues/{id}/batch/date`, симметрично `/batch/plate`: телеметрия и вердикт строки считаются заново за новую дату. Раньше дату можно было поправить только у одиночной заявки полным платным `automate`.
6. **Связанные заявки со статусом и темой** (Okdesk в связях отдаёт только id — подписи бэкенд достаёт из локального кэша, нет в кэше → строка без темы, но со ссылкой). **Ссылка «открыть в Okdesk»** — поле `okdesk_url`, адрес портала знает только бэкенд (`OKDESK_BASE_URL` минус `/api/v1`). **Автор и время ручной правки вердикта** — `verdict_edited_by`/`verdict_edited_at`; при ручной правке обоснование и черновик ИИ снимаются, иначе текст спорит с пилюлей.
7. **Прочее по вёрстке:** панель шаблонов из модалки на весь экран — в инлайн-поп над баром (авто-плейсхолдеры `[сегодня]/[вчера]/[завтра]` заблокированы, считаются по МСК); «Детали заявки» на строках ключ/значение с тихим inline-edit и счётчиком «тип · параметры N/3»; оценка разбора на пилюлях + вопрос-строка и счётчик в заголовке; регион в подвале выводится из кода в гос.номере; лента комментариев показывает последние 4, а не один.
8. **Разбор сведён к ОДНОМУ источнику данных** (кэш запроса react-query, все правки пишут туда же через `setQueryData`). Иначе результат прогона ИИ не попадал бы в уже отрисованную таблицу: в компоненте жили ещё `localBatch` и `run.data`, которые перебивали кэш.
9. **Макет `.figma-shots/card-v4-variants.html` догнан по обе стороны.** Выброшены выдуманные данные: «~6 тыс. токенов · ≈ 8 с» на платной кнопке и «ещё версии» вердикта с процентами — таких полей система не отдаёт (модель возвращает одну категорию, уверенность, обоснование и черновик). Действие OCR перенесено в «Разбор», подписи статусов файлов сведены с кодом, добавлены метка правки даты, сводный ответ по ОДКР, связь без темы и пояснение источника региона. В «Заметках для реализации» — 6 новых пунктов (17–22) про новые поля и эндпоинты.

**Проверено в сессии 23:** `tsc -p tsconfig.app.json --noEmit`, `eslint src/`, `npm run build`, `py_compile` — чисто; правки макета сверены ЗАМЕРАМИ DOM в браузере (не на глаз); на сервере после деплоя проверены живьём: сервис `active`, HTTP 200, `okdesk_url` отдаётся (`https://gpspos.okdesk.ru/issues/65936`), пер-файловый `ocr` приходит на реальной заявке с 21 вложением, `/batch/ai` и `/batch/date` корректно отбивают невалидные запросы (400).

**НЕ проверено:** **ни одного реального платного прогона `/batch/ai`** — он перезаписал бы вердикты живой заявки, это решение пользователя. Первый прогон стоит сделать на пакетной заявке с сохранённым разбором (например внутренний id `4601` = внешний `65845`, 21 вложение) и посмотреть, что per-object `confidence`/`reasoning`/`draft_answer` встали в строки. Карточка живьём глазами тоже не смотрена: вход в прод требует пароля.

**Сделано в сессии 22** (коммиты `2f99758`, `bc9077f` + правка названия вкладки; запушено в `main`, задеплоено в сессии 23):

1. **Порядок блоков карточки переставлен под маршрут оператора** — «что просят → в каких условиях → чем подтверждено → что решил ИИ → чем это проверить → что уже говорили → было ли похожее». Итог: вопрос клиента → **детали заявки** → вложения → разбор → телеметрия → **оценка разбора** → комментарии → связанные заявки → **передать монтажникам**. Детали подняты с 7-го места на 2-е и раскрываются сами, когда тип заявки не указан: без типа Okdesk не пустит заявку ни в «В работе», ни в «Решена», а тип правится именно там. Оценка разбора поднята с 8-го на 6-е — в конце карточки её не открывали, а из неё растёт обучение ИИ.
2. **«Передать монтажникам» — из липкого бара в отдельную секцию.** Это не кнопка-действие, а генерируемый текст, который читают перед отправкой. Текст собирается только по клику на формат, DeepSeek не тратится на заявках без монтажника. `InstallerPreview`/`PreviewCard` удалены как мёртвый код.
3. **Макет-эталон взят под git.** `.figma-shots/` целиком лежал в `.gitignore` — эталон дизайна v4 жил только на одной машине. Паттерн заменён на `.figma-shots/*` + негативное правило на `card-v4-variants.html` (наивное `!` поверх каталога не работает: git не возвращает файл, если исключён сам каталог). PNG-скриншоты по-прежнему игнорируются через `*.png`.
4. **Название вкладки браузера** — было дефолтное `Vite + React + TS`, стало «GPSPOS · Разбор заявок»; `lang` документа с `en` на `ru`.
5. **`verdict_source` доведён до UI.** `POST/GET /issues/{id}/parse` заведены в API-клиент; при открытии карточки сначала кэш `/parse`, при промахе — пересчёт **без** `attachments` (0 токенов, без OCR); включается только когда заявка не пакетная и кэша `automate` нет. Следствие: **одиночная таблица есть до платного прогона** — раньше она рисовалась исключительно из кэша `automate`. Три вида пилюли (пунктир «по правилам» / ✦ ИИ / ✎ оператор), строка расхождения «правила: X → ИИ: Y», бейдж «ИИ не вызывался». Полоса доверия и «Почему такой вердикт» теперь только при источнике `ai` — до этого при вердикте по правилам подставлялась уверенность прошлого ИИ-прогона, то есть проценты объясняли не тот вердикт. Подпись у платной кнопки без выдуманных чисел («платный шаг, отвечает ИИ»); «~6 тыс. токенов · ≈ 8 с» из макета выброшено — такого расчёта в проекте нет.
6. **Макет догнан до кода по 18 пунктам** (демо-режим, авто-дораспознавание PDF, ОДКР, спецтехника, ошибки API, «Сроки» и поля Okdesk, запоминание раскрытости и др.). Состояния-исключения спрятаны за переключатели вверху макета, экран не превратился в свалку предупреждений. Правило на будущее — в памяти `feedback_mock_code_parity`: синхрон в обе стороны, где код богаче — прав код, макет не выдумывает данных, которых нет в системе.

**Проверено в сессии 22:** `tsc -p tsconfig.app.json --noEmit` чисто, `npm run build` проходит; порядок секций, авто-раскрытие «Деталей» и активная секция сверены замерами DOM на макете (не на глаз). **НЕ проверено:** карточка живьём — нужны данные Okdesk с бэкенда, локальная база пустая; первый реальный прогон `/parse` только на сервере.

**Грабля, найденная в сессии 22 при проверке за субагентом:** запоминание раскрытости секций срывало лаймовую полосу на последнюю ВОССТАНОВЛЕННУЮ секцию вместо «Разбора». Событие `toggle` у `<details>` асинхронное, защита через `setTimeout(…, 0)` порядок не гарантирует — нужен флаг на самом элементе («восстановление ≠ выбор оператора»). Субагент этого не увидел, потому что проверял на чистой загрузке с пустым localStorage. Урок: состояния, зависящие от сохранённых данных, проверять на ВТОРОЙ загрузке.

**Карта разрыва сессии 22 закрыта в сессии 23 целиком** (21 пункт вёрстки + 8 бэкендных + 3 решения пользователя: URL портала берём из конфига бэка, регион выводим из гос.номера, стоимость вызова оператору не показываем).

**Сделано в сессии 21** (коммиты `e384238`, `fd3e97d`, `4ca37f9`):

1. **Разбор разделён на 3 уровня по стоимости** — главное архитектурное решение сессии, детали в памяти `project_free_parse_levels`. Выяснилось по коду: `analyze_batch` не тратит DeepSeek вообще (дорог только OCR), а детерминированный вердикт по правилам существовал всегда, но наружу не выходил. Уровни: факты бесплатно при открытии → OCR по кнопке → ИИ по кнопке.
2. **Бэк:** `POST/GET /issues/{id}/parse` — факты + предварительный вердикт без единого вызова DeepSeek, для заявки любого типа с гос.номером. Снят порог «≥2 номера» в `analyze_batch` (одиночная заявка идёт тем же движком — одинаковая таблица для 1 и N ТС). Лестница вердиктов вынесена в `_verdict_from_facts` **без изменения порядка правил** (сверено с `HEAD` построчно). Новые поля `verdict_source` (`rules`/`ai`/`operator`) и `heuristic_category`. Отдельный kind кэша `parse`, чтобы ИИ-шаг и факты не затирали друг друга.
3. **Фронт, шаг 2 переноса макета:** короткие заголовки таблицы разбора с тултипами (единый `ParseTableHead` для одиночной и пакетной), пояснение «строка — объект, клик выбирает», кнопка «Разобрать N вложений», подпись «Почему такой вердикт» в телеметрии. Лаймовая полоса теперь горит у ОДНОЙ активной секции (по умолчанию «Разбор», при сворачивании гаснет).
4. **Макет** `.figma-shots/card-v4-variants.html` дорисован до кода: модалка решения, шаблоны с плейсхолдерами, оценка ИИ, тихий inline-edit параметров, связанные заявки, прогресс OCR. Правило полосы приведено к коду (`.sec.active` вместо `.sec[open]`).

**Проверено:** `tsc -p tsconfig.app.json`, eslint, `py_compile` — чисто. **НЕ проверено:** ни одного прогона против реальных данных — локальная база пустая, бэкенд локально не поднять из-за конфликта Telegram-бота. Первый реальный прогон `/parse` — на сервере.

**Сделано в сессии 20** (коммит `0bc7c0c`, НЕ задеплоено на сервер):
- Утверждён дизайн **v4 «плоские строки»** как основа всего проекта: фон `#0A0A0A`, зеленоватый `bg-card` (#252D25) как подложка секций больше не используется, разделение 1px-линиями (новый токен `line: #262626`), лаймовая полоса 2px слева = раскрытая секция.
- Эталон макета: `.figma-shots/card-v4-variants.html` — автономный HTML, открывать в браузере, интерактив на JS. С сессии 22 файл **в репозитории** (остальное содержимое папки по-прежнему игнорируется).
- Карточка заявки: одна колонка, порядок «вопрос → вложения → разбор → телеметрия → комментарии → связанные → детали → оценка», ответ и копирование монтажнику переехали в липкий бар снизу.
- Трек-панель: flex-сосед слева от карточки, анимация `transform` (не `width`), карточка неподвижна. Магические `55%` убраны в константы `RAIL_W` / `WIDE` в `App.tsx`.
- Цвета статусов Okdesk осветляются до контраста AA 4.5:1 (`statusTextColor` в `src/lib/status.ts`).

**Шаг 2 — ✅ сделан в сессии 21** (см. выше), был про внутренности разбора и телеметрии:
- таблица разбора **всегда**, даже для одного ТС (унифицировать `SingleParseTable` и `BatchAnalysis`); все 8 колонок влезают в 648px за счёт коротких заголовков, полные подписи — в тултипы;
- клик по строке таблицы = выбор объекта, телеметрия ниже показывает данные этого ТС;
- в телеметрии добавить явную подпись блока обоснования — «Почему такой вердикт» (сейчас reasoning без заголовка);
- если несколько вложений/ТС и разбор не запускался — кнопка «Разобрать N вложений» вместо таблицы.

**Отложено, оформлено задачами:** плавное закрытие трек-панели (сейчас размонтируется мгновенно); лаймовая полоса должна гореть только у последней активной секции, а не у всех раскрытых сразу.

**Грабли, на которые не наступать:**
- `npx tsc --noEmit` из `frontend/` проверяет **ноль файлов** (`"files": []` + references) и всегда даёт ложное «чисто». Реальная проверка: `npx tsc -p tsconfig.app.json --noEmit`.
- Локальный запуск бэкенда поднимает второй экземпляр Telegram-бота и конфликтует с рабочим (`TelegramConflictError`) — не держать запущенным долго.
- Локальная база пустая; для проверки глазами копировать с сервера: `scp -i /c/Users/sPec/.ssh/id_ed25519 root@155.212.186.165:/opt/support-ai-agent/support_agent.db ./support_agent.db` (7,9 МБ, 1156 заявок). Запуск бэкенда — конфиг `backend` в `.claude/launch.json`.

---

## Цель

Telegram-бот + веб-дашборд для техподдержки GPSPOS.  
Бот принимает вопросы, вызывает инструменты (Okdesk, GPSPOS, GPSPOS Geo) и возвращает ответ.  
Дашборд — интерфейс для управления заявками Okdesk.

---

## Сервер (прод)

| Параметр | Значение |
|---|---|
| Host | `155.212.186.165` (Beget VPS, Ubuntu 24.04) |
| User | `root` |
| SSH-ключ | `/c/Users/sPec/.ssh/id_ed25519` |
| Путь к проекту | `/opt/support-ai-agent` |
| Порт | `8001` |
| Сервис systemd | `support-ai-agent.service` |
| Диск | 69% занят (5.9G/8.7G, 2.8G свободно) после чистки 2026-07-23 (снёс 4 старых `.vscode-server`, vacuum journal, apt clean). Раньше упирался в 98% — мониторить |

**Рядом на сервере:** `/home/okdesk/okdesk-console/` — Okdesk Console (порт 8000, `okdesk.service`)

SSH-команда: `ssh -i /c/Users/sPec/.ssh/id_ed25519 root@155.212.186.165`

---

## Архитектура

```
support-ai-agent/
├── app/
│   ├── main.py                         # FastAPI + lifespan + SPA fallback + bg refresh
│   ├── static/                         # ✅ Собранный React (Vite build output)
│   ├── api/v1/
│   │   ├── router.py                   # агрегатор роутов
│   │   └── endpoints/
│   │       ├── webhooks.py             # POST /webhooks/okdesk
│   │       ├── test.py                 # POST /test/pipeline
│   │       ├── issues_dashboard.py     # ✅ 7 endpoints для дашборда
│   │       └── employees.py           # ✅ GET /employees — список сотрудников
│   ├── core/
│   │   ├── db/
│   │   │   ├── database.py            # SQLite async (support_agent.db)
│   │   │   └── models.py              # IssueCache (+ assignee_name), AnalysisCache, ...
│   │   ├── services/
│   │   │   └── cache_service.py       # ✅ CacheService: refresh, assign, query
│   │   └── okdesk/
│   │       ├── models.py              # Issue, Employee, IssueParameter, ...
│   │       └── service.py             # assign_issue, list_employees, ...
│   └── services/
│       └── intelligence_service.py
├── frontend/                          # ✅ React 18 + TypeScript + Vite
│   └── src/
│       ├── App.tsx                    # Layout + UserSelector ("Я:")
│       ├── components/
│       │   ├── IssuesList.tsx         # Таблица + пагинация (20/50/100)
│       │   ├── IssueDetail.tsx        # Детали: все поля Okdesk + ответственный
│       │   ├── IssueFilters.tsx       # Фильтры: статус, компания, поиск
│       │   └── StatusBadge.tsx        # Цветные бейджи статусов
│       ├── store/
│       │   ├── issuesStore.ts         # Zustand: фильтры, пагинация
│       │   └── userStore.ts           # Zustand: текущий пользователь (localStorage)
│       ├── api/client.ts              # axios: все API вызовы
│       └── types/index.ts             # TS интерфейсы
└── CONTEXT.md / WEB_DASHBOARD_ARCHITECTURE.md
```

---

## Статус компонентов

| Компонент | Статус | Примечание |
|---|---|---|
| **Telegram бот** | ✅ Работает | aiogram 3.x, polling |
| **AIAgent + Tools** | ✅ 12 инструментов | DeepSeek, Okdesk/GPSPOS/Geo |
| **IssueCache** | ✅ 1000 заявок | пагинация page[number]/page[size], обновление каждые 5 мин |
| **Dashboard API** | ✅ Задеплоен | 7 endpoints: list, detail, refresh, assign, comments, analysis |
| **Employees API** | ✅ Задеплоен | GET /api/v1/employees |
| **React Frontend** | ✅ Задеплоен | Phase 2 завершён |
| **Ответственный** | ✅ Работает | batch sync для всех заявок, "Взять себе", picker по группам |
| **Детали заявки** | ✅ Полные | описание, сроки, параметры (с извлечением телефона), связанные |
| **Пагинация** | ✅ 20/50/100 | селектор в нижней панели таблицы |
| **Wialon** | ⚠️ Заглушка | клиент есть, в агент НЕ подключён |
| **Автоанализ заявки** | ✅ Phase A | парсинг → geo DailyStat+ObjectPackets → классификация (DeepSeek) → черновик ответа |
| **Пагинация persist** | ✅ | `limit` сохраняется в localStorage (issues-prefs) |
| **Вложения + OCR** | ✅ Фаза B | список/просмотр + ИИ читает PDF/Word/Excel/сканы (tesseract rus+eng, PyMuPDF) |
| **Карта+графики заявки** | ✅ | Leaflet трек + uPlot телеметрия, зум, синк с картой |
| **Задел обучения ИИ** | ✅ | training_samples при решении + бэкафилл из решённых (`/training/backfill`, `/training/stats`) |
| **Разбор по форматам вложений** | ✅ | PDF-акты/Word/Excel/сканы: `_parse_act_blocks`/`_parse_body_vehicles`/`_parse_summary_table`/`_parse_grouping_table` |
| **Формат-профили ПО** | ✅ | `_FORMAT_PROFILES` (Волжское/Жигулёвское/Чапаевское/Самарское/… Россети Волга) подсказывают стратегию разбора + промпт |
| **Спецтехника** | ✅ | `_is_special_vehicle` — спецномер/модель без км-вердикта; бейдж в таблице |
| **Детектор «ложного wait»** | ✅ | `_client_awaiting_remote_diag` — клиент подтвердил питание/просит удалённую диагностику → предупреждение в анализе |
| **Колонка «ГЛОНАСС заявл.»** | ✅ | в таблице разбора: заявлено клиентом vs реальный пробег «По факту» |
| **Редактируемый вердикт** | ✅ | `POST /batch/verdict`, ключ строки = (номер, дата, файл); вердикт-по-дате; работает и в ОДИНОЧНОЙ заявке |
| **Правка строк разбора** | ✅ Сессия 24 | добавить/удалить строку (`/batch/row`, `/batch/row/delete`), правка пробега (`/batch/mileage`); правки переживают F5 (`operator_touched`), переразбор предупреждает о потере |
| **Единый мастер ИИ** | ✅ | Разбор → Анализ → Ответ; авто-режим с вложениями и без |
| **Сроки/SLA + уведомления** | ✅ | `deadline_at` в кэше, бейдж срочности; TG-рассылка о дедлайнах — **включена** (`TELEGRAM_ALERT_CHAT_ID` задан) |
| **Петля обратной связи ИИ** | ✅ | оценка оператора good/bad+тип ошибки (`ai_feedback`), экран «Оценки ИИ» (`/issues/ai_feedback/list`) |
| **Копирование монтажнику** | ✅ | `GET /installer_export` — готовые тексты КАЛЕНДАРЬ/МЕССЕНДЖЕР (телефон/контакт из параметров заявки→get_contact) |
| **Редактирование параметров** | ✅ | `POST /issues/{id}/parameters` — Местоположение/Контакт/Телефон (PATCH custom_parameters) → разблокирует «В работе» |
| **Личные шаблоны** | ✅ | `app_templates.user_id`: NULL=общий, username=личный; дедуп/видимость по владельцу |
| **Модель ИИ** | ✅ | `deepseek-v4-flash` (env `DEEPSEEK_MODEL`, default в коде) |

---

## Сотрудники техподдержки (Okdesk ID)

| Имя | ID | Группа |
|---|---|---|
| Свириденко | 21 | Первая линия |
| Рогозин | 22 | Первая линия |
| Лебедь | 2 | Вторая линия |
| Игнашкин | 3 | Вторая линия |

---

## API endpoints (дашборд)

```
GET  /api/v1/issues                    — список; фильтры status/company/search/assignee/issue_id (комбинируются)
GET  /api/v1/issues/cache/refresh      — принудительная синхронизация
GET  /api/v1/issues/{id}               — детали + live данные из Okdesk
POST /api/v1/issues/{id}/analysis      — сохранить анализ пробега
GET  /api/v1/issues/{id}/comments      — комментарии из Okdesk
POST /api/v1/issues/{id}/comments      — добавить комментарий
PATCH /api/v1/issues/{id}/assignee     — назначить ответственного
PATCH /api/v1/issues/{id}/type         — сменить тип
POST /api/v1/issues/{id}/resolve       — комментарий + смена статуса; ALLOWED = completed/delayed/opened/closed/wait/no_time/inst_fin (delayed/no_time требуют delay_to)
GET  /api/v1/employees                 — список активных сотрудников
GET  /api/v1/issue-types               — типы заявок (Okdesk issues/types)
GET  /api/v1/templates                 — шаблоны ответов из okdesk-console
POST /api/v1/issues/{id}/automate      — автоанализ + черновик (POST = прогон ИИ + кэш)
GET  /api/v1/issues/{id}/automate      — кэш автоанализа (без прогона ИИ)
GET  /api/v1/issues/{id}/track[?plate=&date=] — трек+телеметрия; plate/date = трек конкретного ТС (из разбора)
GET  /api/v1/issues/{id}/attachments   — список вложений (+ extractable/kind)
GET  /api/v1/issues/{id}/attachments/{att_id}/download — проксированное скачивание
POST /api/v1/issues/bulk/assignee|type|status          — массовое редактирование
POST /api/v1/issues/{id}/automate_batch                — разбор «общей» заявки по объектам (POST = прогон + кэш)
GET  /api/v1/issues/{id}/automate_batch                — кэш разбора (без прогона)
POST /api/v1/issues/{id}/batch/verdict                 — изменить вердикт строки разбора (ключ: номер+дата+файл)
POST /api/v1/issues/{id}/batch/plate|date              — исправить гос.номер/дату строки (телеметрия и вердикт заново)
POST /api/v1/issues/{id}/batch/mileage                 — исправить ПЛ и/или «ГЛОНАСС заявл.»; вердикт пересчитывается БЕЗ гео
POST /api/v1/issues/{id}/batch/row                     — добавить строку (plate+date; вердикт считает система, manual_row=true)
POST /api/v1/issues/{id}/batch/row/delete              — удалить строку (index + снимок; 409 при рассинхроне, последнюю нельзя)
POST /api/v1/issues/{id}/batch/ai                      — ОДИН платный прогон ИИ по всем строкам (per-object + ai_summary_answer)
POST /api/v1/issues/{id}/compose_answer                — один общий ответ по разбору (группировка по вердиктам)
POST /api/v1/issues/{id}/create_children               — создать вложенные заявки по объектам
GET  /api/v1/issues/{id}/extracted                     — извлечённые данные (номер/дата/ПЛ/ГЛОНАСС/сырой текст) без прогона ИИ
GET  /api/v1/issues/{id}/template_values               — значения для шаблонов
POST /api/v1/issues/training/backfill                  — бэкафилл TrainingSample из решённых заявок (admin, dry-run)
GET  /api/v1/issues/training/stats                     — статистика базы знаний ИИ
GET  /api/v1/issues/{id}/installer_export              — готовые тексты «монтажнику» (КАЛЕНДАРЬ/МЕССЕНДЖЕР), read-only
POST /api/v1/issues/{id}/parameters                    — обновить кастом-параметры (address/contact_person/tel_person) → Okdesk + кэш
POST /api/v1/issues/{id}/ai_feedback                   — оценка оператором ИИ-разбора (good/bad + error_kind/comment/correct_category)
GET  /api/v1/issues/{id}/ai_feedback                   — последняя оценка по заявке
GET  /api/v1/issues/ai_feedback/list[?rating=]         — список оценок (экран «Оценки ИИ»)
```
Результаты автоанализа и разбора кэшируются в таблице `result_cache` (kind=automate|batch) — UI показывает сохранённое без повторного прогона ИИ; кнопка «Обновить» = форс-прогон (POST).

## Автоматизация (Phase A) + анализ

```
app/services/issue_automation.py        # IssueAutomationService: parse → telemetry → classify → draft → track → training sample
app/services/attachment_reader.py       # PDF/Word/Excel + OCR (PyMuPDF+tesseract rus+eng)
app/core/gpspos_geo/service.py          # find_object_by_plate, get_daily_stats, get_packets
```
- **Пробег**: считается из реального трека (сумма haversine по пакетам, без телепортов) — НЕ из DailyStat.length (он отстаёт, не отражает догрузку из чёрного ящика).
- **Парсинг**: гос.номер (с/без региона), дата неисправности (приоритет «за <дата>», не первая дата отчёта), ПЛ/одометр (требует единицу км/м).
- **Категории**: Данные верны / Глушение / Не было питания / Терминал подключился / Изменили настройки / Диагностика. Глушение — по телепортам трека/потере спутников (НЕ по одиночным выбросам скорости). Обрыв трека → «Терминал подключился» (потеря связи + поздняя выгрузка). Занижение пробега vs ПЛ → conf ≤0.7 + needs_review.
- Текст вложений (вкл. OCR сканов) идёт в парсинг + промпт DeepSeek.
- Каждое решение через систему → строка в `training_samples` (факты телеметрии + ответ оператора) для будущего обучения.
- ⚠️ Снапшот в моменте ≠ финальные данные (поздняя выгрузка) — поэтому неопределённость честно помечается на проверку.

**Дата неисправности** (приоритет): «Дата неисправности <дата>» → «в системе [с] <дата>» → «за <дата>» → дата в теме → первая дата. (Первая дата в тексте обычно дата отчёта/отправки, не неисправности — особенно акты Волжского ПО.)
**Поиск объекта geo** по ядру гос.номера без региона (Х371РХ64 → находит «Х371РХ ГАЗ»).
**Назначение ответственного**: `PATCH issues/{id}/assignees` (мн.ч.) + `{assignee_id, group_id}` — группа сотрудника ОБЯЗАТЕЛЬНА (бэкенд резолвит её из employees/list). Путь `issues/{id}/assignee` (ед.ч.) — 404.
**OCR под systemd**: PATH сервиса = только venv, без `/usr/bin` → pytesseract не находил бинарь и OCR молча не работал. Путь к tesseract прописан явно в `attachment_reader._ensure_tesseract_cmd`.
**Гос.номер латиница↔кириллица**: канонизация (A759PC → А759РС) в `_norm_plate`.
**Спецтехника**: формат 4 цифры + 2 буквы в любом порядке (5297СУ / СУ5297); сопоставление с geo по канонической сигнатуре (`GpsposGeoService._special_candidates`, `app/core/gpspos_geo/service.py`; раньше звалась `_special_sig`), т.к. порядок цифр/букв и регион в данных geo непостоянны. С сессии 24 та же канонизация используется в дедупе строк разбора (`_plate_dedup_key`).
**Дата неисправности** также из ISO (YYYY-MM-DD) — для дочерних заявок (раньше ISO не читался; теперь дочерние пишут «Дата неисправности: ДД.ММ.ГГГГ»).
**Разбор по объектам**: глушение приоритетнее совпадения пробега (спуфинг/круг = трек недостоверен, не «данные верны» даже если пробег совпал). У каждого объекта — кнопка 🗺 (трек этого ТС через `track?plate=&date=`).
**Разбор «общих» заявок**: заявка с ≥2 извлекаемыми вложениями (по акту на ТС) → одиночный автоанализ скрыт, показывается «Разбор по объектам» (таблица вердиктов). По объектам «данные верны» — кнопка создания вложенных заявок.
**Создание заявок в Okdesk**: `POST issues` тело `{"issue": {...}}`. Тип на создании ИГНОРИРУЕТСЯ (ставится отдельным `PATCH issues/{id}/types`). Тип «Расхождение пробега» требует кастомный параметр `address` (Местоположение техники) непустым; коды параметров: `address`, `contact_person`, `tel_person`.
**Разбор по форматам вложений** (`automate_batch`): `_parse_act_blocks` (мульти-акт PDF, один акт = один ТС, своя дата/ПЛ), `_parse_body_vehicles` (список ТС в теле), `_parse_summary_table` (сводная таблица), `_parse_grouping_table` (XLSX «Группировка <дата>»). Глобальный дедуп ТС по вложениям.
**Формат-профили ПО** (`_FORMAT_PROFILES`/`resolve_format_profile`): мягкая привязка по подстроке `company_name` (Волжское/Жигулёвское/Чапаевское/Самарское/Ульяновское ПО и др. дочерние Россети Волга) задаёт стратегию разбора (тема/вложения, single/multi, формат акта) и подсказку для промпта.
**Спецтехника** (`_is_special_vehicle`): спецномер (4 цифры + 2 буквы в любом порядке) ИЛИ модель из списка → без км-вердикта; в таблице разбора бейдж «спецтехника», в анализе предупреждение.
**Детектор «ложного wait»** (`_client_awaiting_remote_diag`): клиент подтвердил питание/массу/индикацию ИЛИ просит «удалённую диагностику» → шаблон «включите питание» исчерпан, мяч на нашей стороне → предупреждение «удалённая диагностика».
**Пробег от клиента**: «по путевому листу/одометру» (`_WAYBILL_KM_RE`) и «в системе ГЛОНАСС/ССМ/СМС» (`_GLONASS_KM_RE`) — заявленное клиентом ОТДЕЛЬНО от реальной телеметрии geo; колонка «ГЛОНАСС заявл.» рядом с «По факту». Date-guard: ГЛОНАСС/пробег не путать с датой (`17.06.2026`) и телефоном.
**Вердикт-по-дате**: ключ строки разбора = (номер, дата, файл) — у одного ТС за разные даты разные вердикты (63617). Допустимые вердикты: Глушение / Данные верны / Не было питания / Нет данных / Терминал подключился / Проверить.
**Статусы смены** (`resolve`/bulk): ALLOWED = completed/delayed/opened/closed/wait/no_time/inst_fin. `wait`=«В работе» (без обязательного ответа клиенту), `no_time`/`delayed` требуют `delay_to`. Кнопка «В работе» снизу карточки (баг 64453/64306 исправлен).
**Калибровка глушения** (`_derive_flags`): пороги откалиброваны на 159 «глушений» vs 58 «не-глушений» из training_samples (recall 73%→78%, ложные ~10%). Спутниковые/нулевые сигналы = глушение только если ТС двигалось (у стоящего мало спутников — норма, 64275). Отдельная ветка «стоячее глушение из-за подавления» (64657): `low_sat>0.9` + питание в норме → глушение даже без признаков движения (само подавление обнулило скорость/трек). «Не было питания» НЕ ставим движущемуся ТС (64281) — только стоящему с низким напряжением.
**Петля обратной связи ИИ** (`ai_feedback`): оператор оценивает разбор — good (верно)/bad (+тип ошибки wrong_verdict/wrong_plate/wrong_date/wrong_mileage/other + комментарий + правильная категория). Хранится с категорией, которую выдал ИИ. Экран «Оценки ИИ» (`AiFeedbackReview.tsx`) — основа для роста few-shot из верных разборов и точечной правки промпта/правил по ошибкам.
**Копирование монтажнику** (`installer_export`): два готовых текста — КАЛЕНДАРЬ (телефон/компания/ТС/«не в сети с …») и МЕССЕНДЖЕР (объект/местоположение/контакт/телефон). Телефон и контакт берутся из ПАРАМЕТРОВ заявки (`tel_person`/`contact_person`), фолбэк телефона — `get_contact` (64239). Недостающие поля = плейсхолдер `____`, запрос не падает; read-only (доступно demo).
**Редактирование параметров заявки** (`POST /parameters`): Местоположение техники/Контактное лицо/Номер телефона → `PATCH issues/{id}` с `custom_parameters` (64197). Заполняет обязательные поля, без которых Okdesk не пускает в статус «В работе». UI-блок `EditableParameters` в карточке.
**Личные шаблоны** (`app_templates.user_id`): NULL = общий (виден всем), username = личный (виден/правится только владельцу). Дедуп и фильтр видимости scoped по владельцу; миграция добавляет колонку (`database._column_exists`).
**Модель ИИ**: `deepseek-v4-flash` (env `DEEPSEEK_MODEL`, default в `core/ai/llm.py`); ~$0.20/мес. Исправлено с ошибочного `deepseek-chat`.
**Шаблоны (апгрейд)**: сортировка по `usage_count`, дедуп по контенту, плоский список с фильтром, инкремент usage при вставке, динамические плейсхолдеры `[вчера]`/`[завтра]`/`[сегодня]`. Шаблоны категории подмешиваются в few-shot к `automate` (`fetch_templates_for_category`).
**Извлечение сводной таблицы** (`_parse_summary_table`, 63317 Саратовские РС): «Автомобиль г/н | Дата выезда | Пробег по ГЛОНАСС | Пробег по 1С» → 4-кортеж (номер, дата, glonass=заявл. система, sheet=ПЛ/1С). Интервал дат «DD-DD.MM», пропуск пустых вложений.
**Альтернативный номер** (`_resolve_alt_plate`, object_not_found): если объект по номеру из темы не найден в geo, ИИ переключается на валидный гос.номер из тела/вложений.

---

## Деплой

```bash
# Локально: build frontend + commit + push
cd frontend && npm run build
cd .. && git add app/static/ && git commit -m "..." && git push origin main

# На сервере:
ssh -i /c/Users/sPec/.ssh/id_ed25519 root@155.212.186.165 \
  "cd /opt/support-ai-agent && git pull --rebase origin main && systemctl restart support-ai-agent"

# Проверка
curl http://localhost:8001/api/v1/employees
```

---

## Последние изменения (сессия 7 — 2026-06-15)

| Коммит | Что сделано |
|---|---|
| `a0e7d6a` | feat: Phase A — автоанализ заявок «Расхождение пробега» |
| `0e00dc2` | feat: детект глушения по телепортам/выбросам/спутникам |
| `4eb386c` | feat: карта трека (Leaflet) + графики (uPlot) |
| `3bdd3c3` | feat: зум графика, точка на карте, быстрое решение из комментария |
| `164f343` | feat: маркер на карте, сортировка/память шаблонов |
| `30f9458` | feat: задел обучения (TrainingSample), подсветка строки |
| `c47dc88`/`26f48d4` | feat: Фаза B — вложения + OCR (PyMuPDF + tesseract rus+eng) |
| `717c02b` | feat: Фаза C — массовое редактирование (чекбоксы + bulk) |
| `57eede4` | fix: пробег из трека пакетов; путь типов issues/types; шаблоны в bulk |
| `7483ab3`/`11b16d0` | fix: калибровка глушения, сценарий поздней выгрузки |
| `4c56a7f` | fix: дата «за <дата>», ПЛ/одометр с единицей, обрыв→терминал подключился |

### Сессия 8 (после дня эксплуатации)

| Коммит | Что сделано |
|---|---|
| `877bc8c` | fix: назначение (PATCH issues/{id}/assignees + group_id) — чинит bulk и «Взять себе»; дата из «дата неисправности»/«в системе»; поиск объекта по ядру номера без региона |
| `63ba0fb` | fix: явный путь к tesseract (systemd PATH без /usr/bin) — OCR сканов молча не работал |
| `7e49beb` | feat: фильтры № заявки и ответственный (комбинируются); статус объекта в панели карты (онлайн/посл.сообщение/IMEI/телефон с копированием) + кнопка свернуть из панели |
| `843f96f` | fix: build_track читает вложения (трек для заявок с датой/номером в скане) |

### Сессия 9 (наблюдения за 2-й день)

| Что сделано |
|---|
| fix: латиница↔кириллица в гос.номере (A759PC→А759РС) |
| fix: смена типа через список-кнопки (срабатывает с первого раза); № заявки только с клавиатуры; серые чекбоксы; липкая шапка деталей |
| feat: разбор «общих» заявок — пакетный анализ по ТС из вложений (таблица вердиктов) + массовый ответ про глушение |
| feat: создание вложенных заявок в Okdesk по объектам «данные верны» (parent_id + тип отдельным вызовом) |
| feat: для batch-заявок одиночный автоанализ скрыт; тостер при решении без типа |
| feat: кэш/история анализа (result_cache) — показ без повторного прогона ИИ, кнопка «Обновить» |
| feat: карта+графики по каждому объекту разбора (🗺), per-object track `?plate=&date=` |
| fix: глушение приоритетнее совпадения пробега в разборе (спуфинг ≠ «данные верны») |
| fix: распознавание спецтехники (5297СУ/СУ5297); дата из ISO; связанные заявки кликабельны; статус объекта в треке виден без данных за дату |

### Сессии 10–12 (форматы вложений, спецтехника, «ложный wait»)

| Что сделано |
|---|
| feat: обобщение разбора — все форматы вложений (PDF-акты `_parse_act_blocks`, тело-списки `_parse_body_vehicles`, сводные таблицы `_parse_summary_table`, табличный «Группировка» `_parse_grouping_table`) + пробег по системе ГЛОНАСС от клиента |
| feat: формат-профили ПО (`_FORMAT_PROFILES`, `resolve_format_profile`) — стратегия разбора и подсказка ИИ по company_name (дочерние Россети Волга) |
| feat: единый мастер ИИ (Разбор → Анализ → Ответ), авто-режим с вложениями и без; убраны дублирующие контролы визарда |
| feat: разбор тела/актов по объектам, дата из комментариев, авто-обновление кэша после комментария/решения |
| feat: детектор «ложного wait» (`_client_awaiting_remote_diag`) + спецтехника без км-вердикта (`_is_special_vehicle`); бейджи «спецтехника» и предупреждения «удалённая диагностика»/«спецтехника» в анализе |
| feat: колонка «ГЛОНАСС заявл.» (заявлено клиентом) рядом с «По факту» в таблице разбора |
| fix: статусы wait/no_time в смене статуса (64453/64306); вердикт по (номер, дата) (63617); жёсткий date-guard в пробеге; глобальный дедуп ТС по вложениям; ГЛОНАСС ≠ дата |
| feat: кнопка «В работе» (wait) снизу карточки, авто-скрытие успешных уведомлений, вердикт-по-дате (фронт) |

### Сессия 13 (обратная связь, монтажник, параметры, личные шаблоны)

| Коммит | Что сделано |
|---|---|
| `2c432ab` | fix: модель ИИ `deepseek-v4-flash` (а не `deepseek-chat`) — default в коде + доки |
| `5375385` | feat: личные шаблоны на пользователя (`app_templates.user_id`) — у каждого своя база + общие |
| `fa518fa` | feat: петля обратной связи по качеству ИИ (`ai_feedback`, экран «Оценки ИИ») + калибровка детектора глушения |
| `a252b6e`/`eca4308` | feat: копирование данных монтажнику (`installer_export`) — форматы КАЛЕНДАРЬ/МЕССЕНДЖЕР; телефон/контакт из параметров→get_contact (64239) |
| `45fe0e6` | fix: монтажник-спиннер (isFetching), системные комментарии — фиолетовый цвет, 64577 фолбэк-ответ |
| `17c78b5` | feat: редактирование параметров заявки (Местоположение/Контакт/Телефон) → `POST /parameters`, PATCH custom_parameters (64197) |
| `85361ed` | fix: стоячее глушение из-за подавления (low_sat>0.9+питание); запрет ложного «Не было питания» в промпте; переключение на валидный номер из тела при object_not_found; извлечение ГЛОНАСС/1С из таблицы «Дата выезда» (`_parse_summary_table`→4 поля); интервал «DD-DD.MM»; пропуск пустых вложений (64657/64673/64677/64655/64600/63317/64680) |

### Сессия 14 (OCR-устойчивость, правки разбора, фильтры, госномер, auth)

| Коммит | Дата | Что сделано |
|---|---|---|
| `cb075d9` | 06-29 | feat: раздел «ИИ-чат» с бейджем «скоро» + уточнение логики глушения + обновление md |
| `9fdab8a` | 06-29 | feat: блок 3 — бейдж «можно авто» + управление пользователями; отметка «исправлено» в Оценках ИИ |
| `0ea49a7` | 06-30 | fix: OCR-разбор вешал весь сервер (CPU 100%, цикл «Ошибка разбора») |
| `1307dff` | 06-30 | fix: накопительный возобновляемый OCR для больших многоактных PDF (64726) |
| `f916658` | 06-30 | feat: авто-дораспознавание больших PDF, правка гос.номера в разборе, фикс дочерних (64722) |
| `8635262` | 06-30 | feat: кнопка правки номера, видимые акты без номера (64725), серый цвет системных уведомлений |
| `681a342` | 06-30 | fix: системные уведомления Okdesk серым (детект по автору); план апгрейда сервера |
| `9a270d3` | 06-30 | feat: видимые строки без номера в табличной группировке (Ульяновские); уведомления без курсива |
| `f2af108` | 07-06 | fix: разбор оценок ИИ с ошибками — мульти-акт PDF, даты листов, номер из темы/комментариев, fuzzy-гео, вердикты |
| `088fa89` | 07-07 | fix: UnboundLocalError в `analyze_batch` — `text_plates` определялась через саму себя |
| `6f951ff` | 07-07 | feat: ручная правка гос.номера/даты в одиночном ИИ-анализе; сайдбар — Оценки ИИ выше ИИ-чата |
| `80ac0f8` | 07-07 | feat: пометка неверного формата гос.номера + заметная ручная правка номера/даты |
| `9bd3310` | 07-07 | fix: трек-панель следует за исправленным номером из ИИ-анализа |
| `3ec2c6c` | 07-07 | feat: личные сохранённые фильтры заявок (CRUD + сортировка) |
| `14b5839` | 07-07 | feat: подсветка активного сохранённого фильтра (кнопка + строка списка) |
| `bd7c26d` | 07-07 | ui: кнопка «Сбросить» после всех фильтров (за «Мои фильтры») |
| `f98fa82` | 07-21 | fix: распознавание госномера — префикс-склейка (`г.н.К862ВТ`), дизамбигуация без региона, транслит |
| `d28098d` | 07-21 | feat: ИИ описывает хронологию «подключился → выгрузил → глушение» (64915) |
| `c69fb4b` | 07-21 | feat(auth): fail-fast при пустом/дефолтном `AUTH_SECRET` в production |

**Сессия 14 (продолжение, 2026-07-23) — задеплоено на прод:**

| PR / коммит | Что сделано |
|---|---|
| PR #1 | feat: детект нескольких ТС по **телу письма**, не только по теме (65649: тема = дата, 20 ТС в теле). `analyze_batch` триггерит пакет по номерам темы ∪ тела; фронт считает номера в теме+описании (`multiInText`); подсчёт ТС согласован фронт/бэк (срез хвоста региона в `normPlate` + lookbehind в `PLATE_RE`). |
| PR #2 (`2d92227`) | fix: дата неисправности из тела с **2-значным годом** (65702: «за 21.07.26» → 2026-07-21; раньше терялась и подставлялась неверная). Только с маркером «за» (голую дату не берём — это дата отчёта). |
| PR #3 (`3674a14`) | feat: **извлечение спецномеров** спецтехники (2874СЕ/АА8941/64СН3847) из списков/таблиц вложений (65676: было 1 из 2 ТС → 2 из 2). Гейт `_plate_special_ok` (маркер гос.номера ИЛИ ячейка таблицы; слитные 4 цифры) отсекает времена/интервалы/номера документов. Сопутный фикс: ISO-дата в `_parse_body_vehicles` (было «2026-07-10»→«2010-07-26»). |

Прод на `main` = `5c60e03`. Регресс-проверка спецномеров по 615 OCR-вложениям: +спецномера в 40 заявках, все реальные, 0 ложняков.

**Проверка покрытия (2026-07-23):** Оренбургэнерго и Пензаэнерго (~1300 заявок, раньше не обкатаны) прогнаны вживую — 15/15 распознан номер + найден ТС в geo. Все форматы ПО Россети Волга покрыты (архетипы: один-номер-в-теме / мульти-акт / «общая» / сводная таблица / список в теле).

### Сессия 15 (2026-07-24) — редизайн карточки заявки (Figma-макет, без изменений кода)

Дизайн-сессия: в Figma (через связку `я → промт → DeepSeek → figma-mcp-go → верификация скриншотом`) собран полный макет редизайна карточки заявки (`IssueDetail`):

- **Хедер** — №/тема (2 строки), бейджи статуса+приоритета, кнопки «Решить ▾» / «Открыть в Okdesk ↗».
- **Детали** — 2 колонки (Клиент/объект + Классификация) + лента «Сроки и SLA» во всю ширину; сворачивание до peek-строки (вариант D — нижняя кнопка «Свернуть ▲» / «Показать всё ▼»).
- **Анализ** — 3 шага: ① Разбор (таблица ТС+дата, редактируемая) → ② Телеметрия (сетка метрик-чипов + флаги + вердикт: категория/уверенность/авто-ответ) → ③ Ответ клиенту (черновик + шаблон + «Ответить и решить»).
- **Передать монтажнику** — форматы Календарь/Мессенджер с кнопками «Копировать».
- **Оценка разбора** (👍/👎 + коммент) и **Комментарии** (лента + ввод).

Стиль: монохром (белые карточки, чёрная обводка, radius 8) + цветные бейджи только для статуса/приоритета/просрочки/флагов; тёмные primary-кнопки, бордерные secondary; редактируемые поля в бордер-боксах. Все блоки на auto-layout (hug), сборка «фрейм → auto-layout → дети» (снаружи внутрь).

⚠️ Пока это **только Figma-макет** — в код (`frontend/src/components/IssueDetail.tsx`) не перенесён.

**План v2 (запланировано, сессия 16)** — сверено с реальным кодом `IssueDetail.tsx`:
- Хедер: липкий контекст-бар **Компания + Объект/ТС + Ответственный** (всегда виден при скролле).
- Таблица «Разбор» (Шаг ①) → единая как для мультиобъекта, **даже для 1 ТС**: `Гос.номер✎ · Дата✎ · ПЛ · ГЛОНАСС заявл. · По факту · Вердикт▾ · 🗺` (в коде дата read-only — в макете делаем правимой).
- Детали: состав пока сохраняем; **добавить блок «Связанные заявки»** (родитель/дочерние); проработать блок «Вложения» (в коде есть `AttachmentsSection`).
- Комментарии: скролл-область (max-height) + фильтр Публичные/Внутренние + «Показать все (N)».
- Исполнение переносится на **OpenCode + DeepSeek** (DeepSeek сам водит figma-mcp-go по навыку-скиллу).

### Сессия 16 (2026-07-24) — v2 карточки в Figma: контекст-бар, таблица разбора, вложения/связанные, сворачивание

Продолжение дизайна (Figma-макет, код не тронут). Реализация: связка **OpenCode + DeepSeek** (навык `figma-mcp-go` в `instructions`) строит, Claude проверяет и добивает сложные места через свой MCP-мост.

- **Хедер:** добавлен контекст-бар «🏢 Компания · 🚚 ТС · 👤 Ответственный» (в коде — липкий при скролле).
- **Анализ ① Разбор:** таблица приведена к «тихому» единому виду как мультиобъектный разбор, даже для 1 ТС — колонки `Гос.номер✎ · Дата✎ · ПЛ · ГЛОНАСС заявл. · По факту · Вердикт▾(чип) · Трек`, лёгкая сетка, редактируемы номер/дата/вердикт (в коде дата пока read-only — в макете правимая).
- **Новые блоки:** «Вложения» (список файлов + Скачать) и «Связанные заявки» (родитель/дочерние).
- **Сворачивание всех блоков:** единый тонкий нижний бар «Свернуть ▲»/«Показать всё ▼» (вариант D) + клик по шапке (поведение в коде). Свёрнуты по умолчанию: Передать монтажнику, Оценка, Связанные, Вложения, Комментарии; развёрнуты — Детали (peek) и Анализ.
- **Комментарии:** табы Публичные/Внутренние + скролл-область + «Показать все (N)».
- Итоговый порядок: Хедер → Детали → Вложения → Связанные → Анализ → Передать монтажнику → Оценка → Комментарии.

Замечания по связке: DeepSeek надёжно строит простые/средние блоки, но глубоко вложенные таблицы стабильно ломает (hug вместо fixed-ширин, порядок ячеек, пустая строка) — их добивал Claude через мост; `reorder_nodes` вложенных auto-layout-детей ненадёжен (мелочь: «+ Добавить объект» остался над таблицей). Детали в [[reference_figma_mcp]].

⚠️ По-прежнему **только Figma-макет**, в код (`frontend/src/components/IssueDetail.tsx`) не перенесён.

### Сессия 17 (2026-07-26) — v3 в тёмной теме UI Kit, смена модели на MiMo V2.5

Дизайн-сессия, код не тронут.

**Ключевая находка:** макеты v2/v3 рисовались **светлым монохромом** (белые карточки, чёрная обводка), тогда как реальный фронт — **тёмный**: фон `#0A0A0A`, карточки `#252D25`, обводки `#404040`, акцент **лаймовый `#99D52A`**, Inter + Instrument Sans (`frontend/src/index.css`, UI Kit на 97 компонентов). Две сессии рисовали в визуальном языке, которого в продукте нет. Решение: **карточка v3 рисуется в тёмных токенах UI Kit** — макет 1:1 с тем, что уйдёт в код.

**Собрано в Figma (тёмная тема):**
- `73:194` «КАРТОЧКА v3 — раскладка» — схема: хедер / 2 колонки (872 работа + 464 рельс) / липкий бар.
- `74:2` «② Телеметрия и вердикт» — чипы разделены на **обоснования вердикта** (лаймовая обводка, белое значение) и фон (приглушённые); полоса доверия ИИ вместо голого «0.86».
- `74:43` «Вложения» — у каждого файла **виден статус OCR**: «распознан» / «4 из 6 стр.» / «OCR отключён» (у нас большие PDF дораспознаются частями — оператор должен видеть, что ИИ уже прочитал). Тип файла — цветной чип.
- `74:86` «Связанные заявки» — «Родительская» / «Дочерние», номер-ссылка + контурная пилюля статуса.

**Смена исполнителя: DeepSeek → MiMo V2.5 Free** (OpenCode Zen). Бейк-офф на одном блоке: MiMo держит фикс-ширины, WRAP-сетку, вложенность и порядок — ровно то, что DeepSeek стабильно ломал. Ресёрч по 6 бесплатным моделям подтвердил выбор; **Laguna S 2.1 — ловушка** (лучшие бенчмарки, но задокументированы проблемы с вложенным JSON, а весь figma-mcp-go на них). Слабость MiMo одна и известна: **дефолты Figma** — фрейм без явного `fill` становится белым, текст без цвета чёрным, «FIXED» местами уходит в hug. Самоотчёт модели о проверке скриншотом **недостоверен** — верифицирую только своим мостом. Детали и рецепты промтов — в памяти [[reference_figma_mcp]].

⚠️ Всё ещё **только Figma-макет**, в код (`frontend/src/components/IssueDetail.tsx`) не перенесён.

**Следующий шаг (сессия 18):** проверить блок «① Разбор» (таблица объектов — главный риск, на ней ломался DeepSeek), промт выдан. Затем «③ Ответ», «Комментарии», «Хедер + липкий бар» — и перенос в код.

**UX-идеи v3 (одобрены пользователем, отдельная сессия):** 2 колонки (работа слева: Анализ→Ответ→Комментарии; рельс справа: Детали/Вложения/Связанные/Оценка), **Анализ наверх** (вердикт = решение, не листать), **липкий бар действий** снизу + убрать дубль «Решить», подсветка метрик-обоснований вердикта + видимое доверие ИИ (1-клик отправка), тихий inline-edit в Деталях, peek последнего комментария. Референсы: Linear/Front/Intercom/Zendesk/GitHub. Детали — в памяти project_card_ux_v3. Собирается схема-фрейм «КАРТОЧКА v3 — раскладка».

---

### Сессия 18 (2026-07-26) — макет v3 добит, дизайн-токены сведены с кодом, автоматическая петля вёрстки

**1. Единый источник правды по токенам.** Figma и код разошлись полностью: в Figma лежал стартер-кит (28 переменных Material/Tailwind, 41 paint-стиль, 30 из них пастельные `Status/*`), ни одно значение не совпадало с палитрой проекта, а легаси-макеты были покрашены сырыми хексами. Стартер-кит удалён целиком, создана коллекция `support-ai-agent`: **30 переменных** (19 семантических цветов проекта + альфа-тинты + 5 статусов Okdesk + `text/on-accent`) и **15 текст-стилей** под фактическую шкалу приложения (9/10/11/12/13/14/15/16/18/20 — кеглей 9, 10, 11, 13, 15 в ките не было). `export_tokens format=css` теперь отдаёт готовый `:root{}` — дрейф ловится одной командой. Источник правды — `frontend/tailwind.config.js`.

**2. Правка контраста в коде (единственное изменение кода за сессию).** Посчитал WCAG на фактических фонах: `success #059345` давал 3.57:1 на `bg-card`, `info #146EF5` — 3.60:1 на `bg-frame`, при том что текст у нас 9–11px. Осветлил до `#22C55E` (6.29:1) и `#60A5FA` (6.40:1) в `tailwind.config.js` + `index.css`; 20 использований в 3 компонентах подхватили сами.

**3. Макет v3 собран целиком** (9 фреймов, тёмная тема, цвета через переменные, типографика через текст-стили):
`75:302` Хедер · `74:111` ① Разбор · `74:2` ② Телеметрия · `74:188` ③ Ответ · `75:355` Передать монтажнику · `74:245` Комментарии · `74:43` Вложения · `74:86` Связанные · `75:320` Липкий бар · `73:194` схема раскладки.
Ключевые решения: дубль «Решить» убран — главное действие одно и живёт в липком баре; смена статуса на «В работе»/«Ожидание ответа» спрятана под `Ещё ▾`; вид автора комментария кодирует только цветная полоса слева (бейджи монохромные, цветной один — «ВНУТРЕННИЙ»); блок «Передать монтажнику» **отсутствовал в макете**, хотя в коде есть — добавлен в левую колонку как альтернативный исход заявки.

**4. Автоматическая петля вёрстки.** Раньше: я пишу промт → пользователь вставляет боту → бот верстает. Теперь: `opencode run --agent figma-verstka "$(cat prompt.md)"` — я запускаю сам и проверяю результат. Заведён scoped-агент (`.opencode/agent/figma-verstka.md`): доступен только `figma-mcp-go`, `write`/`edit`/`bash`/`patch`/`task` отключены — проверено на пробой, файл создать и `git` выполнить не может. Скилл-файл `.opencode/skills/figma-mcp-go.md` переписан со светлой палитры на тёмную с ID переменных и стилей. Экономия токенов идёт **от делегирования сборки бесплатной модели, а не от автоматизации** — петля дала скорость и независимость от копипаста.

**5. Починен CLI opencode.** Падал с `NOT NULL constraint failed: session_message.seq`: десктоп 1.16.2 добавил колонку в ОБЩУЮ базу, а CLI 1.15.13 её не заполнял. Вылечено `opencode upgrade` до 1.18.5 (схема не менялась — десктоп базу по-прежнему читает). Попутно вычищено 114 МБ кэша апдейтера.

**6. `.gitignore`:** `auth_users.json` (хранилище паролей операторов) лежал **неигнорируемым** — закрыто. Плюс скриншоты макетов и дампы Figma.

⚠️ Код карточки (`IssueDetail.tsx`) **не тронут** — перенос макета в код это следующая сессия.

**Расхождения макет↔код, которые надо было отработать при переносе** (закрыты в сессии 19): пунктир у внутреннего комментария (`border-dashed`, в Figma сплошная — плагин пунктир не умеет); эмодзи-плейсхолдеры 🗺/⏳ → иконки lucide; кегли 13/15 → `text-[13px]`/`text-[15px]`; фон карточки комментария сотрудника с `bg-card` на `bg-frame` (сливался с подложкой блока); статус `wait` окрашен двумя разными синими — `#2b6684` в `StatusBadge.tsx` и `#2bb3c0` в кнопке `IssueDetail.tsx`, свести к одному.

---

### Сессия 19 (2026-07-27) — карточка v3 в коде, единые статусы, разбор ↔ телеметрия

**1. Статусы Okdesk сведены к одному источнику.** Один статус красился по-разному: `wait` был `#2b6684` в `StatusBadge`, `#2bb3c0` у кнопки «В работе», янтарной пилюлей в макете; `ChatPanel` вёл свой словарь подписей («В ожидании» вместо «В работе»). Создан **`frontend/src/lib/status.ts`** (`STATUS_COLOR` / `STATUS_TEXT` / `STATUS_LABEL` / `statusPillStyle`), те же значения продублированы в `tailwind.config.js` (`colors.status.*`) и в Figma-переменных `status/*`. Правило: статус = залитая пилюля цветом статуса, форма одна везде (`rounded-pill`). Пробовали чёрный текст на светлых статусах ради контраста AA — по решению пользователя вернули белый.

**2. Раскладка v3 перенесена в код** (`IssueDetail.tsx`). Слева работа: **Вопрос клиента → ① Разбор → ② Телеметрия и вердикт → ③ Ответ → Передать монтажнику → Комментарии**; справа рельс контекста: **Детали заявки → Вложения → Связанные → Оценка разбора**; снизу липкий бар с единственным главным действием. Композер уехал из «Комментариев» в блок «③ Ответ», три разноцветные кнопки статуса — в бар (`Ещё` + `Ответить и решить`), «Связанные заявки» вынесены из «Деталей» в свой блок рельса.

**3. Две колонки — по ширине ПАНЕЛИ, а не окна.** CSS container query в `index.css`: `.issue-body { container-type: inline-size }` + `@container (min-width: 780px)`. Порог считается по **content-box** (у панели ещё 40px паддингов) — из-за этого первые попытки с 900 и 820 не срабатывали. Карточке отдано **55%** ширины (список 45%), рельс 33% с границами 17–30rem. Кнопка **«Развернуть»** скрывает список и отдаёт карточке всю ширину (`detailExpanded` в `issuesStore`), взаимоисключима с панелью трека. Грабля: утилита Tailwind `flex-col` живёт в слое `utilities` и перебивала правило `@container` из слоя `components` — направление задаётся только в CSS.

**4. Разбор выглядит одинаково при любом числе объектов.** У пакетной заявки таблица `BatchAnalysis`, у одиночной — `SingleParseTable`, та же таблица из одной строки поверх кэша автоанализа. `ExtractedDataBlock` с сырыми «Текст заявки»/«Текст вложений» удалён. Правка гос.номера и даты — inline в ячейке (карандаш, Enter/Esc), блок «Уточнить номер/дату» убран. ⚠️ У одиночной такая правка вызывает `api.automateIssue` = полный переанализ с тратой токенов DeepSeek (в пакетной правка дешевле — только перепроверка в гео); при росте нагрузки нужен отдельный лёгкий эндпоинт.

**5. Телеметрия показывает объект, выбранный в таблице.** Бэкенд при пакетном разборе уже считал `gather_telemetry` по каждому ТС, но отдавал только `system_mileage_km`/`flags`/`teleport_jumps` — теперь кладёт `item["telemetry"] = asdict(t)` целиком (`issue_automation.py` ~2853), тип `BatchObject.telemetry` на фронте опционален (старые кэши поля не имеют, блок честно предлагает обновить разбор). Новый **`TelemetryPanel.tsx`**: пилюля вердикта, полоса доверия ИИ, сетка метрик с лаймовой обводкой у обоснований вердикта, флаги на русском, обоснование ИИ под метриками (2 строки + «Подробнее»).

**6. Убраны дубли и уплотнена подача.** Из автоанализа удалены «Что прочитал ИИ», вторая сетка метрик, «Прошлый анализ», кнопка «Ответить и решить» (решение только через бар), мастер `AnalysisWizard` со степпером. Текстовые кнопки → иконки с подсказками: хедер (освободилось ~340px под тему), оценка разбора (👍/👎), копирование у монтажника, повторный анализ (↻). Свёрнуто по умолчанию: группы «Сроки» и «Параметры заявки», вложения, лента комментариев (виден последний + «Показать все N»). Радиусы приведены к шкале макета: блок 8px, вложенное 6px, пилюли `rounded-pill` (было вперемешку 44/9/5/3 разных).

**7. Починено попутно:** комментарии из Okdesk приходят с HTML (`<strong id="docs-internal-guid-…">` от писем из Google Docs) и выводились сырыми тегами — теперь через `stripHtml`.

**8. Макет догнан до кода:** в блок «Детали заявки» добавлены группы УЧАСТНИКИ / СРОКИ / ПАРАМЕТРЫ ЗАЯВКИ и поля Автор, Источник, Плановая и Фактическая реакция, Потрачено; собран блок «Вопрос клиента» (`77:432`). Файл Figma называется `temp`, в нём после чистки 4 узла: сборка `76:26` (источник правды), схема `73:194`, «Передать монтажнику — раскрыт» `76:2`, v2-эталон контента `69:220`.

**Остаток по синхронизации:** в макете нет кнопки «Развернуть»; в таблице макета нет колонки создания дочерней заявки и сводки/прогресса OCR; в коде нет кнопки «Скачать все» из макета (нужен эндпоинт массовой выгрузки); пилюли статусов у связанных заявок требуют доработки API (сейчас приходят только id).

---

## Нерешённые проблемы / TODO

- **Разбор «общих» заявок** реализован (batch по форматам вложений + формат-профили ПО); остаётся постоянная задача — покрытие новых форматов писем/актов дочерних Россетей.
- **OCR ограничен** 6 страницами/документ, ~200 DPI; парсер ПЛ из табличного OCR можно докрутить.
- ✅ **OCR больше не вешает сервер**: OCR/парсинг в `asyncio.to_thread` + бюджет времени + накопительный возобновляемый OCR с кэшем `ocr:<att_id>` (0ea49a7/1307dff/f916658). Фронт авто-дораспознаёт большие PDF.
- ✅ **Спецномера в списках/таблицах извлекаются** (65676 закрыт, PR #3): `extract_all_plates(include_special=True)` + гейт `_plate_special_ok` (маркер/ячейка, слитные 4 цифры). Остаток: дефисные спецномера «81-40РВ» из списков не ловим (сознательный компромисс против ложняков-времён).
- **Опечатка года клиентом** (65770: «за 22.07.2025» при заявке 2026 г.) — парсер читает как есть. TODO: прокинуть `created_at` в `parse_issue`, прижимать год к году создания (чинит опечатку + ловит будущие даты).
- **Низкая автономность**: большинство разборов уходит в `needs_review` (эвристика conf 0.3–0.4); решительные вердикты — на явных кейсах. Поднять долю авто-вердиктов.
- **Качество per-object вердиктов ИИ не измерено** (сессия 23): `/batch/ai` отдаёт до 25 объектов в одном промпте, а `compose_aggregate_answer` в своё время путал, какой ТС в какой группе — из-за этого группировку вернули в код. Проверить на реальной заявке, не путает ли модель строки; при необходимости уменьшить `_AI_BATCH_MAX_OBJECTS`.
- **Обучение ИИ**: `training_samples` копится (закрытие + бэкафилл `/training/backfill`); few-shot retrieval подключён к `automate` (similar resolved + шаблоны категории); добавлена явная петля обратной связи (`ai_feedback`, экран «Оценки ИИ»). Метрика автосовпадения ответа ИИ с оператором ещё не считается.
- **TG-уведомления о сроках**: ✅ включены на проде (`TELEGRAM_ALERT_CHAT_ID` задан, бот настроен).
- **Vision-анализ графика** (картинка) — отложен; пока числовой профиль/события.
- **Телефония Mango** и **Telegram-канал** — будущие каналы заявок.
- **Wialon** не подключён к агенту. **Okdesk webhook** не настроен.
- **Диск сервера**: почищен 2026-07-23 до 69% (2.8G свободно) — снесены старые `.vscode-server` (главный пожиратель, ~2.3G), journal vacuum, apt clean. `.vscode-server` копится снова при переподключениях VS Code Remote — периодически чистить. Долгосрочно — апгрейд VPS (диск всего 8.7G).

---

## Дальнейшие шаги

1. ✅ Phase 1–2 (Backend cache, React дашборд)
2. ✅ Phase A: автоанализ (geo-телеметрия + классификация + черновик + решение)
3. ✅ Phase B: вложения + OCR (ИИ читает акты/путевые листы)
4. ✅ Phase C: массовое редактирование + карта/графики + задел обучения
5. ✅ Разбор «общих» заявок по форматам вложений + формат-профили ПО; единый мастер ИИ; сроки/SLA
6. ✅ Редизайн карточки заявки v3: макет в Figma → код, две колонки, единые статусы, разбор ↔ телеметрия по выбранному объекту
7. ✅ Карточка v4 «плоские строки» в коде + макет-эталон в синхроне; три уровня разбора по стоимости; ОДИН платный вызов ИИ на заявку с per-object вердиктами
8. 🟡 Обучение ИИ на накопленных решениях (бэкафилл есть, few-shot retrieval/метрика — следующий шаг)
9. ⬜ Mango/Telegram-каналы заявок; vision-анализ графиков; авто-решение заявок ИИ
