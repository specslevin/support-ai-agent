# Прочёсывание бесплатного разбора по всем ПО

> Рабочий журнал многосессионной проверки. Ведётся Claude Code, обновляется в КОНЦЕ каждой
> сессии проверки. План составлен 2026-07-31 по итогам интервью с пользователем и разведки базы.
> Предыдущая попытка спланировать это (раздел «ПЛАН СЕССИИ 25» в `CONTEXT.md`) опиралась на
> устаревшие числа — здесь они пересчитаны по живой базе.

## Зачем

Сессия 24 нашла **10 дефектов на 26 заявках одного оператора** — это 0.6% базы, почти целиком
Оренбургэнерго и Ульяновские. Такая плотность означает, что на остальных ПО дефектов не меньше.
Плюс в сессии 24 тронуто то, что могло задеть чужие форматы: обязательная единица у заявленного
пробега, регион через пробел в ОБЕИХ регулярках номера, составные ключи формат-профилей, кламп года
по `created_at`. Цель прохода — довести бесплатный разбор (правила + OCR, ноль вызовов DeepSeek) до
состояния, когда новые классы дефектов перестают находиться.

## Когда вопрос считается закрытым

**Две выборки ПОДРЯД без ни одного НОВОГО класса дефекта.** Старые классы, всплывшие на новых
данных, не считаются — они лишь подтверждают, что починка неполная (это отдельная правка, а не
новый класс). Две «чистые» выборки должны быть по РАЗНЫМ архетипам формата — два похожих ПО подряд
критерий не закрывают, иначе он сработает ложно.

Формально пройти всю очередь тоже нужно: критерий может остановить раньше, но непройденные ПО
остаются помеченными как непроверенные, а не как «в порядке».

## Решения пользователя (интервью 2026-07-31) — не перерешивать

| Вопрос | Решение |
|---|---|
| Порядок ПО | **Сначала риск регрессии** от сессии 24 (переименование профилей), потом по объёму |
| Глубина | **10 заявок на ПО, из них 5 с вложениями**; у мелких ПО — сколько есть |
| Режим правок | **Вся диагностика без патчей → починка пачкой по КЛАССАМ → регресс старый-против-нового → деплой** |
| Критерий закрытия | **Две выборки подряд без новых классов** |
| Не-Россети клиенты (Виалон 50, Галактика 24, ВсеИнструменты 32, ИП) | **Одна короткая сессия в конце**, по 3-5 заявок |
| Окно прогонов | **Любое время, строго по одной заявке**, между прогонами проверка `systemctl is-active` + HTTP 200 |

## Фактическая картина базы (разведка 2026-07-31)

Всего в `issue_cache` — **4867** заявок. В `result_cache`: `ocr:*` 747, `batch` 417, `automate` 94,
`parse` 41. **Разбор гонялся почти только по Волжскому ПО** (311 `batch` + 270 OCR); Чапаевское и
Жигулевское — по ~37; у 60+ остальных компаний **ноль записей разбора**.

| Филиал / клиент | Заявок | ПО внутри (заявок) | Состояние разбора |
|---|---|---|---|
| **Оренбургэнерго** | **988** | Восточное 300, Западное 253, Центральное 132, Северное 124, Оренбургское 105, Информэнергосвязь 45, Орское 29 | почти ноль (4 batch у Восточного, 1 у Центрального) |
| **Самарские РС** | **1797** | Волжское 948, Самарское 525, Чапаевское 165, Жигулевское 159 | Волжское прочёсано, Самарское — ноль |
| **Саратовские РС** | **866** | Северное 456, Прихоперское 281, Северо-Восточное 85, Правобережное 26, Приволжское 10, Центральное 7, Заволжское 1 | Северное — ноль, Прихоперское 8 batch |
| **Пензаэнерго** | **429** | Нижнеломовское 134, Сердобское 103, Кузнецкое 92, Пензенское 50, Каменское 50 | ноль |
| **Ульяновские РС** | **115** | Ульяновское 91, Барышское 15, Димитровградское 6, Южное 3 | 6 batch у Ульяновского |
| Мордовэнерго | 50 | — | 1 batch |
| Чувашэнерго | 5 | Алатырское 3, Северное 1, Южное 1 | ноль |
| ОДКРА | 28 | — | 1 batch |
| Прочие клиенты | 256 | Виалон 50, ВсеИнструменты 32, Галактика 24, гроздь ИП/ООО | ноль |
| *(без компании)* | 333 | автоуведомления `geo@gpspos.ru` | ноль, в проверку не берём |

**Что из этого следует для метода:**
- **Выбирать заявки с вложениями по теме НЕЛЬЗЯ.** «Актоподобных» по теме: Волжское 906/948, но
  Самарское 0/525, Саратов Северное 0/456, вся Пенза 0/429, Оренбург Восточное/Западное/Северное 0.
  Наличие вложений спрашиваем у Okdesk (`GET /issues/{id}/attachments`).
- **Один автор = один формат** у крупных ПО: Самарское 525/525 Сафронов, Саратов Северное 456/456
  Ефимов, Волжское 913/948 Исайчев, Восточное 293/300 Шашкин. Выборка из 10 заявок там
  репрезентативна. **Исключение — Прихоперское**: 5+ авторов и формат гуляет, там выборка ПО АВТОРАМ
  (по 2 заявки на каждого из топ-5).
- Известные архетипы тем: Волжское `Отправка: 31.07.2026_с455ах_Камаз 43118_Акт23_ВПО`, Саратов
  Северное `23-07-2026 ГАЗ-22171 Легковой Фургон В603ТВ нет данных`, Самарское — короткие
  бессистемные (`06.07.2026`, `А165РМ`, `добавить ТС`), Восточное — `Заявка 130` + госномер.

## Метод на одно ПО (повторять дословно)

0. **Токен** — минт без пароля тем же секретом (см. память `reference_rerun_analysis`), запуск через
   `/opt/support-ai-agent/.venv/bin/python`. Внутренний `id` ≠ `external_id`: эндпоинты берут
   `issue_cache.id`.
1. **Кандидаты**: `SELECT id, external_id, subject, contact_name, created_at FROM issue_cache
   WHERE company_name = ? ORDER BY created_at DESC LIMIT 40`.
2. **Кто с вложениями**: `GET /api/v1/issues/{id}/attachments` по кандидатам → отобрать **5 с
   непустым списком и 5 без**. Для Прихоперского — разложить выборку по авторам.
3. **Снять состояние «ДО»**: `SELECT kind, created_at, length(result_json) FROM result_cache WHERE
   issue_external_id IN (...)` — иначе потом не отличить «сломали» от «так и было».
4. **Прогон** (оба вызова БЕСПЛАТНЫ, DeepSeek не зовут): `POST /issues/{id}/parse` — все 10;
   `POST /issues/{id}/automate_batch` — только 5 с вложениями (тянет OCR).
   **Строго по одной заявке**, между прогонами `systemctl is-active support-ai-agent` + `curl` HTTP 200.
   Один воркер uvicorn: параллельные OCR-прогоны вешают весь сервис (память `project_event_loop_blocking`).
5. **Сверять с ИСХОДНИКОМ, а не с ожиданием**: `GET /issues/{id}/extracted`, `GET /issues/{id}/attachments`,
   текст OCR из `result_cache` (`kind LIKE 'ocr:%'`). Ожидание «в голове» — главный источник ложных дефектов.
6. **Классифицировать по осям** (см. ниже) и записать в журнал находок.
7. **Не патчить.** Правки — отдельной сессией починки, пачкой по классам.

### Оси классификации

| Ось | Что ищем |
|---|---|
| Номер | OCR-искажение, латиница вместо кириллицы, спецтехника (`64СН3847`), регион (слитно/через пробел/потерян), фантомные ТС из мусора |
| Дата | дата отчёта/акта вместо даты неисправности, год потерян («за 27.07»), опечатка года, ISO vs словом («18 июня 2026») |
| Пробег по путевому листу | ключи-синонимы («пут,л», «по 1С», «по спидометру»), путаница с датой/телефоном/номером акта |
| Заявленный клиентом | ключи («данные гл», «по Галактике», «в ПК»), обязательная единица «км» в отрыве от числа (шапка таблицы) |
| Состав строк | строки потеряны, задвоены, лишние; общая дата не раздалась; ОДКР/aggregate |
| Вердикт | вердикт против фактов; ложное «Данные верны»; спецтехника без км-пробега; ложный «Не было питания» |
| Гео | объект по номеру не найден, хотя в системе есть (полный номер vs без региона) |

### Формат записи находки

| # | ПО | Заявка | Ось | В исходнике | В разборе | Класс дефекта | Статус |
|---|---|---|---|---|---|---|---|

`Класс дефекта` — короткий ярлык, повторяющийся между ПО (например `ПЛ: ключ не распознан`), именно
по классам потом идёт починка. `Статус`: `найден` / `починен` / `формат, не дефект` / `решение пользователя`.

## Роли субагентов (экономия токенов)

| Роль | Как запускать | Что делает | Что возвращает |
|---|---|---|---|
| **Прогонщик** | **1 на сессию, НИКОГДА параллельно** | шаги 4: `/parse` и `/automate_batch` по одной, проверка здоровья между | список `id → ok/ошибка`, время, ничего больше |
| **Сверщик** | 2-4 **параллельно**, read-only | шаги 5-6 по 3-5 заявкам каждый: исходник против разбора | ТОЛЬКО таблицу расхождений по осям, без дампов JSON и OCR-текстов |
| **Классификатор** | это я, не делегируется | свести расхождения в классы, отделить дефект от «такой формат», решить, что выносить пользователю | сводку + журнал |

Жёсткие правила для субагентов: **не коммитить, не деплоить, не править файлы**; не вызывать
DeepSeek (`/batch/ai`); не возвращать полные JSON и OCR-тексты — только выдержки, доказывающие
расхождение; при неудаче честно писать, что не получилось, а не выдумывать числа.

## Очередь сессий

Проверочные сессии — `C1…C10`, починочные — `П1…П4`. После каждой волны: **отчёт пользователю →
интервью по исправлению → починка пачкой → регресс → деплой**. Починка НЕ делается внутри
проверочной сессии.

### Волна 1 — риск регрессии от сессии 24

| Сессия | Состав | Почему здесь | Заявок в выборке |
|---|---|---|---|
| **C1** | Саратовские РС Северное 456 + Оренбургэнерго Северное 124 + Оренбургэнерго Центральное 132 | Один класс риска: составные ключи профилей. «Оренбургэнерго Северное/Центральное» раньше ловились САРАТОВСКИМ профилем. Если переименование что-то сломало — вылезет здесь | 30 |
| **C2** | Ульяновские РС ×4 (115) + Чувашэнерго ×3 (5) | Ульяновским Барышское/Димитровградское/Южное выдан НЕЙТРАЛЬНЫЙ профиль с пометкой «формат не подтверждён примерами» — формат надо наконец увидеть. У Чувашэнерго профиля нет вовсе | ~27 |

→ **Отчёт 1 + интервью → П1**

### Волна 2 — актовый путь (OCR)

| Сессия | Состав | Почему здесь | Заявок |
|---|---|---|---|
| **C3** | Самарские РС Волжское 948 | Самое большое ПО и главная проверка обязательной единицы у заявленного пробега: ищем акты, где «км» стоит в шапке таблицы далеко от числа | 10 (актов много) |
| **C4** | Чапаевское 165 + Жигулевское 159 | Архетипы «один документ = много ТС» и «несколько актов = несколько ТС» — состав строк и раздача общей даты | 20 |

→ **Отчёт 2 → П2**

### Волна 3 — крупные нетронутые, текстовый путь

| Сессия | Состав | Почему здесь | Заявок |
|---|---|---|---|
| **C5** | Самарское 525 + Оренбургэнерго Восточное 300 | 825 заявок с НУЛЁМ разбора. Темы бессистемные («06.07.2026», «А165РМ», «Заявка 130») — самый вероятный источник «Нет номера/даты» | 20 |
| **C6** | Оренбург Западное 253 + Оренбургское 105 + Информэнергосвязь 45 + Орское 29 | Остаток крупнейшего филиала, ни одной записи разбора | 25 |
| **C7** | Пензаэнерго ×5 (429) | Один широкий профиль на 5 разных ПО — форматы почти наверняка различаются | 25 (по 5 на ПО) |

→ **Отчёт 3 → П3**

### Волна 4 — хвосты

| Сессия | Состав | Почему здесь | Заявок |
|---|---|---|---|
| **C8** | Прихоперское 281 (выборка ПО АВТОРАМ) + Северо-Восточное 85 | Прихоперское — единственное ПО с 5+ авторами и гуляющим форматом, свободная форма тем | 20 |
| **C9** | Мордовэнерго 50 + Правобережное 26 + Приволжское 10 + Центральное 7 + Заволжское 1 + ОДКРА 28 | Мелкие ПО без профилей. Правобережное интересно: 26/26 актоподобных | ~25 |
| **C10** | Прочие клиенты: Виалон 50, ВсеИнструменты 32, Галактика 24, топ-3 ИП | Короткая: проверить, что разбор не ломается на чужом формате | ~15 |

→ **Отчёт 4 → П4 → финальный контроль критерия закрытия**

**Итого ~10 проверочных + 4 починочных сессии.** Критерий закрытия может остановить раньше —
тогда непройденные ПО помечаются как непроверенные, а не как исправные.

## Что НЕ делаем

- Не зовём DeepSeek. Прочёсывание бесплатное; платные `/batch/ai` — только точечно и только по
  отдельному решению пользователя, когда надо сравнить вердикт правил с мнением модели.
- Не патчим внутри проверочной сессии (решение пользователя, см. таблицу решений).
- Не коммитим и не пушим без явного «создай commit» / «сделай push». Деплой — только с разрешения.
- Не подкручиваем пороги лестницы вердиктов ради статистики: сомнительную ветку разворачиваем в
  «Проверить» (память `feedback_verdict_over_autoclose`).
- Не берём в проверку 333 заявки без компании (автоуведомления `geo@gpspos.ru`).

## Статус по ПО

`—` не начато · `▶` в работе · `✓` пройдено, дефекты записаны · `✔` дефекты починены и проверены на проде

| Сессия | ПО | Статус | Дефектов найдено | Новых классов | Дата |
|---|---|---|---|---|---|
| C1 | Саратов Северное · Оренбург Северное · Оренбург Центральное | ✔ (П1 на проде, `0aa641f`) | 13 заявок / ~20 строк | 11 | 2026-07-31 |
| C2 | Ульяновские ×4 · Чувашэнерго ×3 | — | | | |
| C3 | Волжское | — | | | |
| C4 | Чапаевское · Жигулевское | — | | | |
| C5 | Самарское · Оренбург Восточное | — | | | |
| C6 | Оренбург Западное · Оренбургское · Информэнергосвязь · Орское | — | | | |
| C7 | Пензаэнерго ×5 | — | | | |
| C8 | Прихоперское · Северо-Восточное | — | | | |
| C9 | Мордовэнерго · Правобережное · Приволжское · Центральное · Заволжское · ОДКРА | — | | | |
| C10 | Прочие клиенты | — | | | |

## Журнал находок

### C1 (2026-07-31): Саратов Северное · Оренбург Северное · Оренбург Центральное

Прогон: 30 заявок `POST /parse` + 5 с вложениями `POST /automate_batch`, всё бесплатно, сервис жив
между каждым вызовом. Выборка: по 10 на ПО; вложения нашлись ТОЛЬКО у Оренбург Центрального (6 из
20 кандидатов), у Саратов Северного и Оренбург Северного вложений нет вовсе — там текстовый путь.

**Что важно знать про фактуру** (иначе выборка строится неправильно): `issue_cache.description`
пуст у всех трёх ПО, но в Okdesk описание ЕСТЬ и именно в нём вся суть заявки. Форматы описаний:
Саратов Северное — `ПЛ-35 км`; Оренбург Северное — `Глушение за 30.07.2026. Пробег по Галактике
101км, по 1С 115км`; Оренбург Центральное — `Расхождение за 28.07 Пробег по путл 97` или PDF-акт
«Заявка на ТО и ремонт ССМ № N» с таблицей `ССМ 76,6км, ПЛ 88км`.

| # | ПО | Заявка | Ось | В исходнике | В разборе | Класс дефекта | Статус |
|---|---|---|---|---|---|---|---|
| 1 | Ор. Центр. | 4538/О174ТХ56, 4343 (4 стр.), 4092 (5 стр.) | ПЛ | акт: `Расхождение пробегов ССМ 76,6км, ПЛ 88км` | `decl=76.6`, `sheet=None` — ПЛ потерян во ВСЕХ 12 строках 4 актов | `ПЛ: ключ «ПЛ Nкм» в строке акта не распознан` | починен |
| 2 | Ор. Центр. | 4343/Х990КО56, 4343/У671НК56, 4538/О174ТХ56 | Вердикт | `sys` совпал с ПЛ (88.12↔88, 80.27↔81, 42.57↔43) | «Проверить» вместо «Данные верны» | `Вердикт: потеря автоответа как следствие №1` | починен |
| 3 | Ор. Сев. | 4576/Т816КС56 | ПЛ | `не отобразился маршрут за 23.07.2026, пробег по путевому 10 км` | `sheet=None`, «Проверить» (а `sys=10.54`) | `ПЛ: ключ «по путевому N км» без слова «лист» не распознан` | починен |
| 4 | Ор. Центр. | 4773/Т216ХН | ПЛ | `Расхождение за 28.07 Пробег по путл 97` | `sheet=None` | `ПЛ: число без единицы отброшено` — **регрессия сессии 24** (обязательная «км») | починен |
| 5 | Сар. Сев. | 3907/РА7029, 3723/СК1165 | ПЛ | `ПЛ-1 м/ч`, `ПЛ-1 м-ч` | `sheet=0.001` (1 м/ч прочитан как 1 метр) | `ПЛ: моточасы приняты за метры → 0.001 км` | починен |
| 6 | Ор. Сев. | 3498/56НВ3071 | ПЛ | `Пробег по 1С 7км(моточасы)` | `sheet=7.0` км, сравнение с `sys=11.26` км | `ПЛ: моточасы, помеченные словом в скобках, приняты за км` | починен |
| 7 | Ор. Центр. | 4092/Т740ХУ56 | Заявленный | `Расхождение пробегов ССМ280,78км, ПЛ 322км` | `decl=None` (ключ слип с числом) | `Заявленный: ключ, слипшийся с числом, не распознан` | починен |
| 8 | Ор. Центр. | 4537/у700ее | Дата | описание `Отсутствуют данные` (даты нет), заявка от 23.07; `27.07.2026` есть только в НАШЕМ ответе «питание восстановлено» | `date=2026-07-27` → телеметрия и вердикт «Глушение» посчитаны по чужому дню | `Дата: подхвачена из ответа поддержки` — **опасно, вердикт по не тому дню** | починен |
| 9 | Ор. Центр. | 3704/ОМ0230 | Дата | `Отсутствуют данные с 02.07` | `date=None` (при этом `за 02.07` в 3675 разобрано верно) | `Дата: «с DD.MM» с предлогом «с» не распознана` | починен |
| 10 | Ор. Центр. | 4391/У360НО56, 4015/О613ТТ56 | Дата | даты нет ни в теме, ни в описании (`нет данных`) | разбор останавливается, объект не проверяется вовсе | `Дата: нет фолбэка на дату создания заявки` | решение пользователя: фолбэк не делаем |
| 11 | Ор. Центр. | 4391, 4015, 3704 | Вердикт | номер найден, нет только даты | ярлык «Нет номера/даты» | `Вердикт: ярлык врёт про номер` | починен |
| 12 | Ор. Центр. | 4674/Е253ВВ | Состав строк | описание `за 27.07 … пут,л 96км` + комментарий КЛИЕНТА `Расхождение за 29.07 Пробег по пут.л 69км` | разобран только 27.07, вторая жалоба потеряна | `Строки: жалоба из комментария клиента не становится строкой` | починен |
| 13 | Ор. Центр. | 4015/О613ТТ | Номер | тема `Нет данных О613ТТ56`, вложение — нечитаемый скан наряда, имя файла `65259_о613тт.jpeg` | batch: `plate=О613ТТ` без региона, тема проигнорирована | `Номер: batch берёт номер из мусорного OCR, регион из темы теряется` | починен |

### Решения интервью 1 (2026-07-31) → задание на П1, не перерешивать

| Вопрос | Решение |
|---|---|
| Моточасы (`ПЛ-1 м/ч`, `7км(моточасы)`) | **Отдельное поле**, в километровый пробег НЕ подставлять; показывать оператору, вердикт спецтехники остаётся «Проверить» |
| Заявка без даты вовсе (`нет данных`) | **Оставлять без разбора** — фолбэка на дату создания НЕ делаем (находка №10 закрыта как решение пользователя; ярлык вердикта из находки №11 всё равно поправить, номер он врёт) |
| Дата/пробег из комментариев | **Только комментарии клиента И только дата ≤ даты создания** заявки |
| Вторая жалоба в комментарии клиента | **Добавлять новой строкой разбора** со своей телеметрией и вердиктом |
| Единица «км» у путевого листа | Разрешить число без единицы, когда рядом явный ключ ПЛ (`по путл 97`) — решение принято по рекомендации |

Порядок П1 по весу: (1) `ПЛ Nкм` в актах ЦПО ССМ + слипшийся ключ `ССМ280,78км`; (2) дата только из
комментариев клиента и не позже создания; (3) ключи ПЛ — `по путевому N км`, число без единицы;
(4) моточасы в отдельное поле; (5) `с DD.MM`; (6) ярлык вердикта при найденном номере; (7) вторая
жалоба новой строкой; (8) в batch не терять регион, когда тема даёт полный номер.

### П1 (2026-08-02): починка пачкой по классам + регресс

Правки: `app/services/issue_automation.py`, `app/api/v1/endpoints/issues_dashboard.py`, фронт
(`types/index.ts`, `IssueDetail.tsx`, `TelemetryPanel.tsx`). Что сделано по классам:

| Класс | Что сделано |
|---|---|
| №1, №3, №4 (ключи ПЛ) | Ключи путевого листа переразделены на «единица опциональна» (`ПЛ`, `путев\w*` без «лист», `пут.л`, `путл`) и «единица обязательна» (`1С`, одометр, спидометр). Голое `ПЛ` — с границами, чтобы не ловить «площадь»/«план»/«пломба» |
| №5, №6 (моточасы) | Новое поле `engine_hours`: единица «м» больше не матчится перед `/ч`, `-ч`, `ч`; слово «моточасы» рядом с числом уводит его из километров. У одиночной заявки моточасы переносятся из шапки в строку разбора |
| №7 (слипшийся ключ) | Когда ключ (`ССМ`/`Галактика`/…) стоит вплотную к числу, запрет «буква перед числом» не применяется; для свободного поиска числа защита от гос.номера сохранена |
| №8 (дата из чужого сообщения) | `_build_comments_context` отдаёт разбору ТОЛЬКО комментарии клиента с их датами; дата неисправности не может быть позже сообщения, в котором названа. Модель по-прежнему видит всю переписку |
| №9 (предлог «с») | `с`/`со` добавлены в маркеры даты — и с годом, и `DD.MM` по `created_at`; «за период с 01.03 по 31.03» и «связано с 12 июня» не ловятся |
| №11 (ярлык) | `есть номер, нет даты` → «Нет даты»; «Номер не распознан» и «Нет номера/даты» — как раньше. Метка добавлена во фронт и в список служебных, не попадающих в ответ клиенту |
| №12 (вторая жалоба) | Пары «другая дата + пробег» из комментариев клиента становятся отдельной строкой с пометкой источника `(из комментария клиента)`, дедуп по (номер, дата) |
| №13 (регион) | Полные номера из ТЕМЫ достраивают регион всем строкам разбора по ключу без региона — единой точкой, не только для «номера из имени файла» |

**Регресс старый-против-нового на тех же 30 + 5 заявках C1** (новый код гонялся во временной копии
на сервере, боевой каталог не трогали): изменились 14 заявок, все изменения — целевые.

- вернулся путевой лист: 4576 `10 км` → «Данные верны» вместо «Проверить»; 4773 `по путл 97`;
  в актах ЦПО ССМ 12 строк получили ПЛ, из них 4 строки сменили «Проверить» → «Данные верны»
  (4538/О174ТХ56, 4343/Х990КО56, 4343/У671НК56, 4092/О406ТХ56, 4092/Т742ХУ56);
- 4092/Т740ХУ56: `ССМ280,78км` теперь читается (было `None`);
- моточасы: 3907 и 3723 `0.001 км` → `engine_hours=1`, 3498 `7 км` → `engine_hours=7`;
- 4537: дата больше НЕ берётся из нашего ответа поддержки → «Нет даты» вместо «Глушение» по чужому дню;
- 4674: появилась вторая строка `29.07 / 69 км` из комментария клиента;
- 3704: `с 02.07` разобрана → вердикт «Нет данных» вместо пустого разбора;
- 4015: `О613ТТ` → `О613ТТ56` (регион из темы), ярлык «Нет даты»;
- **остальные 21 заявка — байт в байт как были**, включая весь Саратов Северное.

Плюс диф по локальному корпусу (6037 текстов: темы 4642, training 761, OCR 634): дат изменилось
0, номеров 0; ПЛ — 38 улучшений на теле заявок и 3 на актах (2 из них — целевой увод моточасов).

**Известное ограничение, оставлено сознательно:** моточасы из АКТОВ (вложений) в `engine_hours` не
попадают — построчные парсеры носят кортежи из 4 полей, расширение до 5 задело бы ~10 мест.
В боевой базе таких актов 2; ошибочные `0.001 км` там больше не появляются (стало `None`).

**Проверено и НЕ сломано** (главный вопрос волны 1 — регрессия сессии 24):
- Составные ключи формат-профилей: Оренбург Северное/Центральное разбираются по своему формату,
  саратовский профиль (`ПЛ-N км`) им не мешает. Переименование ничего не сломало.
- Регион через пробел в теме: `А 506 НА`, `А 958 ОВ`, `Х 780 ХС` → номер собран верно.
- Номер без региона: `О885ХА` → в geo найден `О885ХА56`; `Т179НС` → `Т179НС56`.
- Кламп года по `created_at`: `за 02.07` → 2026-07-02, `за 17.06` → 2026-06-17.
- Тема-мультиномер: `О884ХА56, О892ХА56, Е740ВВ156, У202ЕЕ56` → 4 строки, `pack=0` у одного даёт
  честное «Нет данных» (объект в geo существует).
- Акт «Нет данных в программе Галактика» на 11 ТС (4645): все 11 номеров и дата 27.07 сняты верно,
  у всех 11 объектов geo реально 0 пакетов — «Нет данных» правомерно.
- Обязательная единица у ЗАЯВЛЕННОГО пробега на этих ПО ложных срабатываний не дала; сломала она
  только путевой лист без единицы (находка №4).
