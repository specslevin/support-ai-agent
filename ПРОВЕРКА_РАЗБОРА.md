# Прочёсывание бесплатного разбора по всем ПО

> Рабочий журнал многосессионной проверки. Ведётся Claude Code, обновляется в КОНЦЕ каждой
> сессии проверки. План составлен 2026-07-31 по итогам интервью с пользователем и разведки базы.
> Предыдущая попытка спланировать это (раздел «ПЛАН СЕССИИ 25» в `CONTEXT.md`) опиралась на
> устаревшие числа — здесь они пересчитаны по живой базе.

## Зачем

Сессия 24 нашла **10 дефектов на 26 заявках одного оператора** — это 0.6% базы, почти целиком
Оренбургэнерго и Ульяновские. Такая плотность означает, что на остальных ПО дефектов не меньше.
Плюс в сессии 24 тронуто то, что могло задеть чужие форматы: обязательная единица у заявленного
пробега, регион через пробел в ОБЕИХ регулярках номера, составные ключи формат-профилей, кламп года
по `created_at`. Цель прохода — довести бесплатный разбор (правила + OCR, ноль вызовов DeepSeek) до
состояния, когда новые классы дефектов перестают находиться.

## Когда вопрос считается закрытым

**Две выборки ПОДРЯД без ни одного НОВОГО класса дефекта.** Старые классы, всплывшие на новых
данных, не считаются — они лишь подтверждают, что починка неполная (это отдельная правка, а не
новый класс). Две «чистые» выборки должны быть по РАЗНЫМ архетипам формата — два похожих ПО подряд
критерий не закрывают, иначе он сработает ложно.

Формально пройти всю очередь тоже нужно: критерий может остановить раньше, но непройденные ПО
остаются помеченными как непроверенные, а не как «в порядке».

## Решения пользователя (интервью 2026-07-31) — не перерешивать

| Вопрос | Решение |
|---|---|
| Порядок ПО | **Сначала риск регрессии** от сессии 24 (переименование профилей), потом по объёму |
| Глубина | **10 заявок на ПО, из них 5 с вложениями**; у мелких ПО — сколько есть |
| Режим правок | **Вся диагностика без патчей → починка пачкой по КЛАССАМ → регресс старый-против-нового → деплой** |
| Критерий закрытия | **Две выборки подряд без новых классов** |
| Не-Россети клиенты (Виалон 50, Галактика 24, ВсеИнструменты 32, ИП) | **Одна короткая сессия в конце**, по 3-5 заявок |
| Окно прогонов | **Любое время, строго по одной заявке**, между прогонами проверка `systemctl is-active` + HTTP 200 |

## Фактическая картина базы (разведка 2026-07-31)

Всего в `issue_cache` — **4867** заявок. В `result_cache`: `ocr:*` 747, `batch` 417, `automate` 94,
`parse` 41. **Разбор гонялся почти только по Волжскому ПО** (311 `batch` + 270 OCR); Чапаевское и
Жигулевское — по ~37; у 60+ остальных компаний **ноль записей разбора**.

| Филиал / клиент | Заявок | ПО внутри (заявок) | Состояние разбора |
|---|---|---|---|
| **Оренбургэнерго** | **988** | Восточное 300, Западное 253, Центральное 132, Северное 124, Оренбургское 105, Информэнергосвязь 45, Орское 29 | почти ноль (4 batch у Восточного, 1 у Центрального) |
| **Самарские РС** | **1797** | Волжское 948, Самарское 525, Чапаевское 165, Жигулевское 159 | Волжское прочёсано, Самарское — ноль |
| **Саратовские РС** | **866** | Северное 456, Прихоперское 281, Северо-Восточное 85, Правобережное 26, Приволжское 10, Центральное 7, Заволжское 1 | Северное — ноль, Прихоперское 8 batch |
| **Пензаэнерго** | **429** | Нижнеломовское 134, Сердобское 103, Кузнецкое 92, Пензенское 50, Каменское 50 | ноль |
| **Ульяновские РС** | **115** | Ульяновское 91, Барышское 15, Димитровградское 6, Южное 3 | 6 batch у Ульяновского |
| Мордовэнерго | 50 | — | 1 batch |
| Чувашэнерго | 5 | Алатырское 3, Северное 1, Южное 1 | ноль |
| ОДКРА | 28 | — | 1 batch |
| Прочие клиенты | 256 | Виалон 50, ВсеИнструменты 32, Галактика 24, гроздь ИП/ООО | ноль |
| *(без компании)* | 333 | автоуведомления `geo@gpspos.ru` | ноль, в проверку не берём |

**Что из этого следует для метода:**
- **Выбирать заявки с вложениями по теме НЕЛЬЗЯ.** «Актоподобных» по теме: Волжское 906/948, но
  Самарское 0/525, Саратов Северное 0/456, вся Пенза 0/429, Оренбург Восточное/Западное/Северное 0.
  Наличие вложений спрашиваем у Okdesk (`GET /issues/{id}/attachments`).
- **Один автор = один формат** у крупных ПО: Самарское 525/525 Сафронов, Саратов Северное 456/456
  Ефимов, Волжское 913/948 Исайчев, Восточное 293/300 Шашкин. Выборка из 10 заявок там
  репрезентативна. **Исключение — Прихоперское**: 5+ авторов и формат гуляет, там выборка ПО АВТОРАМ
  (по 2 заявки на каждого из топ-5).
- Известные архетипы тем: Волжское `Отправка: 31.07.2026_с455ах_Камаз 43118_Акт23_ВПО`, Саратов
  Северное `23-07-2026 ГАЗ-22171 Легковой Фургон В603ТВ нет данных`, Самарское — короткие
  бессистемные (`06.07.2026`, `А165РМ`, `добавить ТС`), Восточное — `Заявка 130` + госномер.

## Метод на одно ПО (повторять дословно)

0. **Токен** — минт без пароля тем же секретом (см. память `reference_rerun_analysis`), запуск через
   `/opt/support-ai-agent/.venv/bin/python`. Внутренний `id` ≠ `external_id`: эндпоинты берут
   `issue_cache.id`.
1. **Кандидаты**: `SELECT id, external_id, subject, contact_name, created_at FROM issue_cache
   WHERE company_name = ? ORDER BY created_at DESC LIMIT 40`.
2. **Кто с вложениями**: `GET /api/v1/issues/{id}/attachments` по кандидатам → отобрать **5 с
   непустым списком и 5 без**. Для Прихоперского — разложить выборку по авторам.
3. **Снять состояние «ДО»**: `SELECT kind, created_at, length(result_json) FROM result_cache WHERE
   issue_external_id IN (...)` — иначе потом не отличить «сломали» от «так и было».
4. **Прогон** (оба вызова БЕСПЛАТНЫ, DeepSeek не зовут): `POST /issues/{id}/parse` — все 10;
   `POST /issues/{id}/automate_batch` — только 5 с вложениями (тянет OCR).
   **Строго по одной заявке**, между прогонами `systemctl is-active support-ai-agent` + `curl` HTTP 200.
   Один воркер uvicorn: параллельные OCR-прогоны вешают весь сервис (память `project_event_loop_blocking`).
5. **Сверять с ИСХОДНИКОМ, а не с ожиданием**: `GET /issues/{id}/extracted`, `GET /issues/{id}/attachments`,
   текст OCR из `result_cache` (`kind LIKE 'ocr:%'`). Ожидание «в голове» — главный источник ложных дефектов.
6. **Классифицировать по осям** (см. ниже) и записать в журнал находок.
7. **Не патчить.** Правки — отдельной сессией починки, пачкой по классам.

### Оси классификации

| Ось | Что ищем |
|---|---|
| Номер | OCR-искажение, латиница вместо кириллицы, спецтехника (`64СН3847`), регион (слитно/через пробел/потерян), фантомные ТС из мусора |
| Дата | дата отчёта/акта вместо даты неисправности, год потерян («за 27.07»), опечатка года, ISO vs словом («18 июня 2026») |
| Пробег по путевому листу | ключи-синонимы («пут,л», «по 1С», «по спидометру»), путаница с датой/телефоном/номером акта |
| Заявленный клиентом | ключи («данные гл», «по Галактике», «в ПК»), обязательная единица «км» в отрыве от числа (шапка таблицы) |
| Состав строк | строки потеряны, задвоены, лишние; общая дата не раздалась; ОДКР/aggregate |
| Вердикт | вердикт против фактов; ложное «Данные верны»; спецтехника без км-пробега; ложный «Не было питания» |
| Гео | объект по номеру не найден, хотя в системе есть (полный номер vs без региона) |

### Формат записи находки

| # | ПО | Заявка | Ось | В исходнике | В разборе | Класс дефекта | Статус |
|---|---|---|---|---|---|---|---|

`Класс дефекта` — короткий ярлык, повторяющийся между ПО (например `ПЛ: ключ не распознан`), именно
по классам потом идёт починка. `Статус`: `найден` / `починен` / `формат, не дефект` / `решение пользователя`.

## Роли субагентов (экономия токенов)

| Роль | Как запускать | Что делает | Что возвращает |
|---|---|---|---|
| **Прогонщик** | **1 на сессию, НИКОГДА параллельно** | шаги 4: `/parse` и `/automate_batch` по одной, проверка здоровья между | список `id → ok/ошибка`, время, ничего больше |
| **Сверщик** | 2-4 **параллельно**, read-only | шаги 5-6 по 3-5 заявкам каждый: исходник против разбора | ТОЛЬКО таблицу расхождений по осям, без дампов JSON и OCR-текстов |
| **Классификатор** | это я, не делегируется | свести расхождения в классы, отделить дефект от «такой формат», решить, что выносить пользователю | сводку + журнал |

Жёсткие правила для субагентов: **не коммитить, не деплоить, не править файлы**; не вызывать
DeepSeek (`/batch/ai`); не возвращать полные JSON и OCR-тексты — только выдержки, доказывающие
расхождение; при неудаче честно писать, что не получилось, а не выдумывать числа.

## Очередь сессий

Проверочные сессии — `C1…C10`, починочные — `П1…П4`. После каждой волны: **отчёт пользователю →
интервью по исправлению → починка пачкой → регресс → деплой**. Починка НЕ делается внутри
проверочной сессии.

### Волна 1 — риск регрессии от сессии 24

| Сессия | Состав | Почему здесь | Заявок в выборке |
|---|---|---|---|
| **C1** | Саратовские РС Северное 456 + Оренбургэнерго Северное 124 + Оренбургэнерго Центральное 132 | Один класс риска: составные ключи профилей. «Оренбургэнерго Северное/Центральное» раньше ловились САРАТОВСКИМ профилем. Если переименование что-то сломало — вылезет здесь | 30 |
| **C2** | Ульяновские РС ×4 (115) + Чувашэнерго ×3 (5) | Ульяновским Барышское/Димитровградское/Южное выдан НЕЙТРАЛЬНЫЙ профиль с пометкой «формат не подтверждён примерами» — формат надо наконец увидеть. У Чувашэнерго профиля нет вовсе | ~27 |

→ **Отчёт 1 + интервью → П1**

### Волна 2 — актовый путь (OCR)

| Сессия | Состав | Почему здесь | Заявок |
|---|---|---|---|
| **C3** | Самарские РС Волжское 948 | Самое большое ПО и главная проверка обязательной единицы у заявленного пробега: ищем акты, где «км» стоит в шапке таблицы далеко от числа | 10 (актов много) |
| **C4** | Чапаевское 165 + Жигулевское 159 | Архетипы «один документ = много ТС» и «несколько актов = несколько ТС» — состав строк и раздача общей даты | 20 |

→ **Отчёт 2 → П2**

### Волна 3 — крупные нетронутые, текстовый путь

| Сессия | Состав | Почему здесь | Заявок |
|---|---|---|---|
| **C5** | Самарское 525 + Оренбургэнерго Восточное 300 | 825 заявок с НУЛЁМ разбора. Темы бессистемные («06.07.2026», «А165РМ», «Заявка 130») — самый вероятный источник «Нет номера/даты» | 20 |
| **C6** | Оренбург Западное 253 + Оренбургское 105 + Информэнергосвязь 45 + Орское 29 | Остаток крупнейшего филиала, ни одной записи разбора | 25 |
| **C7** | Пензаэнерго ×5 (429) | Один широкий профиль на 5 разных ПО — форматы почти наверняка различаются | 25 (по 5 на ПО) |

→ **Отчёт 3 → П3**

### Волна 4 — хвосты

| Сессия | Состав | Почему здесь | Заявок |
|---|---|---|---|
| **C8** | Прихоперское 281 (выборка ПО АВТОРАМ) + Северо-Восточное 85 | Прихоперское — единственное ПО с 5+ авторами и гуляющим форматом, свободная форма тем | 20 |
| **C9** | Мордовэнерго 50 + Правобережное 26 + Приволжское 10 + Центральное 7 + Заволжское 1 + ОДКРА 28 | Мелкие ПО без профилей. Правобережное интересно: 26/26 актоподобных | ~25 |
| **C10** | Прочие клиенты: Виалон 50, ВсеИнструменты 32, Галактика 24, топ-3 ИП | Короткая: проверить, что разбор не ломается на чужом формате | ~15 |

→ **Отчёт 4 → П4 → финальный контроль критерия закрытия**

**Итого ~10 проверочных + 4 починочных сессии.** Критерий закрытия может остановить раньше —
тогда непройденные ПО помечаются как непроверенные, а не как исправные.

## Что НЕ делаем

- Не зовём DeepSeek. Прочёсывание бесплатное; платные `/batch/ai` — только точечно и только по
  отдельному решению пользователя, когда надо сравнить вердикт правил с мнением модели.
- Не патчим внутри проверочной сессии (решение пользователя, см. таблицу решений).
- Не коммитим и не пушим без явного «создай commit» / «сделай push». Деплой — только с разрешения.
- Не подкручиваем пороги лестницы вердиктов ради статистики: сомнительную ветку разворачиваем в
  «Проверить» (память `feedback_verdict_over_autoclose`).
- Не берём в проверку 333 заявки без компании (автоуведомления `geo@gpspos.ru`).

## Статус по ПО

`—` не начато · `▶` в работе · `✓` пройдено, дефекты записаны · `✔` дефекты починены и проверены на проде

| Сессия | ПО | Статус | Дефектов найдено | Новых классов | Дата |
|---|---|---|---|---|---|
| C1 | Саратов Северное · Оренбург Северное · Оренбург Центральное | ✔ (П1 на проде, `0aa641f`) | 13 заявок / ~20 строк | 11 | 2026-07-31 |
| C2 | Ульяновские ×4 · Чувашэнерго ×3 | ✔ (П2 на проде, `1970289`) | 28 заявок из 34 | 25 | 2026-08-02 |
| C3 | Волжское | ✔ (П3 на проде, `a67633d`) | 12 заявок / 12 из 12 разобраны | 11 | 2026-08-03 |
| C4 | Чапаевское · Жигулевское | ✔ (П4 на проде: часть 1 `69b1696`, часть 2 `869b02a`, доработка `92d0ba4`) | 20 заявок / 240 строк | 23 | 2026-08-03 |
| C5 | Самарское · Оренбург Восточное | ✔ (П5 на проде, `55650c8`) | 20 заявок / 21 находка | 19 | 2026-08-05 |
| C6 | Оренбург Западное · Оренбургское · Информэнергосвязь · Орское | — | | | |
| C7 | Пензаэнерго ×5 | — | | | |
| C8 | Прихоперское · Северо-Восточное | — | | | |
| C9 | Мордовэнерго · Правобережное · Приволжское · Центральное · Заволжское · ОДКРА | — | | | |
| C10 | Прочие клиенты | — | | | |

## Журнал находок

### C1 (2026-07-31): Саратов Северное · Оренбург Северное · Оренбург Центральное

Прогон: 30 заявок `POST /parse` + 5 с вложениями `POST /automate_batch`, всё бесплатно, сервис жив
между каждым вызовом. Выборка: по 10 на ПО; вложения нашлись ТОЛЬКО у Оренбург Центрального (6 из
20 кандидатов), у Саратов Северного и Оренбург Северного вложений нет вовсе — там текстовый путь.

**Что важно знать про фактуру** (иначе выборка строится неправильно): `issue_cache.description`
пуст у всех трёх ПО, но в Okdesk описание ЕСТЬ и именно в нём вся суть заявки. Форматы описаний:
Саратов Северное — `ПЛ-35 км`; Оренбург Северное — `Глушение за 30.07.2026. Пробег по Галактике
101км, по 1С 115км`; Оренбург Центральное — `Расхождение за 28.07 Пробег по путл 97` или PDF-акт
«Заявка на ТО и ремонт ССМ № N» с таблицей `ССМ 76,6км, ПЛ 88км`.

| # | ПО | Заявка | Ось | В исходнике | В разборе | Класс дефекта | Статус |
|---|---|---|---|---|---|---|---|
| 1 | Ор. Центр. | 4538/О174ТХ56, 4343 (4 стр.), 4092 (5 стр.) | ПЛ | акт: `Расхождение пробегов ССМ 76,6км, ПЛ 88км` | `decl=76.6`, `sheet=None` — ПЛ потерян во ВСЕХ 12 строках 4 актов | `ПЛ: ключ «ПЛ Nкм» в строке акта не распознан` | починен |
| 2 | Ор. Центр. | 4343/Х990КО56, 4343/У671НК56, 4538/О174ТХ56 | Вердикт | `sys` совпал с ПЛ (88.12↔88, 80.27↔81, 42.57↔43) | «Проверить» вместо «Данные верны» | `Вердикт: потеря автоответа как следствие №1` | починен |
| 3 | Ор. Сев. | 4576/Т816КС56 | ПЛ | `не отобразился маршрут за 23.07.2026, пробег по путевому 10 км` | `sheet=None`, «Проверить» (а `sys=10.54`) | `ПЛ: ключ «по путевому N км» без слова «лист» не распознан` | починен |
| 4 | Ор. Центр. | 4773/Т216ХН | ПЛ | `Расхождение за 28.07 Пробег по путл 97` | `sheet=None` | `ПЛ: число без единицы отброшено` — **регрессия сессии 24** (обязательная «км») | починен |
| 5 | Сар. Сев. | 3907/РА7029, 3723/СК1165 | ПЛ | `ПЛ-1 м/ч`, `ПЛ-1 м-ч` | `sheet=0.001` (1 м/ч прочитан как 1 метр) | `ПЛ: моточасы приняты за метры → 0.001 км` | починен |
| 6 | Ор. Сев. | 3498/56НВ3071 | ПЛ | `Пробег по 1С 7км(моточасы)` | `sheet=7.0` км, сравнение с `sys=11.26` км | `ПЛ: моточасы, помеченные словом в скобках, приняты за км` | починен |
| 7 | Ор. Центр. | 4092/Т740ХУ56 | Заявленный | `Расхождение пробегов ССМ280,78км, ПЛ 322км` | `decl=None` (ключ слип с числом) | `Заявленный: ключ, слипшийся с числом, не распознан` | починен |
| 8 | Ор. Центр. | 4537/у700ее | Дата | описание `Отсутствуют данные` (даты нет), заявка от 23.07; `27.07.2026` есть только в НАШЕМ ответе «питание восстановлено» | `date=2026-07-27` → телеметрия и вердикт «Глушение» посчитаны по чужому дню | `Дата: подхвачена из ответа поддержки` — **опасно, вердикт по не тому дню** | починен |
| 9 | Ор. Центр. | 3704/ОМ0230 | Дата | `Отсутствуют данные с 02.07` | `date=None` (при этом `за 02.07` в 3675 разобрано верно) | `Дата: «с DD.MM» с предлогом «с» не распознана` | починен |
| 10 | Ор. Центр. | 4391/У360НО56, 4015/О613ТТ56 | Дата | даты нет ни в теме, ни в описании (`нет данных`) | разбор останавливается, объект не проверяется вовсе | `Дата: нет фолбэка на дату создания заявки` | решение пользователя: фолбэк не делаем |
| 11 | Ор. Центр. | 4391, 4015, 3704 | Вердикт | номер найден, нет только даты | ярлык «Нет номера/даты» | `Вердикт: ярлык врёт про номер` | починен |
| 12 | Ор. Центр. | 4674/Е253ВВ | Состав строк | описание `за 27.07 … пут,л 96км` + комментарий КЛИЕНТА `Расхождение за 29.07 Пробег по пут.л 69км` | разобран только 27.07, вторая жалоба потеряна | `Строки: жалоба из комментария клиента не становится строкой` | починен |
| 13 | Ор. Центр. | 4015/О613ТТ | Номер | тема `Нет данных О613ТТ56`, вложение — нечитаемый скан наряда, имя файла `65259_о613тт.jpeg` | batch: `plate=О613ТТ` без региона, тема проигнорирована | `Номер: batch берёт номер из мусорного OCR, регион из темы теряется` | починен |

### Решения интервью 1 (2026-07-31) → задание на П1, не перерешивать

| Вопрос | Решение |
|---|---|
| Моточасы (`ПЛ-1 м/ч`, `7км(моточасы)`) | **Отдельное поле**, в километровый пробег НЕ подставлять; показывать оператору, вердикт спецтехники остаётся «Проверить» |
| Заявка без даты вовсе (`нет данных`) | **Оставлять без разбора** — фолбэка на дату создания НЕ делаем (находка №10 закрыта как решение пользователя; ярлык вердикта из находки №11 всё равно поправить, номер он врёт) |
| Дата/пробег из комментариев | **Только комментарии клиента И только дата ≤ даты создания** заявки |
| Вторая жалоба в комментарии клиента | **Добавлять новой строкой разбора** со своей телеметрией и вердиктом |
| Единица «км» у путевого листа | Разрешить число без единицы, когда рядом явный ключ ПЛ (`по путл 97`) — решение принято по рекомендации |

Порядок П1 по весу: (1) `ПЛ Nкм` в актах ЦПО ССМ + слипшийся ключ `ССМ280,78км`; (2) дата только из
комментариев клиента и не позже создания; (3) ключи ПЛ — `по путевому N км`, число без единицы;
(4) моточасы в отдельное поле; (5) `с DD.MM`; (6) ярлык вердикта при найденном номере; (7) вторая
жалоба новой строкой; (8) в batch не терять регион, когда тема даёт полный номер.

### П1 (2026-08-02): починка пачкой по классам + регресс

Правки: `app/services/issue_automation.py`, `app/api/v1/endpoints/issues_dashboard.py`, фронт
(`types/index.ts`, `IssueDetail.tsx`, `TelemetryPanel.tsx`). Что сделано по классам:

| Класс | Что сделано |
|---|---|
| №1, №3, №4 (ключи ПЛ) | Ключи путевого листа переразделены на «единица опциональна» (`ПЛ`, `путев\w*` без «лист», `пут.л`, `путл`) и «единица обязательна» (`1С`, одометр, спидометр). Голое `ПЛ` — с границами, чтобы не ловить «площадь»/«план»/«пломба» |
| №5, №6 (моточасы) | Новое поле `engine_hours`: единица «м» больше не матчится перед `/ч`, `-ч`, `ч`; слово «моточасы» рядом с числом уводит его из километров. У одиночной заявки моточасы переносятся из шапки в строку разбора |
| №7 (слипшийся ключ) | Когда ключ (`ССМ`/`Галактика`/…) стоит вплотную к числу, запрет «буква перед числом» не применяется; для свободного поиска числа защита от гос.номера сохранена |
| №8 (дата из чужого сообщения) | `_build_comments_context` отдаёт разбору ТОЛЬКО комментарии клиента с их датами; дата неисправности не может быть позже сообщения, в котором названа. Модель по-прежнему видит всю переписку |
| №9 (предлог «с») | `с`/`со` добавлены в маркеры даты — и с годом, и `DD.MM` по `created_at`; «за период с 01.03 по 31.03» и «связано с 12 июня» не ловятся |
| №11 (ярлык) | `есть номер, нет даты` → «Нет даты»; «Номер не распознан» и «Нет номера/даты» — как раньше. Метка добавлена во фронт и в список служебных, не попадающих в ответ клиенту |
| №12 (вторая жалоба) | Пары «другая дата + пробег» из комментариев клиента становятся отдельной строкой с пометкой источника `(из комментария клиента)`, дедуп по (номер, дата) |
| №13 (регион) | Полные номера из ТЕМЫ достраивают регион всем строкам разбора по ключу без региона — единой точкой, не только для «номера из имени файла» |

**Регресс старый-против-нового на тех же 30 + 5 заявках C1** (новый код гонялся во временной копии
на сервере, боевой каталог не трогали): изменились 14 заявок, все изменения — целевые.

- вернулся путевой лист: 4576 `10 км` → «Данные верны» вместо «Проверить»; 4773 `по путл 97`;
  в актах ЦПО ССМ 12 строк получили ПЛ, из них 4 строки сменили «Проверить» → «Данные верны»
  (4538/О174ТХ56, 4343/Х990КО56, 4343/У671НК56, 4092/О406ТХ56, 4092/Т742ХУ56);
- 4092/Т740ХУ56: `ССМ280,78км` теперь читается (было `None`);
- моточасы: 3907 и 3723 `0.001 км` → `engine_hours=1`, 3498 `7 км` → `engine_hours=7`;
- 4537: дата больше НЕ берётся из нашего ответа поддержки → «Нет даты» вместо «Глушение» по чужому дню;
- 4674: появилась вторая строка `29.07 / 69 км` из комментария клиента;
- 3704: `с 02.07` разобрана → вердикт «Нет данных» вместо пустого разбора;
- 4015: `О613ТТ` → `О613ТТ56` (регион из темы), ярлык «Нет даты»;
- **остальные 21 заявка — байт в байт как были**, включая весь Саратов Северное.

Плюс диф по локальному корпусу (6037 текстов: темы 4642, training 761, OCR 634): дат изменилось
0, номеров 0; ПЛ — 38 улучшений на теле заявок и 3 на актах (2 из них — целевой увод моточасов).

**Известное ограничение, оставлено сознательно:** моточасы из АКТОВ (вложений) в `engine_hours` не
попадают — построчные парсеры носят кортежи из 4 полей, расширение до 5 задело бы ~10 мест.
В боевой базе таких актов 2; ошибочные `0.001 км` там больше не появляются (стало `None`).

**Проверено и НЕ сломано** (главный вопрос волны 1 — регрессия сессии 24):
- Составные ключи формат-профилей: Оренбург Северное/Центральное разбираются по своему формату,
  саратовский профиль (`ПЛ-N км`) им не мешает. Переименование ничего не сломало.
- Регион через пробел в теме: `А 506 НА`, `А 958 ОВ`, `Х 780 ХС` → номер собран верно.
- Номер без региона: `О885ХА` → в geo найден `О885ХА56`; `Т179НС` → `Т179НС56`.
- Кламп года по `created_at`: `за 02.07` → 2026-07-02, `за 17.06` → 2026-06-17.
- Тема-мультиномер: `О884ХА56, О892ХА56, Е740ВВ156, У202ЕЕ56` → 4 строки, `pack=0` у одного даёт
  честное «Нет данных» (объект в geo существует).
- Акт «Нет данных в программе Галактика» на 11 ТС (4645): все 11 номеров и дата 27.07 сняты верно,
  у всех 11 объектов geo реально 0 пакетов — «Нет данных» правомерно.
- Обязательная единица у ЗАЯВЛЕННОГО пробега на этих ПО ложных срабатываний не дала; сломала она
  только путевой лист без единицы (находка №4).

### C2 (2026-08-02): Ульяновские РС ×4 · Чувашэнерго ×3

Прогон: **34 заявки** `POST /parse` + **19** `POST /automate_batch` (17 в основном прогоне, 4206 и 2675
добором), всё бесплатно, между каждым вызовом `systemctl is-active` + HTTP 200 — сервис жив на всех
шагах. Состояние «ДО»: на все 34 заявки в `result_cache` была **одна** запись (64486/batch), то есть
поле чистое и «сломали vs так и было» здесь не спутать.

Выборка: Ульяновское 10 из 91 (один автор Ляхов), Барышское 10 из 15 (один автор Кочетков),
Димитровградское **все 6**, Ульяновские Южное **все 3**, Чувашэнерго **все 5** (Алатырское 3,
Северное 1, Южное 1). Мелкие ПО взяты целиком — их и всего столько.

**Главный вывод волны 1 по этим ПО — не про регулярки, а про тип заявки.** Нейтральный профиль
«Ульяновские РС» стоял с пометкой «формат не подтверждён примерами». Формат теперь виден, и он
такой: **это в подавляющем большинстве НЕ заявки о расхождении пробега.** Установка, замена,
подключение, отключение, деактивация, сезонное включение, перемещение объекта между ПО, сбой списка
ТС. Пробегов и путевых листов в них нет вовсе. У Барышского таких 9 из 10, у Димитровградского и
Ульяновского Южного — почти все, у Чувашэнерго — все пять. Пробеговая лестница гоняется на них
безусловно и выдаёт диагностические вердикты, обвиняющие оборудование или клиента.

Форматы по ПО (для замены заглушки на отдельные ключи профиля):

| ПО | Где данные | Кардинальность | Вложения |
|---|---|---|---|
| Ульяновское | вложение (описание пусто всегда) | multi | XLSX «Группировка ДД.ММ.ГГГГ» (строка = ТС) + PDF-акты на один ТС |
| Барышское | тема («модель + госномер»), пробегов в теме не бывает никогда | single, но **aggregate** при XLSX | 3 вида: PDF-письмо «ЗАЯВКА» (таблица, строка = ТС), XLSX-сводка на **весь парк** (~91 строка, приложена как доказательство), скан «ЗАКАЗ-НАРЯД» подрядчика |
| Димитровградское | два архетипа: тема+описание · PDF-письмо на бланке | single · multi | письмо «№ п/п \| Марка \| Гос. номер \| Местонахождение» + сканы заказ-нарядов, у которых **номер в имени файла** |
| Ульяновское Южное | два архетипа: тема+«Нет данных с ДД.ММ.ГГГГ» · PDF-письмо | single · multi | письмо со **спецномерами «УМ 4698»** и датой договора в шапке |
| Чувашэнерго Алатырское | **имя файла** (`07.07.2026_УР7749__ЗАЯВКА_УСТАНОВКА_658.pdf`) | single | письмо-скан на бланке + его дубль + `smime.p7s` + логотип подписи |
| Чувашэнерго Северное | имя файла | single | заказ-наряд «Приложение №2» с датой **работ** |
| Чувашэнерго Южное | описание (номер есть, даты нет, жалоба в процентах за месяц) | single | нет |

#### Классы дефектов C2

Статус: `найден` · `подтверждён кодом` (доказан не только наблюдением, но и местом в исходниках) ·
`на решение пользователя`.

| # | Класс дефекта | Где встретился | Суть | Статус |
|---|---|---|---|---|
| 1 | **Тип: нет ветки «это не заявка о расхождении пробега»** | 3844, 4527, 2853, 3900, 3242, 2554, 2200, 1591, 4528, 2675 — все 4 ПО | На заявке-установке/замене/отключении гоняется пробеговая лестница: «Не было питания» (3844, 4527), «Глушение» (2853), «Проверить/Данные верны» (3900, 3242, 2554, 2200), «Нет данных» (1591, 4528). Слов «установ/монтаж/подключ/отключ/замен/перемещ» нет ни в одном правиле | найден — **самый крупный класс волны** |
| 2 | **Таблица: спецномер `73ОО####` не ловится построчным парсером** | 4360 (2 стр.), 1252 (4 стр.) | `issue_automation.py:1003` берёт только `_PLATE_STD_RE`/`_PLATE_FALLBACK_RE`; ветка `\d{2}ЛЛ\d{4}` живёт лишь в `_PLATE_RE`. Тот же номер в ТЕКСТЕ заявки (3884) разбирается верно — один класс, два пути | подтверждён кодом |
| 3 | **Таблица: порядковый номер строки уезжает в заявленный пробег** | 4360 (стр. 9→`9.0`, 10→`10.0`), 1252 (`11.0`, `44.0`, `47.0`, `50.0`) | `issue_automation.py:1017-1023`: в безномерной ветке `allnums` собирается по ВСЕМ ячейкам, включая первую, дальше `declared=allnums[0]` | подтверждён кодом |
| 4 | **Таблица: моточасы принимаются за километры** | 4360/О120ОО73 (`1`), 4234/О120ОО73 (`2`), 4206/О120ОО73 (`1`) | Скачан исходный XLSX: у Амкадора ГЛОНАСС и «Пробег ТС» ПУСТЫ, заполнена только колонка «Моточасы». Извлечение выбрасывает пустые ячейки, `glonass=nums[0]` берёт первое число справа от имени → моточасы становятся км | подтверждён сырым файлом |
| 5 | **Таблица: лишняя ячейка сдвигает колонки → ложное «Глушение»** | 1252/К687НА73 за 13.05 | В строке лишняя ячейка: `sheet=3.0` вместо `4.9541`, система 7.98 → «Глушение». Та же машина за 14.05 без лишней ячейки разобрана верно | найден |
| 6 | **Моточасы из вложений не читаются** (`engine_hours` всегда null) | 4206 — все 8 строк, колонка «Моточасы» есть в шапке обоих XLSX | Известное ограничение П1 («построчные парсеры носят кортежи из 4 полей») подтверждено на живом файле, а не как гипотеза | найден, ограничение П1 |
| 7 | **Номер: спецформат «серия + 4 цифры» в таблице письма отбит гейтом** | 2436 (6 снегоходов → 0), 2675 (5 → 0), 544 («УМ 9520 73» потерян), 3900 («гос.№ УР7749» во 2-м файле) | `_plate_special_ok`: слева ячейка «Тайга-Атака \|», маркера «Гос.номер» ближе 25 символов нет, «№» из `_PLATE_MARKER_BEFORE_RE` намеренно исключён. Сам `_PLATE_RE` номер находит. Плюс список ТС собирается только `_PLATE_STD_RE` | подтверждён кодом |
| 8 | **Номер: приклеен к слову дефисом** | 820 (`уаз-х481ат73` → null) | Lookbehind `(?<![A-Za-zА-Яа-яЁё0-9.-])` в `issue_automation.py:301` содержит дефис. Сравн. 2670 `уаз т185тн73` через пробел — разобрано верно. Объект Х481АТ73 в системе есть | подтверждён кодом |
| 9 | **Номер: фантом из модели ТС → задвоение строки** | 4528 (`2322ВТ` — это модель ГАЗ-2322) | Модель принята за спецномер, `spec_vehicle=true`, одно ТС дало 2 строки | найден |
| 10 | **Номер: обрезан, `plate_format_suspect` не выставлен** | 4527 (`Н256РМ7` — перенос строки разорвал регион), 1252 (`СК116`, `АМ893`) | Регион из одной цифры — заведомо невалидный номер, но флаг подозрения не ставится | найден |
| 11 | **Имя файла: регион теряется, номер темы утекает в чужое вложение, строка пропадает** | 2670 (3 наряда): `М940РК` без региона; `Т185ТН73` из темы приписан вложению «РИСЭ», где в OCR свой номер `Е677ВА73`; `total=2` при 3 нарядах. 544: `К166ВВ` без региона + лишняя строка, не схлопнувшаяся с письмом | Номер из имени файла приходит без региона и не привязан к своему файлу | найден |
| 12 | **Дата: дата документа вместо даты неисправности** | 2436 (дата ДОГОВОРА 09.01.2023 вместо работ 15.04.2026), 2853 (дата заказ-наряда = день монтажа), 4172 (дата заявки-PDF 14.07 вместо акта 13.07), 2200 (дата начала обслуживания) | 2675 показал, что архетип сам по себе не ломается: там «с 09.04.2026 г.» взята верно. Ломается, когда в тексте есть более РАННЯЯ дата договора → чинить как **приоритет «дата работ > дата договора»**, а не как «письма ломают дату» | найден |
| 13 | **Дата: двузначный год не читается** | 2670 — все 3 наряда (`ДАТА: 15.04.26`, `ДАТА 15, 04.26.`) → «Нет даты» | найден |
| 14 | **Дата: словесная дата понятна шапке, но не построчному движку** | 4244 (`«15» июля 2026г.`): шапка `parse` взяла 2026-07-15, строка акта — «Нет даты» | Два разных результата на одном тексте | найден |
| 15 | **Дата: открытый интервал «Нет данных с ДД.ММ.ГГГГ» схлопнут в один день** | 4736 (`с 10.02.2026`, заявка от 29.07) | `date_to=null`, вердикт «Глушение» по одному дню; суть жалобы — данных нет 5,5 месяцев | найден |
| 16 | **Дата: кламп года непоследователен и не помечает догадку** | 4731 (2028 → 2026 молча; телеметрия намекает на 2025), 1252 (`За 14.05.2025` НЕ склампен, хотя вложения `14.05.2026.xlsx` и created 2026-05-18) | Одна опечатка правится молча, другая не правится вовсе | найден |
| 17 | **Ключи: синонимы и OCR-опечатки не распознаны** | 4044 (`по системе N км` — **наш собственный текст дочерней заявки**), 4244/4172 (`по одонометру` — OCR-опечатка), 4127 (`пробег составил N км` без ключа), 3884 (`-227м` — пробег в МЕТРАХ после дефиса) | Старый класс C1 с новыми представителями; 4044 отдельно неприятен — round-trip на своём же формате | найден (класс старый) |
| 18 | **Состав: один ТС в двух вложениях даёт две несведённые строки** | 4244 (заявка-PDF 16.07 + акт 15.07 → проверен пустой день 16.07 вместо реального 15.07), 4127 (13.07 + 09.07) | Даты по файлам не сведены | найден |
| 19 | **Состав: мусорная строка из логотипа почтовой подписи** | 1591 (`image001.png` → строка «Нет номера/даты») | найден |
| 20 | **Состав: агрегат не распознан** | 1252 — 182 строки (2 XLSX × ~91, весь парк ПО), `is_aggregate: false` | Сводка приложена как доказательство сбоя списка, а разобрана как 91 претензия по пробегу | найден |
| 21 | **Вердикт: ложное «Глушение»** | 2853 (терминал ФИЗИЧЕСКИ ОТСУТСТВОВАЛ — «провели монтаж нового», а ветка `packets>0 & low_sat>0.9 & питание в норме` дала «Глушение», и категория проходит `auto_eligible`), 4206/М878МС73 (пробег сошёлся: 1.71 заявлено против 2.23 системных, один телепорт на двухкилометровой поездке) | найден — **опаснее прочих: уходит в автоответ** |
| 22 | **Вердикт: `heuristic_category` врёт и противоречит вердикту строки** | 3900 (ПЛ нет вовсе → fallback `Данные верны`, `issue_automation.py:~2023`, ветки для `spec_vehicle` там нет), 4206 и 4360 (`verdict="Проверить"` при `heuristic_category="Данные верны"` в одной строке) | Оператор видит две взаимоисключающие метки | найден |
| 23 | **Вердикт: порог 7% применяется несогласованно** | 4234/М984МТ73 (`169.35/188`, факт 11% при клиентском пороге «НЕ БОЛЕЕ 7%») → «Данные верны»; 4360/М581МТ73 на похожих числах → «Проверить» | найден |
| 24 | **Вердикт: `packets=0` при НЕНУЛЕВОМ ложном пробеге** | 3884 — все 4 ТС (клиент просит обнулить ложные 227/133/101/914 м, ответ поддержки «Экранирование устранено») | Внутреннее противоречие фактов: «Нет данных» при том, что пробег есть. Класса «обнуление ложного пробега (экранирование)» в лестнице нет, хотя это архетип Ульяновского ПО (ещё 3540, 3447, 3295, 3193) | найден |
| 25 | **Устойчивость: удалённая в Okdesk заявка** | 3927 (65171), 3889 (65131) | Okdesk отдаёт `200 {"errors": "Записи не существует"}`, `Issue.model_validate` падает на отсутствующем `id`. `GET /attachments` → **HTTP 500**; `POST /parse` не падает (200), но пишет общую ноту «Обработайте заявку вручную», причину не называет, в `result_cache` не пишет ничего. Готовый `okdesk/service.py:60 issue_exists()` эти эндпоинты не зовут | подтверждён кодом |
| 26 | **Спецтехника: «Амкадор» не в `_SPEC_MODEL_RE`** | 4206, 4360 (О120ОО73 — фронтальный погрузчик) | `spec_vehicle=false`, машина без одометра меряется километровой лестницей (`_SPEC_MODEL_RE`, `issue_automation.py:130`) | подтверждён кодом |
| 27 | **Гео: объект существует под опечаткой в чужом филиале** | 1252/Т918ТМ73 → в системе `Т918ТН73 (Саратовские РС Центральное)` | Отличие в одну букву. Цеплять объект чужого филиала по нечёткому совпадению опасно | **на решение пользователя** |
| 28 | **Чувашэнерго: профиля нет вовсе** | все 5 заявок | Формат виден и РАЗНЫЙ по трём ПО (см. таблицу форматов). Опечатка `ПАО "Россети Волги"` у Северного на подбор профиля **не влияет** — ключи не содержат названия холдинга | найден |
| 29 | **Нет ветки «период = месяц, расхождение в процентах»** | 4642 (`ежемесячная разница в процентном соотношении пробегов … составляет 20-22%`) | Ни даты, ни километров — лестница встаёт на «Нет даты», телеметрия не запрашивается вовсе, хотя для месячного отчёта данных достаточно | найден |

#### Что проверено и НЕ сломано (в том числе починки П1)

- **Дата из ответа поддержки не вернулась**: в 3884, 4044, 4360 в комментариях сотрудников есть даты
  и телефоны — дата взята только из клиентского текста. Починка №8 из П1 держится.
- **Регион через пробел** собирается верно везде, где встретился: `В 347 ВВ 73`, `А 908 АУ 73`,
  `М 648 ВВ 73`, `О 269 АА 73`, `М 581 МТ73`, `Е 372 СУ 73`, `Т 890 АТ`, и не утаскивает день из даты.
- **Задвоенный регион** `М322НА73 73` нормализован верно.
- **Колонки XLSX не перепутаны** на нормальных строках: сверено построчно по 15 строкам в 4360/4234
  и 8 строкам в 4206 — `Пробег по ГЛОНАСС` → `declared_system_km`, `Пробег ТС` → `sheet_mileage_km`.
- **Раздача дат по файлам** верна: в 4206 три строки из `14.07.2026.xlsx` получили 2026-07-14, пять
  из `13.07.2026.xlsx` — 2026-07-13; дата письма (15.07) не подставилась.
- **Фантомов из мусора нет** нигде в выборке: телефоны поддержки, номера писем (`МР6-658`,
  `МР6/122/А/01/01/435`, `№_91`, `№7`), «20-22%», «д. 19», кривые OCR-штампы (`22242`, год `2226`),
  `#VALUE!` и `-100` из Excel клиента — ничего не стало ни номером, ни датой, ни пробегом.
- **`smime.p7s` не читается вообще** (`.p7s` нет ни в одном `_*_EXTS`) — бинарный мусор в текст
  разбора не попадает.
- **Честный отказ вместо выдумки**: где номера в тексте физически нет (3242, 820, 1252/parse, 4206
  без вложений, 3900/2853 без вложений) — нота «не удалось определить гос.номер, укажите вручную».
- **Спецтехника в ТЕКСТЕ ловится**: `5951УХ` (4045) распознан, в гео найден бульдозер, `spec_vehicle`
  и `needs_remote_diagnostics` выставлены; `73ОО3788` (3884) в тексте распознан верно.
- **Состав строк из текста**: 3884 — 4 ТС в одном описании разложены на 4 строки, каждая со своей
  датой (01.07 / 03.07 / 03.07 / 06.07), общая дата не схлопнута.
- «МКАТ-16» и «МКМ-200» в теме не приняты за номер; дедуп номера между двумя вложениями (542) работает.

#### «Такой формат, не дефект»

- Пустое описание в Okdesk у Ульяновского ПО и у 2853 — норма, вся суть во вложении.
- 4045 и 4044 — **дочерние заявки, сгенерированные нашим же кодом** (`создано из общей заявки #65123`,
  `issues_dashboard.py:2163`), их разбор частично циркулярен; 2 из 10 заявок Ульяновского
  нерепрезентативны для клиентского формата.
- Дубль-PDF одного письма двумя вложениями (3900, 1591) и мусорный OCR рукописных заказ-нарядов
  ГК «АСК» — свойство отправителя, важно лишь, что фантомов из них не родилось.
- Номера без региона в системном парке 1252 (`Е588МК`, `В792НЕ`, …) — региона нет в самом источнике.
- Разворот «Проверить» при `heuristic_category="Данные верны"` сам по себе намеренный
  (`feedback_verdict_over_autoclose`) — дефект только в том, что оператору видны обе метки (класс 22).

#### Непроверенное (помечено честно, не считать исправным)

- **Дедуп ТС между двумя XLSX одной заявки**: в 4206 пересекающихся машин не оказалось вовсе, ветка
  ни разу не сработала.
- **Существование объектов `73ОО5235` и `73ОО3788` в гео** по 4360 не проверялось (сверщики read-only,
  гео-запросов не делали) — классы 2-3 доказаны кодом и текстом файла, но не гео-ответом.
- **Полный текст обоих XLSX в 1252** (~91 строка × 2) сверен не построчно: источники ещё двух строк
  «Номер не распознан» (44.0 / 47.0) не сопоставлены. Паттерн подтверждён на строках 11 и 50.
- **2236**: комментарий поддержки говорит «Пробег 21.04.2026 составил 117,65 км», а телеметрия дала
  `packets=0`. Возможно, данные догрузились после прогона — проверять отдельно, низкая уверенность.
- **3889** — исходный формат заявки установить нельзя: запись удалена из Okdesk, в кэше только
  тема/автор/компания.

#### Что вынести на интервью 2 (перед П2)

1. **Тип заявки** (класс 1) — самое дорогое. Заводим ли распознавание «установка/замена/подключение/
   отключение/перемещение» и отдельный честный вердикт «Не заявка о расхождении пробега» вместо
   пробегового диагноза? Это меняет и лестницу, и то, что видит оператор.
2. **Ложное «Глушение» уходит в автоответ** (класс 21, 2853: терминала физически не было). Ужесточаем
   ветку `low_sat>0.9` или исключаем «Глушение» из `auto_eligible` при 33 пакетах за сутки?
3. **Обнуление ложного пробега / экранирование** (класс 24) — архетип Ульяновского ПО. Заводим
   отдельную категорию или оставляем «Проверить»?
4. **Открытый интервал «нет данных с ДД.ММ»** (класс 15) — считаем период до сегодня или до создания
   заявки, и что показываем вместо вердикта по одному дню?
5. **Опечатка года** (класс 16) — молча клампить, помечать догадкой или разворачивать в «Проверить»?
6. **Объект под опечаткой в чужом филиале** (класс 27) — цеплять нечётко или честно «не найден»?
7. **Профили**: заводим отдельные ключи `Ульяновские РС Барышское/Димитровградское/Южное ПО` и три
   ключа Чувашэнерго вместо нейтральной заглушки? Формат для каждого теперь известен.
8. **Жалоба в процентах за месяц** (класс 29, 4642) — считаем месячный отчёт или честно отвечаем
   «нужен конкретный день»? Сейчас — «Нет даты» и телеметрия не запрашивается вовсе.

### Решения интервью 2 (2026-08-02) → задание на П2, не перерешивать

| Вопрос | Решение |
|---|---|
| Класс 1 — заявки не о расхождении пробега | **Распознавать тип заявки, ставить отдельный вердикт, но телеметрию всё равно считать.** По словам «установ/монтаж/подключ/отключ/замен/перемещ/деактив» определяем тип и вместо пробегового диагноза ставим честное «Не заявка о расхождении пробега». Автоответ по таким заявкам не предлагаем. Телеметрия за день считается и показывается оператору **справкой** — ему важно видеть, ожил ли объект после монтажа |
| Класс 21 — ложное «Глушение» | **Ужесточить ветку: мало пакетов = не глушение.** «Глушение» из автоответа НЕ убираем — правим саму ветку. Порог не подкручиваем «на глазок»: считаем аномалией сутки, где пакетов на порядок меньше обычного для этого объекта, и разворачиваем такую строку в «Проверить» с пояснением «терминал почти не выходил на связь — вероятна неисправность или демонтаж». Конкретную границу подобрать в П2 и **обязательно показать на регрессе** (2853: 33 пакета против сотен-тысяч у живых объектов) |
| Класс 24 — обнуление ложного пробега | **Завести отдельную категорию «Ложный пробег / экранирование».** Заодно научиться читать пробег в МЕТРАХ («М 313 НА73-227м»). Автоответ по категории не предлагаем — решение за инженером |
| Класс 15 — открытый интервал «с ДД.ММ» | **Считать период от названной даты до даты создания заявки.** Не до «сегодня» — иначе задним числом потянем чужую телеметрию |
| Класс 16 — опечатка года | **Клампить в ОБЕ стороны + видимая пометка «год исправлен».** И будущий год, и сильно прошлый притягиваются к дате создания заявки, но рядом с датой видно, что это наша догадка, а не текст клиента |
| Класс 27 — объект под опечаткой в чужом филиале | **Оставить «объект не найден».** Нечёткое совпадение в одну букву по чужому филиалу не цепляем ни в каком виде |
| Класс 28 — формат-профили | **Завести все 6 ключей в П2**: `Ульяновские РС Барышское/Димитровградское/Южное ПО` + три ключа Чувашэнерго, у каждого свой hint (где номер, где дата, какие вложения). Нейтральную заглушку «Ульяновские РС» убрать |
| Класс 29 — жалоба в процентах за месяц | **Оставить как есть, но внятно объяснить.** Вместо сухого «Нет даты» — «Жалоба за месяц в процентах, нужен конкретный день». Месячный отчёт не считаем |

Технические классы, где решение не требовалось (чиню по месту): 2-5 (табличный XLSX), 6 (моточасы из
вложений — теперь идёт в одной пачке с классом 4), 7-14, 17-20, 22, 23, 25, 26.

**Порядок П2 по весу:**

1. **Класс 1** — распознавание типа заявки + отдельный вердикт + справочная телеметрия. Задевает
   лестницу, `auto_eligible` и фронт; делать первым, пока не оброс правками.
2. **Класс 21** — порог «мало пакетов ≠ глушение». Единственный класс, который сейчас уходит клиенту.
3. **Классы 2, 3, 4, 5, 6** — табличный XLSX одной пачкой: спецномер `\d{2}ЛЛ\d{4}` в построчном
   парсере, выброс порядкового номера строки из чисел, привязка чисел к КОЛОНКАМ (а не к «первому
   числу справа»), моточасы в `engine_hours` вместо километров.
4. **Класс 24** — категория «Ложный пробег / экранирование» + пробег в метрах.
5. **Класс 7** — спецформат «серия + 4 цифры» в таблицах писем: сейчас теряются ВСЕ ТС заявки
   (2436 — 6 из 6, 2675 — 5 из 5).
6. **Класс 15** — период вместо одного дня.
7. **Даты**: 12 (приоритет «дата работ > дата договора»), 13 (двузначный год), 14 (словесная дата
   в построчном движке), 16 (симметричный кламп + пометка).
8. **Номера**: 8 (дефис в lookbehind — однострочная), 9 (модель ТС не номер), 10 (`plate_format_suspect`
   при обрезанном регионе), 11 (имя файла: регион, привязка к своему вложению, потерянная строка).
9. **Состав и ярлыки**: 17 (ключи-синонимы), 18 (один ТС в двух вложениях), 19 (логотип подписи),
   20 (агрегат), 22 (`heuristic_category` против вердикта), 23 (порог 7%).
10. **Класс 25** — `issue_exists()` в эндпоинтах: 500 → внятное «заявка удалена в Okdesk».
11. **Класс 26** — «амкадор» и прочие погрузчики в `_SPEC_MODEL_RE`.
12. **Класс 28** — 6 ключей формат-профилей.
13. **Класс 29** — текст пояснения.

Класс 27 в П2 **не берём** (решение пользователя).

### П2 (2026-08-03): починка пачкой по классам + регресс

Правки: `app/services/issue_automation.py` (~630 строк), `app/api/v1/endpoints/issues_dashboard.py`
(~205), `app/services/attachment_reader.py` (~13), фронт — `types/index.ts`, `IssueDetail.tsx`,
`TelemetryPanel.tsx` (~300). Что сделано по классам:

| Класс | Что сделано |
|---|---|
| №1 (тип заявки) | `_detect_non_mileage_intent`: пять ярлыков (перемещение / замена / отключение-деактивация / подключение / установка-монтаж) + вердикт `NOT_MILEAGE_VERDICT` «Не заявка о расхождении пробега». Ярлык берётся СНАЧАЛА из слов клиента (тема+описание) и лишь потом из вложений — в наряде есть все слова разом, и «Установка новых приборов» получала ярлык «замена». Ограничитель: тип применяем только когда из заявки не извлечено ни одного пробега И нет прямых пробеговых маркеров. Телеметрия по-прежнему собирается и видна оператору справкой. Вердикт служебный → в автоответ не идёт |
| №2 (спецномер в таблице) | построчный парсер таблицы получил `_PLATE_SPECIAL_RE` — `73ОО5235`/`73ОО3788` больше не «Номер не распознан» |
| №3 (номер строки в пробег) | в безномерной ветке числа берём со ВТОРОЙ ячейки: порядковый номер строки в `declared_system_km` не попадает |
| №4 (моточасы как км) | `_xlsx` больше не выбрасывает пустые ячейки; колонки привязаны к шапке (`_GROUP_COLS`) ОТНОСИТЕЛЬНО ячейки названия ТС, поэтому мапинг верен и на свежем извлечении, и на старом схлопнутом кэше. Моточасы уходят в `engine_hours` |
| №5 (лишняя ячейка) | следствие №4: привязка по колонкам, а не «первое число справа» |
| №6 (моточасы из вложений) | ограничение П1 снято: моточасы носятся отдельной картой `(номер, дата) → м/ч`, кортеж `targets` не расширялся (иначе задело бы десяток ветвей) |
| №7 (спецномер в письме) | `_plate_special_ok`: код региона за номером («УМ 5379 73») больше не считается соседней ячейкой; при наличии в документе колонки «Гос. номер» достаточно ОДНОСТОРОННЕЙ изоляции. Единицы измерения («1500 КМ») отбиты явно |
| №8 (дефис) | `_PLATE_HYPHEN_RE`: «уаз-х481ат73» читается; даты «15-06-2026» и модели «Нефаз-4208» не затронуты |
| №9 (модель как номер) | спецномер не берётся из строки, где уже есть обычный гос.номер: «2322ВТ бригадный \| А 818 МЕ 73» больше не даёт фантомный ТС |
| №10 (обрезанный номер) | `_PLATE_VALID_FULL`: регион 2-3 цифры либо его нет; «Н256РМ7» теперь `plate_format_suspect` |
| №11 (имя файла) | номер из ТЕМЫ больше не приписывается чужому вложению при мульти-документе; глобальный дедуп схлопывает строку без даты, если тот же ТС есть с датой. **Регион для `М940РК` восстановить нечем** — его нет ни в теме, ни в имени файла, ни в читаемом OCR; оставлено как есть |
| №12 (дата документа) | `_drop_contract_dates`: реквизит «договор № … от ДД.ММ.ГГГГ» вырезается ПЕРЕД поиском даты (после разбора номера) |
| №13 (двузначный год) | последняя ветка цепочки — `_DATE_SHORT_BOUNDED` с жёсткими границами по цифрам |
| №14 (словесная дата) | `_MONTH_DATE_RE` допускает кавычку между днём и месяцем: канцелярское `«15» июля 2026г.` |
| №15 (открытый интервал) | «Нет данных с ДД.ММ.ГГГГ» → период от названной даты до ДАТЫ СОЗДАНИЯ заявки (не до «сегодня») |
| №16 (кламп года) | кламп стал симметричным + поле `date_year_fixed` и пометка «год испр.» во фронте. Прошлое чиним только если тот же день-месяц в году заявки ложится в ±45 дней от её создания — законная старая дата (`07.10.2024` при заявке от 13.04.2026) не трогается |
| №17 (ключи) | `по системе N км` (наш же формат дочерней заявки), OCR-опечатка `одо\w{0,3}метр` («одонометр»), метры после дефиса — см. №24 |
| №18 (один ТС в двух файлах) | **не сделано осознанно** — см. «Что осталось» |
| №19 (логотип подписи) | `_SIGNATURE_IMAGE_RE`: `image001.png` и т.п. в разбор не берутся |
| №20 (агрегат) | `_is_aggregate`: ≥25 распознанных ТС — сводная выгрузка, независимо от непустого описания |
| №21 (ложное «Глушение») | флаг `sparse_data` при 0 < пакетов < 200 и разворот в «Проверить». Порог по данным: у живых объектов медиана 1250 пакетов/сутки, p25=749, p5=178; все «Глушения» с малым числом пакетов — 6, 33, 144, 145, 146, а кластер 144-146 это ровно пакет в 10 минут, т.е. СТОЯЩИЙ терминал на heartbeat. «Не было питания» стоит в лестнице выше и сохраняется |
| №22 (врущий ярлык) | `_heuristic_category` больше не проваливается в безусловное «Данные верны»: нет путевого листа — «Проверить»; `sparse_data` — «Проверить» |
| №23 (порог 7%) | **НЕ ТРОГАЛ.** Подкрутка допуска 10%→7% перевернула бы вердикты по всей базе, а не только на C2 (память `feedback_verdict_over_autoclose`: пороги не подкручиваем). Нужно отдельное решение пользователя с замером |
| №24 (ложный пробег) | категория `FALSE_MILEAGE_VERDICT` «Ложный пробег / экранирование» + чтение метров, приклеенных к номеру («М 313 НА73-227м» → 0.227 км). Ветка «голое число за номером» включается ТОЛЬКО в этом архетипе — общее правило рождало фантомные километры |
| №25 (удалённая заявка) | `_live_issue()` + `issue_exists()`: 410 Gone с внятным текстом там, где был 500; `/parse` и `/automate_batch` отдают 200 с нотой «Заявка удалена или слита в Okdesk». `issue_exists()` зовётся ТОЛЬКО после падения валидации — здоровая заявка по-прежнему стоит один поход в Okdesk. В `result_cache` пустышка не пишется (затёрла бы правки оператора) |
| №26 (спецтехника) | в `_SPEC_MODEL_RE` добавлены амкадор/автовышка/АГП/БКМ/ямобур/снегоход/болотоход/манипулятор/автокран |
| №28 (профили) | заведены ШЕСТЬ ключей вместо нейтральной заглушки «Ульяновские РС»: Барышское, Димитровградское, Ульяновское Южное + три ключа Чувашэнерго, у каждого свой hint (где номер, где дата, вид вложений). Проверено: профили C1 (Оренбург, Саратов) подбираются как раньше |
| №29 (проценты за месяц) | вместо сухого «Нет даты» — нота «Жалоба сформулирована за МЕСЯЦ и в процентах… нужен день». Месячный отчёт не считаем |

Плюс не из журнала C2, найдено по ходу: **`/automate_batch` вообще не получал тип заявки** (эндпоинт
зовёт `analyze_batch` напрямую, минуя `parse_facts`) — класс №1 работал бы только через `/parse`.
Тип теперь считается внутри `analyze_batch` по КАЖДОМУ документу, а сам эндпоинт стал отдавать
сводный `parsed` (раньше не отдавал вовсе, и фронт не мог показать ни ярлык, ни «год исправлен»).

#### Регресс старый-против-нового на 34 заявках C2

Гонялся ДВУМЯ копиями приложения в `/tmp` со своими копиями базы; боевой каталог и боевая база не
менялись. Устаревший OCR табличных вложений в копиях обнулён (13 записей) — иначе новое извлечение
XLSX не проверить. **Прогоны шли скриптом в процессе, а не через uvicorn** (см. «Осторожно» ниже).

`ИЗМЕНИЛОСЬ: 39 прогонов, БАЙТ В БАЙТ: 14`, ошибок в прогонах ноль. Целевые изменения:

- **4360** (Ульяновское, 13 ТС): спецтехника `73ОО5235`/`73ОО3788` получила номера и моточасы
  (4 и 5 м/ч), строка «Номер не распознан» исчезла; у Амкадора `declared 1.0 км` → `engine_hours 1.0`
  (то же в 4234 и 4206). Проверено по СЫРОМУ XLSX: у этих строк колонки ГЛОНАСС и «Пробег ТС» пусты,
  заполнены только «Моточасы».
- **2436**: 0 из 6 снегоходов → **6 из 6**; **2675**: 0 из 5 → **5 из 5**. Дата в 2436 больше не
  дата договора (было `2023-01-09`, стало `2026-04-15`).
- **3884** (обнуление): все 4 ТС на месте, у каждого появился заявленный пробег в метрах
  (0.227 / 0.133 / 0.101 / 0.914 км) и вердикт «Ложный пробег / экранирование» вместо «Нет данных».
- **2853**: ложное «Глушение» (терминала на машине физически не было) → «Не заявка о расхождении
  пробега», флаг `sparse_data` выставлен. **4527**: «Не было питания» на заявке о включении блока →
  тоже ушло. **3844** — то же.
- **1252** (сводка на весь парк): `is_aggregate` False → **True**, строк 182 → 204 (распознались
  спецномера), дата `2025-05-14` → `2026-05-14` с пометкой «год исправлен». Массово исчез
  `sheet_mileage_km` и «Данные верны» сменилось на «Проверить» — и это ПРАВИЛЬНО: в этом файле
  колонка «Пробег ТС» ПУСТА у всех строк, а старый код брал за путевой лист колонку «Отклонение по
  пробегу», т.е. прежние «Данные верны» стояли на выдуманном числе.
- **3927 / 3889** (удалены в Okdesk): нота стала называть причину. В старом прогоне на них внутри
  падал `pydantic ValidationError`, в новом трассы нет.
- **820**: «уаз-х481ат73» → номер распознан, строка появилась. **4044**: `по системе 10.83 км`
  прочитано. **4642**: появилась нота про месяц и проценты. **4731 / 4736**: открытый интервал
  получил `date_to` = дата создания.

#### Диф по ВСЕМУ локальному корпусу (5754 текста: темы заявок, training-образцы, OCR)

Главная проверка «не задели чужие ПО». Изменений: `plate` **2**, `date` **204**, `date_to` 0,
`sheet` **2**, `declared` 0, `engine_hours` 0. **Ни одного `значение → None` ни по одному полю** —
разбор ничего не потерял, только добавил.

- `plate` +2: «Расхождение в показаниях УАЗ-**Х048ХА64**» и «уаз-х481ат73» — обе от правки дефиса.
- `date`: 189 случаев `None → дата` и 15 `дата → другая`. Первые — два архетипа, ранее уходившие
  в «Нет даты»: Жигулевское «Отправка: 15.06.**26**_…» (двузначный год) и Саратовское
  «**2026.05.18** Неисправность приборов ГЛОНАСС на …» (ISO через точку).
  Все 15 «дата → другая» — ровно исправление ГОДА при совпадающих дне и месяце (2025→2026, 2023→2026)
  плюс `ocr:1455` `2023-01-09 → 2026-04-15` (та самая дата договора).
- `sheet` +2: два акта с OCR-опечаткой «одонометр» (82 км и 169 км) — раньше пробег терялся целиком.

**Найдено регрессом и исправлено по ходу** (иначе уехало бы на прод):
1. В ветке «без вложений» переменная типа заявки была не определена → `analyze_batch` падал, и
   заявка 3884 схлопывалась с 4 ТС до 1. Тип вынесен в общий помощник `_intent_for()`.
2. Последняя ветка поиска даты без границ читала «2026.05.18» с середины как «26.05.18» и давала
   26 мая вместо 18-го (5 заявок). Добавлены границы по цифрам и отдельная ветка ISO через точку.
3. Задвоение строки: тот же ТС с датой и без неё (61292) — схлопывается в датированную.

Фронт: `npx tsc -p tsconfig.app.json --noEmit` чисто (113 файлов, из них 27 в `src/`), `npm run build`
проходит и кладёт в `app/static/`, `eslint` — 0 ошибок. Оба новых вердикта попали в список служебных,
отображение новых полей устойчиво к их отсутствию (старый кэш разбора валиден).

#### ⚠ Осторожно на будущее: копия приложения — это ещё и копия Telegram-бота

Регресс сначала поднимали как второй `uvicorn` из копии каталога. Копия запустила Telegram-бота, и
три бота на один токен подрались за `getUpdates`: боевой сервис ~45 секунд сыпал
`TelegramConflictError` и в это время не получал обновлений. Инстансы сняты, опрос восстановился сам.
**Правильный способ**: прогонять разбор скриптом В ПРОЦЕССЕ (`parse_issue_facts`/`automate_batch`
вызываются напрямую с построенными зависимостями), из каталога копии и со СВОЕЙ копией базы — путь к
БД относительный (`sqlite+aiosqlite:///./support_agent.db`), поэтому запись уходит в копию.

#### Что осталось незакрытым

- **Класс 23** (допуск 10% против клиентского «не более 7%») — требует отдельного решения
  пользователя, в П2 не входил.
- **Класс 18** (один ТС в двух вложениях даёт две несведённые строки, 4244/4127) — не сделан:
  сведение строк по одному ТС из разных документов задевает дедуп и раздачу даты во всех ветвях
  `analyze_batch`; это отдельная правка, а не пачка регулярок. Помечено как непочиненное.
- **Класс 11 частично**: регион у `М940РК` восстановить нечем.
- В 1252 одна из новых строк (`0342УХ`) получила «Ошибка данных» — это флапающий гео-запрос
  (`object_status_failed` виден в логах обоих прогонов), а не дефект разбора.

#### Проверка на проде после деплоя (2026-08-03, `1970289`)

Сервис перезапущен, HTTP 401 без токена, ошибок в логе нет, Telegram-опрос стартовал с
`failures_streak: 0`. Перед проверкой удалены **13 устаревших OCR-записей** табличных вложений:
извлечение XLSX изменилось, и на старом кэше колонки читались неверно (кэш восстанавливается сам
при следующем разборе, платного OCR это не требует). База забэкаплена в
`/root/support_agent.db.pre-p2`.

Подтверждено на живых заявках: 3844 → «Не заявка о расхождении пробега» (было «Не было питания»);
3884 → 4 строки «Ложный пробег / экранирование» с пробегом в метрах; 2675 → 5 снегоходов из 5;
4360 → спецтехника `73ОО5235`/`73ОО3788` с моточасами 4 и 5, у Амкадора `engine_hours=1.0` вместо
фантомного «1 км»; 2853 → ложное «Глушение» ушло, флаг `sparse_data` выставлен; 3927 → нота
называет причину; 4642 → нота про месяц и проценты; 4736 → `date_to` = дата создания заявки.

**Новое наблюдение (в очередь, не дефект П2).** У 4736 период получился 5,5 месяца, и вердикт по нему
вышел «Глушение». Пороги глушения откалиброваны НА СУТКИ (`teleport_jumps >= 5`, `low_sat_ratio > 0.08`,
`speed_spike_count >= 100`): на длинном окне телепорты накапливаются, а доли усредняются, поэтому
«Глушение» по интервалу в месяцы доверия не заслуживает. Порог `sparse_data` (200 пакетов/сутки) на
длинном окне тоже не срабатывает. Нужно либо нормировать пороги на длину окна, либо на интервале
длиннее N суток отдавать «Проверить» с раскладкой по дням. Вынести на интервью 3.

### C3 (2026-08-03): Самарские РС Волжское ПО

Прогон: **12 заявок** `POST /parse` + **12** `POST /automate_batch`, всё бесплатно, между каждым вызовом
`systemctl is-active` + HTTP 200 — сервис жив на всех шагах, OCR завершился с первой попытки у всех
12 (`ocr_progress.complete=True`). Состояние «ДО» по этим 12 заявкам ПУСТО: они самые свежие
(создано 31.07-03.08), а прежние 311 `batch` Волжского лежат на более старых заявках.

**Это первая проверочная сессия НА КОДЕ ПОСЛЕ П2** — заодно служила регрессом П2 на чужом
архетипе. Все три сверщика независимо подтвердили: **регрессии актового пути нет**. Пробеги и даты
читаются полностью, ложного «Не заявка о расхождении пробега» не появилось ни на одной заявке,
моточасы вместо километров не подставляются. Правило П2 про `image001.jpg` сработало: у 4931
OCR-записи по картинке подписи нет вовсе (пропуск стоит ДО вызова Tesseract), лишней строки
«Нет номера/даты» нет.

#### ГЛАВНЫЙ ВОПРОС ВОЛНЫ 2 — ОПРОВЕРГНУТ

Волна 2 ставилась под подозрение: «обязательная единица у заявленного пробега (сессия 24) сломается
на актах, где «км» стоит в ШАПКЕ КОЛОНКИ далеко от числа». **Не сломалась.** Табличные акты
Волжского дают ровно такую структуру — `Пробег в системе ГЛОНАСС (км) | 93`,
`Пробег по путевому листу (км) | 104` — единица в скобках ПЕРЕД числом, число за разделителем.
Чистый тест: **4860**, где в тексте акта чисел нет вообще («В программе ССМ ГЛОНАСС отсутствует
информация о пробеге») и таблица — единственный источник: извлечено `sheet 141` / `declared 109`.
Работает за счёт ветвей «единица ПЕРЕД числом»: `_DECLARED_KM_RE` (`[^\d№]{0,25}?\b(?:км|м)\b…`) и
первой ветви `_WAYBILL_KM_RE`, которая единицу вообще не требует. По всем 12 заявкам оба пробега
извлечены; посторонних чисел (номер акта «№29», год, телефон `89276531566`) в пробегах нет.

#### Фактура Волжского: ЧЕТЫРЕ подтипа, а не один

Профиль в коде описывает только первый. Остальные три увидены впервые:

| Подтип | Тема | Вложение | Особенности |
|---|---|---|---|
| A. «Отправка» | `Отправка: 03.08.2026_м720хт_Соболь _Акт38_ВПО` | **Word .docx** | жёсткая структура темы, дата+номер+модель+№акта; текст чистый |
| B. «FW: акт» | `FW: акт Т 130 АС 31.07.26г` | **PDF-скан** | номер с ПРОБЕЛАМИ, двузначный год, дата бывает СЛИТА с номером; OCR грязный |
| C. «FW: Акт \<номер\>» | `FW: Акт c147cc` | PDF + `image001.jpg` | **даты в теме нет вовсе**, номер латиницей строчными, картинка подписи Outlook |
| D. «Расхождение в глонасс» | `FW: Отправка: Расхождение в глонасс от 30.07.2026 У 538 ОК` | PDF | без «Акт№»; чисел в тексте нет, всё в таблице |

Внутренность PDF-акта (подтип B) — скан с грязным OCR: «Акт № от 29.07.2026г», список комиссии,
«в системе с **902026** произошел сбой БО ГЛОНАСС», «( пробег по **CMC** — 120 км, по одометру 161 км».
То есть дата НЕИСПРАВНОСТИ внутри тела акта регулярно разрушена (`902026`, `991072020`,
`84:07. 2026`, `30.07 80861.`) — и ни одна из этих руин в результат не утекла: дата берётся по маркеру
«Дата неисправности» либо из темы. Гос.номер внутри акта печатается ЛАТИНИЦЕЙ
(`H718HB 763`, `M665KT`, `H800BM 763`) — транслитерация работает, объект в гео находится.

#### Классы дефектов C3

| # | Класс дефекта | Где встретился | Суть | Статус |
|---|---|---|---|---|
| 1 | **Номер: день даты, слипшийся с номером, читается как код региона** | 4934 (`Н718НВ29`), 4945 (`Н807ОР29`) | Тема `акт Н 718 НВ29.07.26г` → регион «29» это ДЕНЬ. Номер формально валиден (буква+3цифры+2буквы+2цифры), поэтому `plate_format_suspect` НЕ встаёт — оператор не видит, что номер выдуман. Корень точечный: в `_PLATE_STD_RE` у ветки с регионом ЧЕРЕЗ ПРОБЕЛ есть страж `(?![\d.,:/-])`, у СЛИТНОЙ ветки `\d{0,3}` — нет. Поэтому «МА 24.007.2026» (через пробел, 4952) регион не прилепил, а «НВ29.07.26г» прилепил | подтверждён кодом |
| 2 | **Актовая ветка не имеет фолбэка номера из темы/имени файла → платный разбор УХУДШАЕТ результат** | 4945 | Бесплатный `parse` нашёл объект `ГАЗ Sadko NEXT Н807ОР 763` и дал вердикт. После `automate_batch` строка стала «Номер не распознан», `telemetry: null`, `jamming_count` 1→0. Фолбэк «номер только в теме» есть в НЕактовой ветке, в `elif act_like` его нет. Плюс UI читает кэш в порядке `("batch","parse")` — то есть показывает именно ухудшенный результат. **Не свежий**: обе ветки старше П1/П2 | подтверждён кодом — **самый опасный класс сессии** |
| 3 | **Вердикт: ложное «Глушение» по одному артефакту при идеальных спутниках** | 4951 (бесспорно), 4966 и 4943 (пограничные) | 4951: `avg_sat 16.3`, `low_sat_ratio 0.0`, `speed_spike 0`, телепортов 2, но `max_implied 7591 км/ч` — сработало правило `max_implied>500 && teleport_jumps>=1`. Без флага лестница дала бы «Данные верны» (\|68−63.81\|=4.2 ≤ 6.8). 4966: `low_sat 0.10` против порога 0.08, телепортов 3. 4943: `low_sat 0.18` при среднем 14.4 спутника. Для сравнения, бесспорное глушение (4944: `avg_sat 1.3`, `low_sat 0.9`, 3875 прострелов) выглядит совершенно иначе | найден — три случая независимо, **на решение пользователя (порог)** |
| 4 | **Заявленный: ключ «СМС/ССМ» ЛАТИНИЦЕЙ не распознаётся** | 4952, 4934, 4940 (во всех — `по CMC`) | Прямая проверка: `_DECLARED_KEY_FULL` даёт 0 совпадений на латинском «CMC» и совпадение на кириллическом «СМС». Потери НЕ случилось только потому, что в этих актах читалась табличная часть («Пробег в системе ГЛОНАСС (км)»). Выстрелит на акте, где таблица не распознана | подтверждён тестом, **латентный** |
| 5 | **Заявленный: нет фолбэка «единица в скобках ПЕРЕД числом» в разборе ТЕЛА заявки** | латентно, на выборке не проявился | В `parse_issue` у путевого листа фолбэк `\((км\|м)\)[^\d\n]{0,4}(\d+…)` есть, у заявленного — нет: он требует единицу только ПОСЛЕ числа. Приди такая же таблица не вложением, а в ТЕЛЕ письма — путевой лист прочитается, заявленный потеряется. У Волжского описания пустые, поэтому не сработало | подтверждён кодом, **латентный** |
| 6 | **Дата: трёхзначный месяц не нормализуется** | 4952 (`24.007.2026` в теме И в имени файла) | `_DATE_SHORT_RE` не даёт совпадения ни в одной позиции строки; заплатка `_norm_typo_year` покрывает только 3-значный ГОД. Бесплатный разбор → «Нет даты»; правильную дату 24.07 спасает только OCR акта | подтверждён кодом |
| 7 | **Противоречие внутри ОДНОГО акта не показывается** | 4924 | Нарратив: «по СМС – 107 км, по одометру – 135 км». Таблица того же акта: «ГЛОНАСС (км) \| 58», «путевой лист (км) \| 67». `re.search` берёт ЛЕВОЕ совпадение, т.е. всегда нарратив; таблица и сам факт расхождения оператору не видны. Какие числа верные — вопрос к клиенту | найден |
| 8 | **Номер: OCR-путаница цифра↔буква не лечится** | 4945 (`Гос. номер ТС H 8070P`) | После транслита получается `Н8070Р` — не валидный формат, строка уходит в «Номер не распознан» | найден |
| 9 | **Спецтехника: усечённый номер сам по себе даёт `spec_vehicle`** | 4924 (автобус ПАЗ `ЕК424`) | `_is_special_vehicle`: «номер не обычного формата → спецтехника». Автобус помечен спецтехникой, км-лестница отключена. Последствие консервативное («Проверить»), но причина неверна и оператор видит неверную пометку | найден |
| 10 | **Шапка пакетного разбора не синхронизирована со строками** | 4934, 4943, 4944, 4952, 4966 | В `parse_facts` синхронизация есть, а эндпоинт `automate_batch` считает шапку только `parse_issue` по теме/телу. Оператор видит в шапке `Н718НВ29` и пустые пробеги, хотя строка уже несёт `Н718НВ763` и 175/154; бейдж «номер подозрительный» во фронте считается по шапке. **Это недоделка П2**: сводный `parsed` в batch-ответ добавил я, чтобы фронт показывал ярлык типа и «год исправлен», и не синхронизировал его со строками | найден — **моя недоделка** |
| 11 | **`ocr_progress` считает пропущенную подпись в знаменателе** | 4931 | `attachments_total: 2, attachments_done: 1, complete: true` — оператор видит «1 из 2» при полностью выполненном разборе. Следствие правила П2 про `image001.jpg` | найден — **моя недоделка**, косметика |
| 12 | **Регион то приклеен, то отрезан между шапкой и строкой** | 4860 (`У538ОК` в шапке, `У538ОК163` в строке) | Один и тот же текст даёт два разных номера. Объект в гео найден в обоих случаях | найден, низкий приоритет |
| 13 | **Гео: код региона при поиске объекта игнорируется** | 4934 (ложный регион «29» совпадению не помешал) | Объект `н718нв763` нашёлся по номеру с ЧУЖИМ регионом. Здесь помогло, но на другом ТС так же тихо подберётся машина другого региона | найден, **латентный риск** |

#### Что проверено и НЕ сломано

- **Регрессии П2 нет** (главный вопрос этой сессии): все 12 заявок дали пробеги и даты, служебный
  вердикт «Не заявка о расхождении пробега» не появился нигде, `image001.jpg` корректно пропущен.
- **Даты**: двузначный год `26г` читается как 2026; дата СОСТАВЛЕНИЯ акта нигде не подменила дату
  неисправности (4966 — акт от 03.08, взято 01.08; 4924/4922 — акт от 03.08, взято 31.07; 4860 —
  акт от 31.07, взято 30.07; 4931 — даты в теме нет, взята из тела акта `Дата неисправности 31. 07.2026г`,
  а НЕ дата акта 03.08). Дата, слитая с номером (4945) и двойной пробел (4940) — обе прочитаны верно.
- **Транслитерация номера из скана**: `H718HB 763→Н718НВ763`, `M665KT→М665КТ`, `H800BM 763→Н800ВМ763`,
  `c147cc→С147СС`, `c155cc→С155СС`. Гео нашло объект в 11 заявках из 12.
- **Обрезанный номер в теме** (`М 665`, `Н 800`) на бесплатном уровне честно даёт `plate=null` и
  внятную ноту «укажите номер вручную или разберите по вложениям» — номер не выдумывается.
- **Пробеги не перепутаны нигде**, включая 4944, где по документу «в системе» (160) БОЛЬШЕ одометра
  (100): разбор отдал `sheet 100 / declared 160` ровно как в акте. Первоначальное подозрение на
  перепутанные колонки СНЯТО — так написано в исходнике.
- **Один акт = одна строка** во всех 12; лишних и потерянных строк нет, `is_aggregate: false` везде.
- Ложных «Данные верны» на выборке нет ни одного. 4922 — «Нет данных»/«Диагностика» при `packets: 0`
  и `last_message_date: 2026-07-04` — верно.

#### Что вынести на интервью 3 (перед П3)

1. **Класс 2** — самый дорогой: разбор по вложениям может УХУДШИТЬ результат, и UI показывает именно
   его. Заводим фолбэк номера из темы/имени файла в актовой ветке? И/или меняем приоритет кэша,
   чтобы худший результат не затирал лучший?
2. **Класс 3** (ложное «Глушение») — три случая. Требуем ли для `max_implied>500` ещё и плохие
   спутники? И что делать с пограничными `low_sat 0.10` против порога 0.08?
3. **Класс 7** — противоречие нарратива и таблицы внутри одного акта: какие числа считать верными,
   и показывать ли оператору сам факт расхождения?
4. **Класс 9** — усечённый номер как признак спецтехники: разделять «нестандартный номер» и
   «спецтехника»?
5. **Класс 13** — учитывать ли код региона при поиске объекта в гео (сейчас игнорируется).
6. Классы 1, 4, 5, 6, 8, 10, 11, 12 — техника, решения не требуют, чиню по месту в П3.

Плюс осталось с предыдущих волн: **класс 23 из C2** (допуск 10% против клиентского «не более 7%»),
**класс 18 из C2** (один ТС в двух вложениях), и **пороги глушения на длинном интервале** (наблюдение
после деплоя П2).

### П3 (2026-08-03): починка пачкой по классам C3 + регресс

Правки: `app/services/issue_automation.py`, `app/api/v1/endpoints/issues_dashboard.py`, фронт
(`types/index.ts`, `IssueDetail.tsx`, `TelemetryPanel.tsx`). Что сделано по классам:

| Класс | Что сделано |
|---|---|
| №1 (регион из даты) | Страж `(?!\d)(?![.,/-]\d{1,3}(?:[.,/-]\|\s*г\b\|$))` добавлен СЛИТНОЙ ветке региона в `_PLATE_RE` и `_PLATE_STD_RE` (раньше был только у ветки через пробел). «Н 718 НВ29.07.26г» больше не даёт выдуманный «Н718НВ29». Диапазон региона оставлен `\d{1,3}`, чтобы битый «Н256РМ7» сохранился и по-прежнему помечался `plate_format_suspect` (правка П2) |
| №2 (разбор ухудшал результат) | В `elif act_like` добавлен фолбэк номера из имени файла/темы, когда OCR акта номер не прочитал — но ТОЛЬКО при одном документе (иначе номер темы приписался бы каждому непрочитанному файлу, эту ошибку чинили в П2). 66180: строка снова получает `Н807ОР`, объект «ГАЗ Sadko NEXT Н807ОР 763» и вердикт вместо «Номер не распознан» с пустой телеметрией |
| №4 (латинское «по CMC») | В `_DECLARED_KEY_FULL` буквы С и М у ССМ/СМС заданы классами «кириллица-или-латиница», добавлен `glonas\w*`. Проверено прямым тестом: раньше 0 совпадений на латинице, теперь заявленный пробег читается |
| №6 (месяц из трёх цифр) | `_fix_triple_month`: «24.007.2026» → «24.07.2026». Применяется ДО разбора НОМЕРА (не только перед датой) — иначе страж региона не узнаёт в таком тексте дату и приклеивает день к номеру. Нашлась вторая такая заявка: «АКТ Т 506 АС 21.005.26г» (63527) |
| №7 (текст против таблицы в акте) | `_act_table_numbers` + `_numbers_conflict`: числа таблицы («ГЛОНАСС (км) \| 58») сравниваются с числами нарратива («по СМС – 107 км»). Расхождение > 1 км и > 2% даёт пометку `act_numbers_differ` и разворот причинного вердикта в «Проверить»; цифры оставляем как есть, выбор за оператором (решение пользователя, интервью 3). Проверка стоит в ДВУХ ветвях: актовой и «номер в имени файла» — Word-акты Волжского идут во вторую |
| №9 (ПАЗ как спецтехника) | `_is_special_vehicle` больше не считает спецтехникой всякий нестандартный номер: только модель из `_SPEC_MODEL_RE` либо НАСТОЯЩИЙ спецформат (`_SPEC_PLATE_RE`: `64СН3847`, `5297СУ`, `СУ5297`). Битый номер отмечается только `plate_format_suspect` |
| №13 (конфликт регионов) | Регион из номера заявки сверяется с регионом в НАЗВАНИИ найденного объекта; несовпадение даёт `region_conflict` и разворот в «Проверить». Номер БЕЗ региона сопоставляется свободно, как и раньше |
| №18 (две даты у одного ТС) | Пометка `two_dates_one_plate` + разворот. Строки НЕ сводим (решение пользователя). Условие УЗКОЕ: ровно один ТС на заявку, больше одной даты И больше одного файла — см. регресс ниже |
| №10, №11 (недоделки П2) | Шапка `automate_batch` синхронизируется с ЕДИНСТВЕННОЙ строкой (строка побеждает шапку: номер из акта точнее номера из темы); `ocr_progress` не считает пропущенную подпись в знаменателе, добавлено поле `attachments_skipped` |
| №28 (профиль Волжского) | hint переписан под ЧЕТЫРЕ подтипа с их ловушками (разрушенная OCR дата внутри акта, латиница номера и ключа, числа в нарративе против таблицы) |
| №3 (порог глушения) | **НЕ ТРОГАЛ** — решение пользователя: абсурдная implied-скорость считается одним из ГЛАВНЫХ признаков глушения, набираем статистику ([[project_operator_checks_all_verdicts]]) |
| №5 (единица в скобках у заявленного) | **Находка НЕ ПОДТВЕРДИЛАСЬ.** Сверщик утверждал, что фолбэка нет. Прогон теста: таблица в ТЕЛЕ заявки читается — `ПЛ=141, заявл=109`. Фолбэк в коде есть с прошлых сессий, чинить было нечего |
| №8 (OCR-путаница цифра↔буква) | **НЕ ДЕЛАЛ осознанно.** «H 8070P» → «Н8070Р»: после фолбэка номера из класса 2 заявка 66180 разбирается верно и без этого, а нечёткое сопоставление номера — риск подобрать ЧУЖУЮ машину. Записано как непочиненное |
| №12 (регион шапка↔строка) | Закрылся сам синхронизацией шапки (класс 10) |

#### Новая находка П3: интервал НЕ работает сквозь телеметрию

Пытаясь прокинуть `date_to` в строки разбора, обнаружил, что **класс 15 из П2 реализован только
наполовину**. Проверено прямым замером на объекте В627РО (заявка 66024, «Нет данных с 02.02.2026»):

| Окно | `get_daily_stats` | `get_packets` |
|---|---|---|
| один день 02.02 | 0 записей | 165 |
| интервал 02.02 → 29.07 (178 суток) | **158 записей** | **165** |

Окно считается верно, суточную статистику гео слушается, а `get_packets` отдаёт одно и то же число
независимо от ширины окна. Значит `facts.packets` на длинном окне бессмысленно — а именно на нём
стоит порог `sparse_data` (200 пакетов/сутки) из П2. Показывать оператору «метрики за весь период»
было бы ложью, поэтому прокидывание `date_to` в строки **откачено**; фронт к нему готов заранее и
просто не зажигает пометку. Починка требует разбора агрегации телеметрии, а не пачки регулярок —
вынесено на интервью 4.

#### Регресс старый-против-нового

Базовая линия — боевой `eba3517` (результат П2). Гонялись две копии приложения со своими копиями
базы; боевой каталог и боевая база не менялись, **uvicorn не поднимался** (см. предупреждение в
разделе П2). Выборка: 34 заявки C2 + 12 заявок C3 = 46 `parse` и 31 `automate_batch`.

`ИЗМЕНИЛОСЬ: 23 прогона, БАЙТ В БАЙТ: 54`, ошибок нет. Вердикт сменился РОВНО В ОДНОМ месте —
`parse:4952` «Нет даты» → «Проверить» (целевой эффект класса 6). Остальное: 7 изменений номера,
6 снятий ложной пометки спецтехники, синхронизация шапок.

Диф по всему локальному корпусу (5755 текстов): номер **7**, дата **2**, заявленный пробег **9**,
путевой лист / интервал / моточасы — **0**. **`значение → None` — ноль по всем шести полям.**
Все 7 изменений номера объяснены: шесть — «дата отклеилась от номера» (4288, 4345, 4489, 4719,
4934, 4945), одно — улучшение `ocr:464` («гос. номер а137мо 763,» → регион приклеился, раньше
строгий страж отбрасывал его из-за запятой). Единственное изменение уже существовавшего пробега
(`ocr:185`, 234.54 → 77.44) разобрано: в одном OCR-документе ДВА акта, в первом ключ латиницей
(«по CMC — 77,44»), во втором кириллицей («по СМС — 234,54»); старый код латиницу не видел и
подставлял в первый акт число из второго. В бою текст режется по актам, так что это исправление.

#### Регресс поймал ТРИ моих собственные ошибки — все в одном страже

Ни одну из них не показали 10-15 придуманных контрольных случаев:

1. Страж `(?![\d.,:/-])` отвергал регион перед ЛЮБОЙ запятой → **32 заявки** теряли регион,
   включая многономерные темы «О884ХА56, О892ХА56, Е740ВВ156» и «FAW К674ХН164 и ещё 12».
2. Пометка «две даты у одного ТС» срабатывала на 62959 (сводка по ВСЕМУ парку в двух суточных
   отчётах) → **40+ вердиктов** уходили в «Проверить» без причины: там каждая машина законно
   встречается на две даты. Отсюда узкое условие «один ТС + разные даты + разные файлы».
3. После сужения страж съел регион в «М 313 НА73-227м» — за регионом дефис с цифрой, но это пробег
   в МЕТРАХ (архетип класса 24), а не дата. Плюс внутри этой же правки обнаружилась опечатка:
   регулярки собираются f-строками, и `{1,2}` молча превращалось в подстановку `(1, 2)` — регексп
   получался другим, а тесты «зелёными».

Вывод занесён в память `feedback_plate_regex_needs_corpus`: правку регулярок номера и даты
проверять ТОЛЬКО дифом по всему корпусу, смотреть направления изменений, и двоить фигурные скобки
в f-строках.

#### Фронт

Новые пометки сомнений (`region_conflict`, `act_numbers_differ`, `two_dates_one_plate`) — чипы у
вердикта с расшифровкой при наведении, неизвестный код не ломает интерфейс. Длина окна телеметрии
показывается при `date` ≠ `date_to` (пока не зажигается, см. находку выше). Отдельно: субагент
заметил, что до П3 чип «спецтехника» ФАКТИЧЕСКИ подменял сигнал о битом номере в таблице, а сам
`plate_format_suspect` жил только в баннере, который виден лишь после платного прогона ИИ. После
разделения классов 9 сигнал исчез бы из таблицы совсем — поэтому у гос.номера появилась отдельная
тихая пометка. `npx tsc -p tsconfig.app.json --noEmit` чисто, `npm run build` проходит,
`eslint` — 0 ошибок.

#### Что осталось незакрытым

- **Класс 3** (порог глушения) — по решению пользователя набираем статистику.
- **Класс 8** (OCR-путаница цифра↔буква в номере) — не делан, причина выше.
- **Класс 23 из C2** (допуск 10% против клиентского «не более 7%») — ждёт отдельного решения.
- **Класс 18 из C2** — строки не сводятся, только помечаются (решение пользователя).
- **НОВОЕ: интервал не работает сквозь телеметрию** (`get_packets` игнорирует ширину окна) —
  на интервью 4.

#### Проверка на проде после деплоя (2026-08-03, `a67633d`)

Сервис перезапущен, HTTP 401 без токена, ошибок и конфликтов в логе ноль, Telegram-опрос стартовал
с `failures_streak: 0`. База забэкаплена в `/root/support_agent.db.pre-p3`. OCR-кэш в этой сессии
НЕ чистился: извлечение вложений не менялось.

Подтверждено на живых заявках:

| Класс | Заявка | Результат |
|---|---|---|
| 1 | 4934, 4945 | `Н718НВ`, `Н807ОР` — фантомный «регион» из дня даты исчез |
| 6 | 4952 | дата `2026-07-24` (месяц «24.007.2026» прочитан) |
| 2 | 4945 (batch) | `Н807ОР`, ПЛ 65 / заявл 47, вердикт «Глушение» вместо «Номер не распознан» |
| 9 | 4924 | `spec_vehicle=false` И `plate_format_suspect=true` — два сигнала разведены |
| 7 | 4924 | `warnings=['act_numbers_differ']` при 135/107 в тексте против 67/58 в таблице |
| 10 | 4934 (batch) | шапка `Н718НВ763` + пробеги 175/154, то есть синхронизирована со строкой |
| 11 | 4931 | `ocr_progress` = «1 из 1» плюс `attachments_skipped: 1` |
| контроль | 1252 | 204 строки, вердикты распределены нормально (37 «Глушение», 62 «Проверить», 98 «Нет данных») — массового разворота в «Проверить» НЕТ, ошибка сужена верно |

### C4 (2026-08-03): Чапаевское · Жигулевское

Прогон: 20 `POST /parse` + 12 `POST /automate_batch`, всё бесплатно, сервис `active` и HTTP 401 без токена проверены между каждым из 32 вызовов, ни одного зависания. Выборка: по 10 заявок на ПО, с вложениями 7 (Чапаевское) и 5 (Жигулевское); 75 вложений, все `.docx`. Решения пользователя по глубине: построчно сверяем **8 самых рискованных строк на заявку**, остальные — скопом (число строк против числа актов, раздача даты, дубли); заявки не про пробег берём как есть. Итог прогона: 240 строк разбора, «Данные верны» — **3 строки из 240**.

**Два разных архетипа, оба видны по вложениям, а не по теме:**

| ПО | Архетип | Что несёт номер и дату | Пробеги |
|---|---|---|---|
| Чапаевское (166 заявок, автор Гомзяков) | ОДИН `.docx` = много ТС | нумерованный список 17–30 позиций внутри ОДНОЙ ячейки таблицы, номера БЕЗ региона (фактически все 763); в теме номеров нет вовсе | графы «Пробег по путевому листу» и «Пробег в системе Глонасс» **пусты всегда** — вердикт может опираться только на телеметрию |
| Жигулевское (165, автор Трофимов) | МНОГО `.docx` = много ТС (1–28 актов) | номер и дата в ИМЕНИ файла (`27.07.26__СУ_5297_ВАЛЬТРА_Заявка_№СР83_ЖПО.docx`), разделители плавают (`__`, двойная точка `03.08..26.`, `А_499_НТ`) | таблица акта из 6 строк; рабочая дата — поле «Дата неисправности» ВНУТРИ акта, она НЕ равна дате в имени файла (обычно на 1–3 дня раньше), бывает диапазоном «С 20.07 по 31.07» |

| # | ПО | Заявка | Ось | В исходнике | В разборе | Класс дефекта | Статус |
|---|---|---|---|---|---|---|---|
| 1 | Чап. | 4913, 4843, 4798, 4580, 4452, 4749, 4662 — **65 строк из 103** | Номер | `9.⇥ГАЗ, М244ХО` ⏎ `10.⇥ГАЗ, Н896НУ` | `М244ХО10`, `Н896НУ11`, … `Р625ОО17` | `Номер: номер пункта списка приклеен как код региона` — портится всё с пункта 10, когда нумерация двузначная | найден — **самый массовый класс сессии** |
| 2 | Чап. | 58 строк из 103 | Вердикт | тот же парк ЧПО, регион 763 | `warnings=[region_conflict]`, вердикт разжалован «Глушение» → «Проверить» при `heuristic_category='Глушение'` | `Вердикт: ложный region_conflict как следствие №1` (страж П3 сработал честно, причина другая) | найден, следствие №1 |
| 3 | Чап. | 4913 #9; 4843 #9,13,16,17,18,22,26; 4798 #16 | Номер | `22.⇥ГАЗ, М244ХО` | `М244ХО23`, вердикт вынесен, `warnings=[]`, `plate_format_suspect=false` | `Номер: склейка проходит молча` — у объекта в гео нет региона в названии, страж не сработал; выдуманный номер формально валиден | найден, следствие №1 |
| 4 | Жиг. | 4880 | Состав строк | вложений в Okdesk **28** | `attachments_total: 25, done: 25, complete: true`; акты `А974ТМ №СМ120`, `М396УМ763 №СР102`, `М341УМ №СР103` не читались вовсе, этих ТС в вердикте НЕТ | `Состав: жёсткая обрезка вложений + ложное «обработано всё»` — `_BATCH_MAX_ATTACHMENTS=25` (`:3540`), обрезка `:3771`, знаменатель считается ПОСЛЕ обрезки (`:4114`) | найден — **вторая потеря данных, подтверждена кодом** |
| 5 | Жиг. | 4915 (`№ТР150`), 4916 (`№ТР02`), 4880 (`№ТР138`) | Состав строк | у ТС М790ЕА за один день ДВА путевых листа: 203/157 и 157/47 | остаётся один акт, второй выброшен молча, счётчик не уменьшается, `warnings` пусты | `Состав: дедуп (номер, дата) теряет второй путевой лист` (`:4028`) — дедуп оправдан мульти-акт-PDF, формат ЖПО в него не заложен | найден — **первая потеря данных** |
| 6 | Жиг. | 4915 #3 АМ6283, #11 СВ8797; 4916 #0; 4880 #2 ТО8723 | ПЛ / единицы | `Пробег по путевому листу \| 41 ч`, `Пробег в системе Глонасс \| 34,10 мин` | `sheet_mileage_km=41.0` (как км), `engine_hours=None`, `declared_system_km=None` | `ПЛ: моточасы «ч» приняты за км, «мин» потеряны` — архетип класса из П1 в новой обёртке (единица одной буквой, без слова «моточас») | найден |
| 7 | Жиг. | 4880 #13 Н822ОВ; 4915 #6,7,10,12,4 | Вердикт | ПЛ 85 → наш пробег 86.21 (1.4%); 227→225.8; 220→213.6 | «Глушение» | `Вердикт: флаг глушения стоит в лестнице ВЫШЕ совпадения пробега` (`:4494`) — клиент получает «глушение» там, где наши данные ПОДТВЕРЖДАЮТ путевой лист | найден — **самый вредный вердикт сессии** |
| 8 | Жиг. | 4880 #4, 15, 19, 13 | Вердикт | клиент жалуется, что система показывает БОЛЬШЕ путевого (16→33.8; 19→26; 137→230; 85→105) | «Глушение» ×4 | `Вердикт: направление расхождения не учтено` — глушение объясняет недобор, а не перебор, диагноз логически невозможен | найден |
| 9 | оба | 4913 #5 и 4580 #4 В152ТУ; 4662 #4, #11; 4880 #23 и 4917 #9 Е179АК; 4523 | Вердикт | `tj=0`, спайков 0, `impl` 90–148 км/ч, 3875–3998 пакетов, `avg_sat` 13.0 | «Глушение» по одному признаку: `low_sat_ratio>0.08` (`:2512`) либо `impl>500 & tj>=1` (ОДИН битый фикс) | `Вердикт: ложное глушение по единственному слабому признаку` — примерно каждое пятое; у Е179АК `low_sat_ratio=1.0` в ДВА разных дня, т.е. протокол объекта просто не отдаёт число спутников | найден |
| 10 | оба | 4452 #1 (1257.9 км при `move_time` 82.4 мин ≈ 916 км/ч), #4 (1068 км за 17 мин), 4662 #6, 4880 #9 М910МС (428.55 при ПЛ 80, `impl=182994`) | Гео | графы пробега в исходнике пусты | `system_mileage_km` подан оператору как факт без пометки недостоверности | `Гео: наш собственный пробег, накрученный телепортами, выдан как факт` | найден — **опасен: это цифра в ответе клиенту** |
| 11 | Жиг. | 4915 (7 строк из 15), 4916 (6 из 13), 4523 | Дата | рабочая дата — «Дата неисправности» ВНУТРИ акта (22.07), в имени файла дата отправки (23.07) | `parse` берёт дату из имени файла/темы → телеметрия за чужой день; 4523: за 23.07 ноль пакетов → «Нет данных», а за 22.07 391 пакет | `Дата: бесплатный parse спрашивает телеметрию за день отправки` — систематическое расхождение parse↔batch на всём ПО | найден |
| 12 | Жиг. | 4915 #11 СВ8797 | Дата | `Дата неисправности \| С 20.07.2026 по 31.07.2026` | `date=2026-07-20`, `date_to` в строку не попал → телеметрия за одни сутки → «Нет данных» | `Дата: диапазон схлопнут в первый день` — родня находки П3 «интервал не работает сквозь телеметрию» | найден |
| 13 | Чап. | 4866 | Дата / период | живой текст «Нет данных **с** 13.07.2026» | шапка `date=13.07`, `date_to=31.07`, а строка и телеметрия — только за 13.07 (`tail_gap`/`day_profile` ставятся лишь при `day_to==day`) | `Дата: интервал посчитан в шапке, но до строки не доехал` — 18 дней молчания в анализ не попали | найден, то же семейство |
| 14 | оба | 4915 #9, 4916 #8 и #11, 4917 #3,4,10, 4880 #24 + 58 строк Чапаевского | Вердикт | — | `verdict='Проверить'` при `heuristic_category='Данные верны'` (и наоборот при «Глушение») | `Вердикт: две взаимоисключающие оценки в одной строке` — класс 22 из C2 не закрыт на ветке «ПЛ есть, но не сошёлся» | найден |
| 15 | Чап. | 4913/4843/4798/4580 | Вердикт | — | `jamming_count` = 8/15/5/16, а флаг `jamming` стоит у 12/24/9/22 строк | `Вердикт: счётчик шапки расходится со строками` (следствие №2) | найден |
| 16 | Жиг. | 4880 #5 (`СМ122`), 4917 parse #13 (`СР9108`) | Номер | акта с таким «номером ТС» не существует — это номер акта серии ЖПО | фантомная строка, «Объект не найден»; у 4880 фантом создал batch, у 4917 — parse, при том что batch его НЕ создал | `Номер: номер акта принят за госномер, страж работает непоследовательно` | найден |
| 17 | Жиг. | **4772** (тема «СМ102») | Номер / Гео | живой текст: «Расхождение пробега… (создано из общей заявки #65870)» — это **дочерняя заявка, созданная НАШИМ сплиттером**, а `СМ102` — номер акта из темы родителя | `plate='СМ102'` → объект «КАВЗ-4235 автобус **М102СТ**» по трём цифрам; вердикт «Нет данных» вынесен по ЧУЖОЙ машине. `plate_format_suspect=true` поднят, но ничего не останавливает | `Номер: фантом из номера акта материализуется в реальную дочернюю заявку и получает вердикт по чужому ТС` | найден — следствие №16, **но опаснее: попадает в Okdesk** |
| 18 | Чап. | 4560 (65804) и 4555 (65800) | Состав | клиент дважды прислал «Заявка 156»: 65762 в 08:30 и 65764 в 08:44 | из обоих родителей созданы дети с идентичным разбором (К690ОК, 22.07) | `Состав: сплиттер не сверяет создаваемых детей с уже существующими по (номер, дата)` | найден |
| 19 | оба | Чап. 4913 (17 ТС) и 4798 (17) против 4843 (29) и 4580 (30); Жиг. 4917 (12) против 4880 (25) | Классификация | одинаковые письма одного ПО и одного автора | `is_aggregate` = false/true по порогу «≥25 уникальных номеров» (`issues_dashboard.py:48`) | `Состав: порог сводности режет один архетип пополам` — часть заявок отвечается сводно, часть разрезается на детей | найден |
| 20 | Чап. | 4580 #29 `Н189НУ30`, 4452 #19 `Н479РЕ20` | Гео | опечатка КЛИЕНТА: в парке есть `Н189ЕУ` и `Тойота Камри Н749РЕ` (найдены в других строках) | «Объект не найден», телеметрия пуста, похожий номер не предложен | `Гео: опечатка клиента не даёт подсказки оператору` | найден |
| 21 | Жиг. | 4880 #14 (`№ТР140`/31.07 в имени против «№ТР126 от 29.07» в теле), 4917 #1 (`ЖР266`/`ЖР259`), #5 (`СР9108`/`СР108`) | Дата / номер акта | расхождение имени файла и тела документа | `warnings=[]` | `Состав: расхождение имя↔тело не помечается` | найден |
| 22 | Жиг. | 4971 (`last_message_date=2026-03-16`, трекер молчит 4.5 месяца) против 4637 (`2026-08-03`, трекер жив) | Вердикт | — | оба «Нет данных» + удалённая диагностика | `Вердикт: мёртвый трекер и пустой день не различаются` | найден |
| 23 | Чап. | 4798, позиции 6 и 11 | Состав строк | клиент задвоил ТС: `6. КАМАЗ, М719МА … 11. КАМАЗ, М719МА` | 18 позиций → 17 строк, схлопнуто без пометки | `Состав: дубль в исходнике не показан оператору` (схлопывание верное) | найден, косметика |

**Корень классов 1–3 и 15 — одна строка.** `_parse_body_vehicles` (`app/services/issue_automation.py:996`) начинает с `body = _strip_html(text)`, а `_strip_html` (`:1712`) делает `re.sub(r"\s+", " ", …)`. Перевод строки — единственная граница между пунктами списка, и обе регулярки номера на неё прямо опираются: у `_PLATE_STD_RE` (`:481`) в комментарии написано «перевод строки — уже следующий пункт списка, иначе в `В152ТУ\n2.` прилипал бы 2». Нормализация пробелов обесценивает эту защиту ДО поиска номера, и ветвь региона через пробел забирает «10.», «11.» … Страж `(?![.,/-]\d{1,3}…)` заточен под даты и здесь не помогает: после «10.» идёт таб и марка, а не цифра. Воспроизведено дважды независимо — прогоном копии `_PLATE_RE` по исходному тексту и сравнением `extract_all_plates` (чистые номера) с `_parse_body_vehicles` (склеенные).

**Масштаб класса 1 по всему корпусу OCR** (замер: 858 документов из `result_cache`, номера построчно против номеров после `_strip_html`): **238 испорченных номеров из 2051**, в 27 документах.

| ПО | Испорчено номеров |
|---|---|
| Чапаевское | 228 |
| Приволжское (Саратов) | 5 |
| Барышское | 2 |
| Волжское · Димитровградское · Чувашэнерго Северное | по 1 |

Барышское и Димитровградское проходили в C2, Волжское — в C3. **Класс существовал всё время, и три сессии его пропустили**: на других ПО списки короче десяти позиций, а склейка начинается ровно с пункта 10. Вывод для метода: длина нумерованного списка — отдельный признак при построении выборки.

#### Про массовое «Глушение» (главный вопрос сессии)

Вердиктов «Глушение» 25 из 88 строк в `parse` и больше половины в `batch`. Проверено по телеметрии, а не по ожиданию: **это НЕ вердикт по пустым данным** — под «Глушением» 316–9363 пакета в сутки, единственная строка с `packets=0` (4580 #12) корректно ушла в «Проверить» через `sparse_data`. Расклад: примерно **две трети** — реальный спуфинг (десятки-сотни телепортов, `max_implied_kmh` 3792–182994, `avg_sat` 5–8), там вердикт защитим. Остальное — три разных ложных механизма: (а) флаг стоит выше проверки «ПЛ ≈ система», поэтому диагноз выносится там, где наш пробег СОШЁЛСЯ с путевым (класс 7); (б) вход по одному артефакту `impl>500 & tj>=1`, то есть по одному битому фиксу (класс 9); (в) `low_sat_ratio` как постоянное свойство объекта, а не событие (Е179АК: 1.0 в два разных дня при идеально ровном потоке). Слабое звено — **порядок ветвей лестницы, а не значения порогов** (памятка `feedback_verdict_over_autoclose` о неподкручивании порогов не нарушается).

Оба вердикта «Данные верны» у 4880 проверены построчно — **ложных нет**, оба защитимы (Н026ОВ: ПЛ 26 → 27.09, `tj=0`, `avg_sat` 15.3; Е728АК: ПЛ 15 → 13.82 при клиентских 13.62).

#### Что проверено и НЕ сломано (в том числе починки П1–П3)

- **Состав строк Чапаевского точен**: 17/29/17/30/20/24/29 позиций документа → столько же строк. Ни потерь, ни фантомов из шапки-инструкции, номера заявки, телефонов подписи и ФИО. Порядок сохранён.
- **Раздача общей даты работает** у всех 103 строк Чапаевского: дата шапки, а не номер заявки (164), не телефон, не дата отчёта; `date_year_fixed=false`.
- **Пустое остаётся пустым**: где графы пробега в документе пусты, там `sheet_mileage_km`, `declared_system_km`, `engine_hours` = null во всех 103 строках. Ничего не выдумано.
- **Починки П3 держатся на новом ПО**: к номеру не приклеилась ни одна дата и ни один номер заявки (`№ТР148`…`№СР114`), двойная точка `03.08..26` разбор не сломала, год не потерян нигде, спецтехника опознана (`spec_vehicle=true` у ЕТ-400, ХАРВЕСТЕРА, ТО8718/ТО8723) и честно разворачивается в «Проверить» вместо км-вердикта.
- **Починка П2 «заявки не про пробег» держится**: 4550 («передача Т/С в Мордов-Энерго, прошу убрать из программы галактика») → `issue_intent='отключение / деактивация'`, «Не заявка о расхождении пробега», пробеговый диагноз не поставлен, дата не выдумана, номер `К631СС` взят из живого текста верно.
- **Номера Жигулевского**: пробелы и подчёркивания свёрнуты верно («Х 652 АТ», «А_499_НТ», «АМ 6283»), модель из имени файла (ЕТ-400) за номер не принята, регион 763 не потерян там, где он есть, `Н274МВ` и `Н274МВ763` разрешаются в один объект 18115.
- **Цифры актов Жигулевского**: 27 пар «ПЛ / заявленный» совпали с исходником до знака, включая дроби 117,98 / 200,83 / 0,701 и честный ноль; кроме случаев с единицами «ч»/«мин» (класс 6).
- **Дата batch берёт «Дату неисправности» из акта**, а не дату отправки (4880: 30.07 при письме от 31.07; 4917: 02.08 при письме от 03.08) — на платном пути класс 11 не проявляется.
- OCR/извлечение: `complete=true` у всех 12 batch, 75 вложений; `verdict_source='rules'` везде, платный ИИ не звался ни разу.

#### Что вынести на интервью 4 (перед П4)

1. Класс 5 — дедуп `(номер, дата)`: два путевых листа одного ТС за один день. Резать дедуп по номеру акта, показывать оба пробега или помечать?
2. Класс 4 — лимит 25 вложений: поднимать, обрабатывать пачками или оставить, но честно показывать «25 из 28, три не разобраны»?
3. Классы 7, 8 — порядок лестницы: что побеждает, флаг глушения или совпадение пробега с путевым; и разворачивать ли «Глушение» при переборе (система > путевого).
4. Класс 9 — `low_sat_ratio` как постоянное свойство объекта: как отличать свойство от события.
5. Класс 17 — фантомные дети из номеров актов: нужен ли страж серий ЖПО (`СМ/СР/ТР/ШР/ЖР` + цифры) и что делать с уже созданными в Okdesk детьми-фантомами.
6. Класс 11 — чинить ли дату в бесплатном `parse` для формата ЖПО или бесплатный уровень оставить как есть, полагаясь на batch ([[project_answer_sources]]).
7. Класс 10 — показывать ли `system_mileage_km`, раздутый телепортами, и с какой пометкой.
8. Класс 19 — порог сводности: привязывать к архетипу/ПО вместо числа 25?
9. Классы 14, 15 — синхронизировать `verdict` и `heuristic_category` или показывать обе оценки.
10. Классы 20, 22 — предлагать похожий номер при опечатке клиента; различать мёртвый трекер и пустой день.

Классы 6, 12, 13, 16, 18, 21, 23 — техника, решений не требуют, чиню по месту в П4.

### Решения интервью 4 (2026-08-03) → задание на П4, не перерешивать

| Вопрос | Решение |
|---|---|
| Класс 5 — два путевых листа одного ТС за день | **Две строки, по одной на акт.** В ключ дедупа добавить номер акта: разные акты = разные строки со своими цифрами и вердиктами. Настоящие дубли (ОДИН акт, приложенный дважды) по-прежнему схлопывать |
| Класс 4 — лимит 25 вложений | **Разбирать все + честный счётчик.** Лимит поднять (Word читается быстро: 28 файлов за 8 с), знаменатель `attachments_total` считать ДО обрезки, чтобы «готово» никогда не врало. Для тяжёлых сканов/PDF защита остаётся по ВРЕМЕНИ, а не по числу файлов (память `project_event_loop_blocking`) |
| Классы 7, 8 — флаг глушения против совпавшего пробега | **«Проверить» с двумя причинами.** Когда наш пробег сошёлся с путевым, «Глушение» клиенту не отвечаем, но и «Данные верны» автоматом не штампуем: прострелы координат есть, решает оператор. Пороги НЕ подкручиваем ([[feedback_verdict_over_autoclose]]) |
| Класс 17 — фантомные дети из номеров актов | **Страж + ревизия базы.** Серии актов (`СМ/СР/ТР/ШР/ЖР` + цифры) никогда не считать госномером; отдельно прочесать базу и показать список уже созданных фантомных детей в Okdesk — удалять/закрывать их решает пользователь, сам не трогаю |
| Класс 9 — `low_sat_ratio` как свойство объекта | **Нет данных о спутниках — признак не считается.** Доля «плохих» фиксов ровно 1.0 при среднем числе спутников около нуля = протокол молчит, а не глушение: признак игнорируем и судим по телепортам, скорости и пробегу |
| Класс 11 — дата в бесплатном `parse` (формат ЖПО) | **Вердикт не давать, звать на разбор вложений.** Дата в имени файла — день отправки; бесплатный `parse` для этого формата честно говорит, что нужен разбор вложений (он тоже бесплатный), а не выносит вердикт по чужому дню ([[project_answer_sources]]) |
| Класс 10 — пробег, раздутый телепортами | **Помечать недостоверным и не сравнивать.** При рваном треке (телепорты, невозможная скорость) цифра показывается с явной пометкой «недостоверно» и НЕ используется ни в вердикте, ни в ответе клиенту |
| Классы 14, 15 — вердикт против ярлыка | **Синхронизировать.** Одна строка не может нести «Проверить» и «Данные верны» одновременно; счётчик глушений в шапке считать по тем же строкам, что видит оператор |
| Класс 22 — мёртвый трекер и пустой день | **Различать.** «Молчит 4,5 месяца» и «жив, но нет данных за день» — разные поводы для монтажника, ответ должен быть разным |
| Класс 19 — порог сводности | **Решает ФОРМА письма, а не число машин.** Одно вложение с общим списком ТС (архетип Чапаевского) — всегда сводный ответ, детей не создаём. Отдельные акты на каждую машину (архетип Жигулевского) — поштучно, как сейчас. Порог 25 остаётся только страховкой от выгрузок по всему парку (62959) |
| Класс 20 — подсказка похожего номера при опечатке клиента | **В П4 не берём** (не выбрано пользователем). Остаётся непочиненным |

### П4, часть 1 (2026-08-03, на проде `69b1696`): классы 1, 4, 5

| Класс | Что сделано |
|---|---|
| №1 (склейка номера с номером пункта) | Добавлен `_strip_html_keep_lines` (`:1748`) — блочные HTML-теги становятся переводом строки, `</td>`/`</th>` СОЗНАТЕЛЬНО нет (иначе ключ пробега и число разъезжаются по строкам и ломается Жигулевское). Используется ТОЛЬКО в `_parse_body_vehicles`, сам `_strip_html` не тронут (он в десятке других ветвей). Плюс `_ORPHAN_REGION_RE` (`:987`): диф нашёл 6 номеров, где на следующей строке стоял НАСТОЯЩИЙ регион (`АГП Т309УС \n164` — 63317 Приволжское, `А 818 МЕ\n73` — 65772 Барышское) — простая правка их бы потеряла; страж не даёт вернуться самому дефекту |
| №4 (обрезка вложений) | `_BATCH_MAX_ATTACHMENTS` 25 → 100; `attachments_total` считается по ПОЛНОМУ списку пригодных вложений, появился `attachments_skipped`, при обрезке `complete` больше не может быть `true`. От тяжёлого OCR защищает бюджет ВРЕМЕНИ (`_BATCH_REQUEST_BUDGET_S = 60`, он уже был), а не число файлов. В `issues_dashboard.py:1275` скидка на картинки подписи теперь ПРИБАВЛЯЕТСЯ к `attachments_skipped`, а не затирает число из сервиса |
| №5 (дедуп съедал второй путевой лист) | Ключ глобального дедупа стал `(номер, дата, файл)`. Настоящий дубль схлопывается двумя путями: мульти-акт PDF — внутри вложения (как было, 64513), один акт двумя файлами — по совпадению всех цифр `(номер, дата, ПЛ, заявленный, моточасы)` |

**Диф по корпусу** (5831 текст: OCR + темы + описания + training): **226 номеров стали короче**,
`значение → None` — **0**, номеров «длиннее/другие» — 0, изменений даты, ПЛ и заявленного пробега — **0**,
число строк не изменилось нигде, шапка `parse_issue` байт в байт. Каждое из 226 изменений доказано по
исходному тексту: отвалившийся хвост стоит в нём как маркер пункта (`\n10.\t`, включая кривой OCR-вариант
«29 .»). Правка регулярок проверена корпусом, как требует [[feedback_plate_regex_needs_corpus]].

**Проверка на проде после деплоя** (база забэкаплена в `/root/support_agent.db.pre-p4`, ошибок и
трейсбеков в журнале 0, сервис `active`, HTTP 401 без токена):

| Заявка | Результат |
|---|---|
| 4880 | **28 из 28 вложений**, `skipped=0`, `complete=true` честно; строк 25 → **29**; вернулись акты №СМ120 А974ТМ, №СР102 М396УМ763, №СР103 М341УМ |
| 4880 / М790ЕА | **две строки**: №ТР139 `129/0` и №ТР138 `227/158` — второй путевой лист больше не выбрасывается |
| 4915 | строк 15 → **16**, у М790ЕА две строки: `157/47` и `203/157` |
| 4913, 4843 (Чапаевское) | 17 и 29 строк, **ни одного номера с приклеенным хвостом** |

**Побочный эффект, который надо знать оператору:** у Чапаевского «Глушение» стало 14 из 17 и 26 из 29
(было 8 и 15). Это не регресс — раньше эти строки разжаловывались в «Проверить» ложным
`region_conflict` от выдуманного номера (класс 2), а телеметрия и вовсе запрашивалась по несуществующей
машине. Теперь номер верный, но **классы 7, 8, 9 (ложное «Глушение») ещё НЕ починены**, поэтому доля
«Глушения» на этом ПО завышена. Чинить их — первое дело части 2.

**Осталось на П4 часть 2:** классы 7, 8, 9 (порядок ветвей лестницы, перебор вместо недобора,
`low_sat` как свойство объекта), 10 (недостоверный пробег), 11–13 (даты и интервалы), 14 (вердикт
против ярлыка), 17 (страж серий актов + ревизия фантомных детей), 19 (сводность по форме письма),
22 (мёртвый трекер против пустого дня), техника 6, 16, 18, 21, 23. Решения по всем уже приняты выше —
интервью повторять не нужно. Файлы прогона C4 лежат на сервере в `/root/c4/`, срезы корпуса — в
`/root/p4/*.json`.

**Порядок П4 по весу:** (1) класс 1 — склейка номера с номером пункта списка, 238 номеров по корпусу,
правку проверять ТОЛЬКО дифом по всему корпусу ([[feedback_plate_regex_needs_corpus]]); за ней сами
закроются классы 2, 3, 15; (2) классы 4 и 5 — две потери данных; (3) классы 7, 8, 9 — ложное
«Глушение»; (4) класс 17 + страж серий актов, затем ревизия фантомных детей; (5) класс 10 —
недостоверный пробег; (6) классы 11, 12, 13 — даты и интервалы; (7) классы 14, 19, 22; (8) техника
6, 16, 18, 21, 23.

### П4, часть 2 (2026-08-04): классы 7–14, 17–19, 22 + техника 6, 16, 21, 23

Порядок работ по весу, как и планировалось: сначала ложное «Глушение», затем страж серий актов,
недостоверный пробег, даты и интервалы, сводность и хвосты. Интервью не повторялось — решения все
приняты выше. Правки шли параллельно по РАЗНЫМ файлам (сервис · эндпоинты · фронт), у каждого
исполнителя свой временный каталог на сервере; боевой каталог и база не тронуты.

| Класс | Что сделано |
|---|---|
| №7, №8 (порядок ветвей лестницы) | Цифры проверяются ДО того, как ответить «Глушение»: `_jamming_conflicts_with_mileage`. Если наш пробег СОШЁЛСЯ с путевым листом (±10%, но не меньше 5 км) или система ВЫШЕ путевого — вердикт «Проверить» с двумя причинами: глушение клиенту не отвечаем, но и «Данные верны» автоматом не штампуем, прострелы координат в строке есть. Пороги не тронуты, изменён только порядок ветвей ([[feedback_verdict_over_autoclose]]) |
| №9 (`low_sat` как свойство объекта) | `sat_blind`: доля «плохих» фиксов ровно 1.0 при среднем числе спутников ≈0 — это протокол объекта не отдаёт спутники вовсе (Е179АК: 1.0 в два разных дня при ровном потоке), а не событие глушения. Признак не считается, судим по телепортам, скорости и пробегу |
| №10 (пробег, накрученный телепортами) | `_mileage_unreliable` + флаг и пометка `mileage_unreliable`: **5+ прострелов** ЛИБО арифметическая невозможность (км ÷ время движения > 150 км/ч, либо километры при нулевом движении). Такая цифра не сравнивается с путевым листом, не подставляется в шаблон ответа клиенту и не уезжает в описание дочерней заявки в Okdesk. Пометка НАМЕРЕННО не разжалует вердикт (`_DEMOTING_WARNINGS`): рваный трек — признак реального спуфинга, «Глушение» по нему защитимо |
| №14, №15 (вердикт против ярлыка) | `_sync_labels` во всех точках сборки строки (`_analyze_object`, `rules_row`, обе точки добавления сомнений): если вердикт развёрнут в «Проверить», ярлык эвристики обязан замолчать. Счётчик глушений в шапке и так считался по вердиктам — после починки он совпадает с тем, что видит оператор |
| №22 (мёртвый трекер против пустого дня) | Флаг `tracker_silent` (последнее сообщение ≥30 дней ДО даты неисправности). Свой ответ клиенту («не выходит на связь с DD.MM.YYYY, удалённо не восстановить, нужна проверка на месте»), своя группа в сводном ответе, своя причина в описании дочерней заявки — вместо обещания удалённой диагностики |
| №11 (дата из имени файла = день отправки) | Служебный вердикт `Нужен разбор вложений` + пометка `date_from_filename`. Срабатывает, когда тема/имя файла даёт «дата…Заявка №<серия><цифры>» и ни в теле, ни во вложениях нет маркера даты неисправности — по базе это **ровно 129 заявок, все Жигулевское ПО**; формат Волжского («…_Акт№_ВПО») под правило не попадает. Телеметрия за день отправки не запрашивается вовсе (−622 запроса в гео), вердикт по чужому дню не выносится ([[project_answer_sources]]) |
| №12, №13 (интервалы) | `date_to` доезжает до строки и до телеметрии (`_row_date_to`, `_range_ok_for_row`), закрытый диапазон под маркером «Дата неисправности» разрешён до 31 дня. Интервал НЕ раздаётся суточным строкам с путевым листом — замер на живой телеметрии: Н704МК за 22.07 «Данные верны» (ПЛ 8, наш 6.32), а за 22–28.07 уже 870 км и 95 телепортов → «Глушение». Путевой лист — цифра суточная, пороги лестницы тоже |
| №18 (сплиттер плодит дубли детей) | Перед созданием ребёнка ищем уже существующего: кандидаты по номеру в локальном кэше (тема ребёнка = ровно гос.номер), окно 45 дней, до 6 новейших, описание сверяется живьём из Okdesk. Дубль — совпадение ТЕЛА описания (номер, дата И цифры), а не пары (номер, дата): после починки класса 5 два путевых листа одного ТС за день дают две ЗАКОННЫЕ дочерние. В ответе эндпоинта появился счётчик `existing` со ссылкой на существующую заявку |
| №19 (порог сводности) | Решает форма письма: хотя бы одно вложение с ≥5 разными ТС — сводно; «один акт = один файл» (в среднем <2 ТС на источник) — поштучно, сколько бы машин ни было; порог 25 остался страховкой от выгрузок по всему парку. Фронт при сводном разборе гасит кнопку создания дочерней заявки с объяснением, а не надеется на дисциплину оператора |
| №17 (ревизия фантомных детей) | Прочёсана вся база: детей сплиттера **143 от 48 родителей** (06.06–04.08.2026), из них фантом **ровно один** — заявка 66016 «СМ102» (Жигулевское, родитель 65870). Клиенту по ней ничего не ушло, оператор уже закрыл её с пометкой «ошибочно создана» — в Okdesk трогать нечего. Ещё 8 фантомов разбор извлёк, но детей из них не создали |

**Ключевое уточнение по классу 10, найденное замером, а не рассуждением.** Первая версия признака
недостоверности повторяла условие глушения («1 прострел при расчётной скорости >500») и помечала
**408 строк из 710** — почти половину. Причина: сами прыжки в пробег и так НЕ засчитываются при
сборке трека (`else: track_m += dist`), поэтому один битый фикс цифру не портит. Порог «5+
прострелов» (тот же, что у явного спуфинга в лестнице) даёт 330 строк, и это уже осмысленно:
на Чапаевском и Жигулевском половина парка реально едет в зоне подавления.

#### Регресс: старый код против нового, по всей базе

| Что мерили | Объём | Изменилось |
|---|---|---|
| Лестница вердиктов по КЭШИРОВАННОЙ телеметрии (производные флаги пересобраны обеими версиями) | 911 строк | **24 вердикта**: «Глушение»→«Проверить» 22 (классы 7 и 8), «Терминал подключился»→«Проверить» 1 и «Данные верны»→«Проверить» 1 (класс 10). Плюс снят ложный флаг глушения у 11 строк (класс 9). Каждое изменение сверено с цифрами строки |
| Корпус текстов (OCR вложений + темы + описания + training) | 5910 текстов | Шапка разбора — **1 текст** (акт СВ8797: дата та же, появился `date_to`). Построчный разбор `_parse_body_vehicles` — **0** |
| Бесплатный `parse` по всем заявкам (телеметрия-заглушка, без сети) | 5028 заявок | **129 заявок**: 128 Жигулевское (класс 11 — 622 строки получили «Нужен разбор вложений») + 1 Волжское (заявленный период доехал до строки). Номера, даты, ПЛ, число строк, шапка — **0 изменений**, ошибок разбора 0 |
| Разбор вложений `analyze_batch` по заявкам с OCR-кэшем | 370 заявок | **15 заявок / 24 строки** — все получили `date_to` (1–6 дней), каждая проверена по исходнику. Номера, даты, ПЛ, вердикты, пометки, число строк — **0 изменений** |
| Признак сводности | 463 разбора | **42 заявки стали сводными** (Чапаевское +27, Волжское +8, Оренбургэнерго +4, остальное по 1–2), **3 стали поштучными** (отдельные акты на машину, в т.ч. 4880 с 28 актами). По 130 бесплатным разборам — **0** |

**Что оператор увидит на карточке:** спокойный серый чип «пробег недостоверен» (в отличие от
тревожных янтарных пометок он вердикт не разжалует) и цифра пробега приглушённой с «≈»; чип
«молчит с 16.03.2026» рядом с «Нет данных»; синий вердикт «Нужен разбор вложений», в колонке
«Факт» — «не запрашивали» вместо «—»; период у даты строки как `20.07.2026 — 31.07`; при сводном
разборе кнопка дочерней заявки серая с объяснением, почему детей не создаём.

#### Номера и состав строк (классы 6, 16, 17, 21, 23)

| Класс | Что сделано |
|---|---|
| №16, №17 (номер акта принят за гос.номер) | Страж опирается на КОНТЕКСТ слева от токена («Заявка №», «Акт №», «№»), а форма серии `{ТР\|СР\|СМ\|ШР\|ЖР}`+цифры — только усиливающий признак у голого «№». Отсекать по форме токена НЕЛЬЗЯ: `СВ3297`, `ТО8718`, `АХ9393`, `УМ5379`, `СР0955`, `СК1165` — настоящие прицепы и спецтехника, и они уже дали законные строки. Вторая половина дефекта: спецномер из ИМЕНИ ФАЙЛА не извлекался вовсе (гейт требовал ячейку таблицы или маркер), поэтому в 4772 номер акта победил настоящий `АХ 9393` — теперь имя файла считается безопасным контекстом |
| №6 (единицы «ч» и «мин» в таблице акта) | «Пробег по путевому листу \| 41 ч» → моточасы, а не 41 километр; «Пробег в системе Глонасс \| 34,10 мин» в километры не превращается. Единица обязана стоять в ТОЙ ЖЕ строке/ячейке (`_TIME_GAP`) — иначе «227⏎часов работы 8» съедало километры целиком |
| №21 (имя файла ≠ тело документа) | Пометка `act_name_body_differ`: номер акта требуется с обеих сторон и нулевое пересечение. Вердикт НЕ разжалуем сознательно — дату в имени файла этот формат и так пишет как день ОТПРАВКИ (класс 11), а цифры и дата строки берутся из тела и согласованы; под вопросом только происхождение строки |
| №23 (схлопнутый дубль) | Пометка `duplicate_in_source` на уцелевшей строке: схлопывание верное, но оператор не должен искать «потерянную» строку, сверяя 17 строк с 18 позициями письма |
| Источник строки | `rules_row` больше не помечает всё псевдо-источником «(из текста заявки)»: когда номер пришёл из имени вложения, в строку идёт реальное имя файла. Без этого правило сводности по форме письма (класс 19) слепло на бесплатном разборе. Прокинуто и в кэш фактов после платного прогона |

**Три ошибки, найденные аудитом чужой правки** (первая версия стража писалась вслепую, без корпуса):
1. «№» был ОПЦИОНАЛЬНЫМ, и слово «Акт» само становилось маркером документа — тема «FW: Акт 6593AM
   A830PC C147CC» теряла первый живой номер. По корпусу **пропадало 7 номеров** (63807, 60951,
   63469, 66243, 64526, 65143, 65187); после правки все семь вернулись.
2. `акт\w*` жадный: «Активный № СР0955», «Актуальный №…» тоже становились маркером — заменено на
   падежные формы.
3. У стражей единиц времени зазор был `\s*`, то есть ЧЕРЕЗ перевод строки: «Пробег по путевому
   листу \| 227⏎часов работы 8» читалось как «227 часов», километры терялись.

Это ровно тот случай, ради которого написана памятка [[feedback_plate_regex_needs_corpus]]: три
ошибки в одном страже, ни одна не видна на глазок, все три поймал диф по 5914 текстам.

**Диф по корпусу** (5914 текстов, боевой код против нового):

| Поле | Изменений | Что именно |
|---|---|---|
| Номер в строках | **1** | `СР9100 → None` (тема 64698 «…_Заявка_№СР9100ЖПО» — это номер заявки). Короче/длиннее/другие/появились — 0 |
| Дата | 2 | тот же текст: после ухода фантома дата 29.06 досталась настоящему `М312УМ` |
| Пробег по путевому листу | 56 | все `значение → None` — те же тексты, где в графе стоят ЧАСЫ (7ч ×11, 5ч ×9, 6ч ×7, …, 41ч ×1) |
| Заявленный клиентом | **0** | — |
| Число строк | 1 текст `7 → 6` | ушла фантомная строка `СР9100` |
| Шапка `parse_issue` | 60 текстов | 55 — часы ушли из километров в моточасы, 1 — `date_to` от правки интервалов, 4 — новые заявки, которых в базовом срезе ещё не было |

Законные спецномера проверены поштучно, число текстов «было → стало»: ТО8718 24→24, ТО8723 4→4,
СВ8797 31→31, СУ5297 51→51, УМ5379 2→2, АХ9393 40→40, АМ6283 4→4, СР0955 1→1, СК1165 4→4,
СВ3297 35→35, АМ5644 9→9, АК6197 12→12, ТО8766 4→4. Необъяснённых изменений — ноль.

**Точки проверки:** 4772 — из имени файла извлекается ровно `АХ9393`, `СМ102` номером быть перестал;
4880 и 4917 — фантомных строк из серий актов нет; 4915/4916/4880 — «41 ч» даёт моточасы, «34,10 мин»
в километры не идёт, остальные 27 из 28 вложений читаются километрами как раньше; 4798 — 17 строк и
пометка про задвоенный `М719МА`.

**Остаточные риски, честно** (проверяются только на живой заявке после деплоя):
- ветка «номер из ИМЕНИ вложения» корпусным дифом не покрыта: в кэше лежит текст OCR, а имена
  вложений — нет, и тема заявки обрезана до 255 символов (видно 5–6 имён из 28). Проверена
  юнит-тестами на реальных строках 4772, но не срезом;
- попарную проверку `act_name_body_differ` по всем вложениям офлайн воспроизвести нельзя. Если у
  Жигулевского имя файла расходится с телом СИСТЕМАТИЧЕСКИ, пометка станет шумом на всех строках
  письма — смотреть на первом же живом разборе;
- 88-дневный интервал (64486, Барышское) остался суточным из-за ограничения 31 день; в шапке
  полный период по-прежнему виден;
- нормировка порогов лестницы на длину окна как задача не закрыта — именно поэтому интервал не
  раздаётся суточным строкам с путевым листом.

#### Проверка на проде после деплоя части 2 (`869b02a`, база забэкаплена в `/root/support_agent.db.pre-p4-2`)

Прогон: 10 заявок, строго по одной, только бесплатные `parse` и `automate_batch`; сервис `active`
после каждого вызова, `NRestarts=0`, трейсбеков в журнале ноль, самый долгий вызов 4.5 с (4880, 28
вложений из OCR-кэша). Сравнение «до/после» честное — базовые числа сняты из бэкапа, сделанного
ДО деплоя.

| Заявка | Что проверяли | Результат |
|---|---|---|
| 4523 | бесплатный parse не судит по дню отправки | «Нужен разбор вложений» + пометка `date_from_filename`, телеметрия не запрашивалась ✓ |
| 4915 | интервал из акта | СВ8797: `date=20.07`, `date_to=31.07` ✓ |
| 4915 | два путевых листа за день (починка П4-1 держится) | две строки, ТР150 ПЛ 203 и ТР151 ПЛ 157 ✓ |
| 4880 | все вложения | 28 из 28, `complete: true`, `skipped: 0` ✓ |
| 4880 | фантомы серий актов | `СМ122` и `СР103` ушли, строк 29 → 28 ✓ |
| 4880, 4915 | пометка недостоверного пробега | 0 расхождений «5+ прострелов ⇔ пометка», построчно ✓ |
| 4913, 4843 | склейка номера и сводность | 17 и 29 строк, хвостов нет; 4913 `is_aggregate` false → **true** ✓ |
| 4866 | интервал в бесплатном разборе | `13.07 → 31.07`, окно 19 суток реально запрошено ✓ |
| 4798 | задвоенный ТС | 17 строк, `duplicate_in_source` ровно на `М719МА` ✓ |
| 4971 против 4637 | мёртвый трекер | у 4971 `tracker_silent` и ответ «не выходит на связь с 16.03.2026 … проверка на месте», у 4637 прежний «удалённая проверка» ✓ |
| 4452 | пометка не разжалует глушение | А530ВС 1257.9 км / 82.4 мин → пометка + «Глушение» сохранено ✓ |
| 4772 | настоящий номер вместо номера акта | **проверить нечего**: у заявки в Okdesk теперь 0 вложений и пустое описание, имени файла с `АХ 9393` больше не существует. Механизм проверен юнит-тестами |

**Три дефекта, которые нашла именно прод-проверка** (все три исправлены следом, см. ниже):

1. **Классы 7 и 8 фактически не сработали — моя ошибка проектирования.** `_mileage_matches` и
   `_jamming_conflicts_with_mileage` обе выходили на `mileage_unreliable`, а он ставится уже при 5+
   прострелах. На Чапаевском и Жигулевском прострелов 5+ почти у всех «глушений» — сравнение с
   путевым листом не проводилось вовсе. Флагманская строка класса 7 (`Н822ОВ` заявки 4880: ПЛ 85
   против нашего 86.21, расхождение 1.4%, `tj=17`) так и уходила клиенту как «Глушение». Решение
   пользователя сформулировано ИМЕННО про такие строки («прострелы координат есть, решает
   оператор»), поэтому ветка «пробег сошёлся» обязана работать независимо от пометки; ветка класса 8
   («система выше путевого») по накрученной цифре работать не должна — направление по ней не
   определить; «Данные верны» недостоверной цифрой не штампуется по-прежнему.
2. **Фантом `2026ЕТ` — регресс класса 17.** Из имени `03.08.2026_ЕТ-400_АМ_6283_…` извлекатель
   спецномеров вытащил год из даты плюс серию модели. Побочно потерялись моточасы: они раздаются
   только когда в документе ОДНА строка, а фантом сделал две — «41 ч» у `АМ6283` не доехали.
3. **`act_name_body_differ` почти всегда ложная**: 6 ложных против 1 достоверной. Сравнение не
   нормализует подчёркивания внутри номера акта («№СМ_3» против «СМ 3»). Достоверная одна —
   `Н693УТ` заявки 4880: в имени `№ТР140` от 31.07, в теле «Заявка №ТР126 от 29.07.2026».

**Что выросло, и почему это не регресс.** «Глушение» на 4452 стало 17 из 20 (было 13): ушли
склеенные номера, а с ними ложный `region_conflict` (4 пометки → 0), который раньше разжаловывал
вердикт. На 4843 «Глушение» 26 (было 25): `А822ТТ` ушла из «Проверить» в «Глушение» по синхронизации
с ярлыком (avg_sat 3.5, low_sat 0.75 — вердикт защитим), а `С935РХ` и `Н489НУ` наоборот ушли из
«Данные верны» в «Проверить». Урок для метода: **доля «Глушения» на этих ПО остаётся завышенной,
пока не нормированы пороги на длину окна** — это отдельная задача, не часть П4.

**Про параллельную нагрузку:** во время прогона в журнале видны POST'ы `parse`, `automate_batch`,
`batch/verdict`, `batch/row/delete` по заявкам 4855–4950 с внешнего адреса — это пользователь
разбирал накопившиеся заявки в интерфейсе. Сервис выдержал одновременную работу оператора и
проверочного прогона, трейсбеков не было. Вывод для метода: условие «строго по одной заявке»
касается НАШИХ прогонов, живую работу оператора оно не блокирует — но при тяжёлом OCR лучше
спрашивать, свободен ли прод.

#### Доработка по итогам прод-проверки (те же три дефекта)

| Дефект | Что сделано |
|---|---|
| Классы 7, 8 не срабатывали | В `_jamming_conflicts_with_mileage` убран общий выход по `mileage_unreliable`: ветка «пробег СОШЁЛСЯ с путевым листом» работает ВСЕГДА (решение пользователя сформулировано именно про строки с прострелами), ветка «система ВЫШЕ путевого» — только при достоверной цифре, потому что направление расхождения по накрученному пробегу не определить. `_mileage_matches`, по которой выносится «Данные верны», проверку сохраняет: недостоверной цифрой «всё в порядке» не штампуем. Комментарии-обманки в лестнице и в `_heuristic_category` исправлены |
| Фантом `2026ЕТ` | `_filename_plate_text`: перед поиском номеров в имени файла маскируются даты `ДД.ММ.ГГ(ГГ)` и обозначения моделей «буквы-1..3 цифры» («ЕТ-400», «МТЗ-82»); ровно четыре цифры после дефиса не маскируются — у спецномера цифр всегда четыре |
| Ложные пометки актов | `_normalize_act` (убирает `_`, пробелы, дефисы, точки, латиница→кириллица) + `_same_act_number`: номер-ПРЕФИКС считается тем же актом (обрезка OCR). Ложная пометка обесценивает все пометки, пропущенная лишь лишает оператора подсказки и цифры строки не меняет |

**Числа доработки:** вердиктов изменилось **27**, все «Глушение» → «Проверить» и все по ветке «пробег
сошёлся с путевым листом» (по ветке «система выше» — 0); «Данные верны» 36 → 36, ни одного нового;
ярлык эвристики двигается синхронно (класс 14 не нарушен). Диф по корпусу из 5951 текста — **0
изменений** и в шапке, и в строках. По 1224 именам вложений изменилось 4 — исчез `2026ЕТ`, вместе с
ним вернулись моточасы «41 ч» у `АМ6283` (строк в документе снова одна). Пометок актов 30 → 19:
снялись десять подчёркиваний и одна обрезка, достоверная на `Н693УТ` осталась.

**Оставлено сознательно:** `СР9108` в имени против `СР108` в теле (66160/Н316ОВ) пометку сохраняет —
это не префикс, расхождение в середине; отличить лишнюю цифру OCR от другого акта по данным нельзя.
Правило префикса — сознательный ложноотрицательный компромисс: если в одной заявке реально окажутся
разные акты `ТР15` и `ТР155`, пометки не будет.

**Подтверждение доработки на проде** (`92d0ba4`, два прогона `automate_batch`, сервис жив, трейсбеков
ноль, OCR из кэша — токены не тратились):

| Точка | Результат |
|---|---|
| 4880 / `Н822ОВ` (ПЛ 85, наш 86.21) | «Глушение» → **«Проверить»**, пометка `mileage_unreliable` на месте |
| 4880, вердикты по заявке | «Глушение» **24 → 20**, «Проверить» 1 → 5. Развернулись `Н274МВ763` (47/45.77), `М063СО` (65/58.57), `Н822ОВ` (85/86.21), `А974ТМ` (248/223.65) |
| 4880, пометки актов | 2 → **1**: ложная `Заявка_№СМ_118` ушла, достоверная на `Н693УТ` (`№ТР140` в имени против «№ТР126 от 29.07» в теле) осталась |
| 4915 | фантом `2026ЕТ` исчез, строк 17 → **16**; у `АМ6283` моточасы **41**, путевой лист пуст, пометка `mileage_in_hours`; пометок расхождения актов 5 → **0** |

`М063СО` прошла в «Проверить» с запасом 0.07 км (расхождение 6.43 при допуске 6.5) — самый край
правила, но формально верно. Отдельно к сведению: `POST /automate_batch` перезаписывает кэш `batch`,
то есть ручные правки вердиктов оператора в переразобранной заявке заменяются свежим разбором по
правилам — при проверочных прогонах это надо учитывать.

### C5 (2026-08-04/05): Самарское · Оренбург Восточное

Прогон: **26 вызовов** (15 `parse` + 6 `automate_batch` + добор), все HTTP 200, сервис `active` после
каждого, трейсбеков ноль, самый долгий вызов 11.5 с (сводная 1186, 224 строки). Выборка: Самарское 10
(вложений у этого ПО практически нет — проверено 83 заявки, найдено ОДНО xlsx, поэтому 10 текстовых),
Оренбург Восточное 10 (5 с PDF-заявками, 5 без вложений). Состояние «ДО» снято: у 10 заявок из 20
разбор уже был — операторы жали его в проде, «нуля» у этих ПО больше нет.

**Два совершенно разных ПО в одной сессии, и это оказалось полезно.** Восточное — почти чистое: 5
находок, все вокруг переписки. Самарское — самое проблемное ПО за все пять сессий: 18 находок, из них
две системные, задевающие каждую строку.

| # | ПО | Заявка | Ось | В исходнике | В разборе | Класс дефекта |
|---|---|---|---|---|---|---|
| 1 | Сам. | 4405, 4450, 4461, 4522 — **42 строки из 42** | Номер | `В418УО 162км`, `Н456ОТ 109км` | `В418УО162`, `Н456ОТ109` | `Номер: пробег приклеен как код региона` |
| 2 | Сам. | 18 строк | Вердикт | тот же парк | `region_conflict` → вердикт разжалован | `Вердикт: ложный region_conflict как следствие №1` |
| 3 | Сам. | все 9 одиночных — **47 строк из 47** | ПЛ | число «NNкм» есть в каждой строке | `sheet_mileage_km` и `declared_system_km` пусты — сравнения с телеметрией не было НИ РАЗУ | `ПЛ: одинокое «NNкм» не принимается за путевой лист` |
| 4 | Сам. | 4331 (5 интервалов), 4335 (2) | Состав | `26.06 08.00-20.00 60км / 26-27.06 20.00-08.00 121км / 27-28.06 138км / 28.06 81км / 28-29.06 109км` | ОДНА строка, интервалы 3–5 потеряны целиком | `Состав: несколько интервалов одного ТС схлопнуты в одну строку` |
| 5 | Сам. | 4322 | Состав | `26.06.2026 11км` + `28.06.2026 2км` | одна строка, только 26.06 | `Состав: вторая дата того же ТС потеряна` |
| 6 | Сам. | 4522 | Состав | описание 1 ТС + комментарий КЛИЕНТА с 8 ТС | 1 строка; 8 ТС не разобраны, хотя оператор по ним отвечал | `Состав: список ТС из комментария клиента игнорируется` |
| 7 | Сам. | 4461 | Состав/дата | `М618ХТ 18км 20.00-08.00 05.07.2026-06..07.2026`, `А702АР 39км 04-.05.2026` | обе строки потеряны (18 → 16) | `Дата: строка с опечаткой в дате отбрасывается целиком` |
| 8 | Сам. | 4461 стр. 11 | Дата | `А702АР 28км 20.00-08.00 03-04.07.2026` (ночь 03→04) | дата 04.07, начало ночи отброшено | `Дата: ночное окно сплющено в следующие сутки` |
| 9 | Сам. | 4405, 4461, 4331, 4335 | Дата | `08.00-20.00`, `20.00-08.00` | времени в строках нет вовсе, телеметрия за полные сутки | `Дата: окно смены игнорируется` |
| 10 | Сам. | 4405 `Е900КЕ`, 4450 `Р067СТ` | Гео | таких номеров в парке нет | молча подобраны `Е900КА`, `Р067СА` — вердикт по ЧУЖОЙ машине | `Гео: похожий номер подбирается молча` |
| 11 | Сам. | 4450 `Н857СМ` 02.07 | Факт | оператор написал клиенту «11,12 км» | наш пробег 74.17 (в 6.7 раза больше); вторая пара 125,42 против 109.94 | `Гео: наш пробег расходится с цифрой оператора` — расследуется отдельно |
| 12 | Сам. | 1186 (**63026**) | Вердикт | заявка про пропавший список ТС («Список техники восстановлен»), вложение — НАШ системный «Сводный отчет за 12.05.2026» на 225 машин | 224 пробеговых вердикта, 56 «Глушение» | `Вердикт: пробеговая лестница на заявке не про пробег` |
| 13 | Сам. | 1186 | Заявленный | колонка «Пробег» заполнена у 109 строк | заявленный пробег пуст во всех 224 | `Заявленный: колонка отчёта не подхвачена` — **снято решением пользователя** (такие заявки не разбираем вовсе) |
| 14 | Сам. | 1186 | Состав | 225 ТС + «Итого» | 224 строки: «Итого» отброшено верно, потеряна машина `Т 1502 466СВ` | `Состав: ТС с нестандартным именем потерян` — **снято** там же |
| 15 | Сам. | 4324, 3863 | Интент | «некорректный расход», «некорректный трек» | тема жалобы не распознана (пробеговый диагноз при этом НЕ поставлен) | `Интент: жалобы про топливо и трек не опознаются` (мягкий) |
| 16 | Вост. | 4430 | Комментарии | коммент клиента `Извиняюсь 31км по путевому листу` | в фактах остался путевой лист 90 из описания | `Комментарии: правка цифры клиентом не подхвачена` |
| 17 | Вост. | 4430 | Комментарии | `Расхождение за 20.07 198 км по пут л` | строка за 20.07 создана, но пробеги пусты — «198» в разборе нет | `Комментарии: из фразы берётся только дата, пробег теряется` |
| 18 | Вост. | 4602 | Номер | коммент клиента `О135УН56 … расхождение пробега за 28.06.2026 по путевому листу 48 км` | дата взята, номер нет, 0 строк, нота «номер не найден» | `Комментарии: номер из той же фразы выбрасывается` |
| 19 | Вост. | 4676 `Т320ХТ56` | Вердикт | ПК 100,52 / ПЛ 203, наш 180.96, трек чистый | вердикт «Терминал подключился» при ярлыке «Данные верны» | `Вердикт: ярлык рассогласован на НЕ-«Проверить» парах` — синхронизация покрыла только «Проверить» |
| 20 | Вост. | 4676 `Т918ВН56` | Вердикт | ПК 3,59 / ПЛ 123, наш 61.69, **разрыв трека 6 часов** | «Глушение» | `Вердикт: глушение поставлено при длинном разрыве трека` |
| 21 | оба | 4602, 4676 | Инфраструктура | — | разбор в кэше от 29.07, то есть ДО всей починки П4; кэш при смене правил не инвалидируется — оператор смотрит вердикт прежней логики | `Кэш: разбор не помечен версией правил` |

#### Что проверено и НЕ сломано

- **Оренбург Восточное, главный риск формата — не подтвердился**: во всех 19 строках PDF взята дата
  НЕИСПРАВНОСТИ, а не «дата обнаружения» из соседней колонки (4602 — 23.07 при 24.07 в журнале,
  4779 — 28.07 при 29.07, 4649 — две разные даты прочитаны раздельно). Ни одной подмены.
- Десятичная запятая и перенос «км» на другую строку: `79,48`, `155,8`, `3,59`, `367,76`, «455 км»
  через перенос — всё прочитано верно. Заявленная проблема 4602 («заявленный пробег пуст») **не
  подтвердилась**: пусто ровно там, где цифры в исходнике нет.
- Регион `56`/`156` не теряется и не выдумывается; латиница из марки (`3813DO`, `6G74`, `LVB`, `DA 328`)
  номером не стала; спецномера распознаны (`56НМ5737` → «Беларус-2022», `5167АХ` → «Беларус 5167ах»,
  в сводной 1186 — `8362СА`, `0146СМ`, `9140СУ`); номер заказа 1С `476942` фантомной строки не дал.
- Состав строк Восточного точен: 6/6, 3/3, 6/6, 2/2, 2/2 против числа пунктов PDF.
- **Починки П4 работают на новом ПО**: развороты ложного «Глушения» видны (4649 `Е698СЕ156` ПЛ 65 против
  62.37 и 4779 `М655ВС156` 416 против 390.84 — оба «Проверить»; 4602 `Т495КО56`, где система выше ПЛ, —
  тоже «Проверить»). Ложных «Данные верны» нет ни в 47 строках Самарского, ни в 224 строках сводной, ни
  в 19 строках Восточного; в старом кэше 4461 они были и теперь исправлены.
- Раздача даты из темы верна: в 4405 общая `01.07` разошлась по 19 ТС, а локальная `29.06` досталась
  именно `Н747РЕ`. Окно суток строится в МСК, систематического сдвига на 3 часа нет.
- «Нет данных» в сводной 1186 — **настоящие**: телеметрия найдена у 224 из 224 строк, у всех «Нет
  данных» реально ноль пакетов. Числа из отчёта не выдуманы (65.98 → 63.8, 21.63 → 21.95).
- Жалобы не про пробег («некорректный расход», «некорректный трек») пробегового диагноза не получили.
- Признак сводности верен: у 1186 сводно, у всех девяти одиночных поштучно — включая многострочные.

#### Решения интервью 5 (2026-08-05) → задание на П5, не перерешивать

| Вопрос | Решение |
|---|---|
| Окно смены (`08.00-20.00`, ночная `20.00-08.00`) | **Оставить сутки.** Время смены в вердикт не берём, пороги лестницы откалиброваны на сутки; но такие строки ПОМЕЧАТЬ, чтобы оператор видел: клиент спрашивал про смену, а цифра посчитана за сутки |
| Несколько интервалов по одному ТС (4331: пять) | **Отдельная строка на каждый интервал** — своя дата, свой путевой лист, своя телеметрия, свой вердикт. Как уже сделано для двух путевых листов за день и для 13 актов в одном PDF |
| Опечатка клиента в номере (`Е900КЕ` при `Е900КА` в парке) | **Не подбирать вовсе.** «Объект не найден» + подсказка похожих номеров оператору. Вердикт по чужой машине не выносится никогда |
| «Глушение» при длинном разрыве трека (4676/`Т918ВН56`, 6 часов) | **Разрыв важнее — но по напряжению.** Если во время разрыва НЕТ и данных по напряжению, это не глушение: вердикт «Проверить», решает оператор. Если напряжение в норме и данные идут — «Глушение» остаётся законным |
| Устаревший разбор в кэше (вердикты от 29.07 по прежним правилам) | **Сбрасывать кэш при деплое:** хранить версию правил рядом с разбором и при её смене считать кэш невалидным. OCR при этом переиспользуется, токены не тратятся |
| Заявка 1186 / **63026** (системный сводный отчёт на заявке «список») | **Заявку без вопроса не разбираем вовсе.** Пользователь посмотрел: в 63026 нет ни жалобы, ни вопроса — заявку создали, а контекст передали по телефону; операторы такие не разбирают. Значит разбор обязан честно говорить «в заявке нет вопроса, разбирать нечего», а не выдавать 224 пробеговых вердикта. Находки 13 и 14 (колонка отчёта, потерянная машина) этим решением СНЯТЫ: читать наш собственный сводный отчёт как жалобу больше не нужно |
| Починка 66241 (13 актов вместо одной строки) | **Выкладывать вместе с П5**, отдельным деплоем не гнать |

#### Дополнение к интервью 5 (2026-08-05): наша цифра пробега врёт

Расследование расхождения «оператор написал клиенту 11,12 км, у нас 74.17» (объект 18102, 02.07.2026)
нашло ДВА независимых дефекта в самом расчёте пробега. Это важнее всех остальных находок C5: цифра
уходит и оператору, и клиенту, а ни один наш признак недостоверности при этом не срабатывал.

**Дефект A — подменённый трек, который мы принимаем за правду.** 06:38 прыжок 23 км (190 км/ч,
отброшен фильтром); **06:38–07:16 трек шесть раз проходит замкнутую петлю из одних и тех же шести
точек** ровными 99–103 км/ч, шаги 160–195 м каждые 6 секунд — **+38.97 км фантома**; 07:26 возврат
21.89 км на **131 км/ч, ниже нашего порога 150, поэтому засчитан как реальный пробег**; 07:53–08:40 —
единственная настоящая поездка, 11.12 км, ровно цифра оператора. Признаки молчали: телепортов 2 (порог
5), макс. расчётная 203.5 (порог 500), средняя 38.7 км/ч (порог 150). Одометр терминала обманут тоже
(+35 км), суточная статистика гео дала 51.91. **Правильный ответ здесь — «Глушение» и отказ называть
число, а не цифра.**

**Дефект B — пакет без координат рвёт цепочку.** Условие «у обоих пакетов есть координаты» выбрасывает
ОБА сегмента вокруг битого пакета, и реально пройденное между валидными точками теряется. Объект 7793
(«ГАЗ-3308 Р067СА»), 02.07.2026, 16 битых пакетов из 974: у нас 109.94, при склейке цепочки —
**125.42, совпадение с операторской цифрой до сотых**. Дефект у 7 объектов из 12 проверенных,
расхождение от 0.4% до пятикратного.

| Вопрос | Решение |
|---|---|
| Фантомная петля | **Новый признак «повтор точек»**: трек многократно проходит одни и те же координаты → «Глушение» + пометка недостоверного пробега, число клиенту не называем. Пороги телепортов НЕ трогаем |
| Разрыв цепочки на битых пакетах | **Склеивать КОРОТКИЕ провалы** (мало времени между валидными точками, хорда правдоподобна) — на чистом треке цифра совпадёт с операторской. **Длинные провалы не склеивать** и помечать строку недостоверной. Решение переспрошено после разбора: сначала выбрали «не чинить вовсе», но это систематически занижало пробег на всех чистых треках, а заниженная цифра уходит клиенту |

**Урок для метода, важнее конкретных правок.** Пять сессий подряд мы сверяли РАЗБОР ТЕКСТА с
исходником, а саму цифру телеметрии считали данностью. Она данностью не является: на глушёных ПО
надёжны только ручные выводы оператора, а все три машинных источника (наш пересчёт по треку, суточная
статистика гео, одометр терминала) могут быть обмануты одновременно. Отдельная ось сверки —
**«наша цифра против цифры, которую оператор написал клиенту»** — должна войти в метод следующих
сессий: она нашла то, чего не видно ни в одном разборе текста.

### П5 (2026-08-05/06): починка пачкой по классам C5

Правки шли тремя волнами по РАЗНЫМ областям одного файла плюс отдельный кусок про кэш, у каждого
исполнителя свой временный каталог на сервере. Порядок волн выбран по цене ошибки для клиента:
сначала цифра пробега и лестница, потом состав строк, потом текст.

| Класс C5 | Что сделано |
|---|---|
| №1 (пробег приклеен как регион, 42/42) | Страж «за числом не стоит км» в обеих регулярках номера. Хвост «код региона» вынесен в ОДНУ константу и подключён и в `_PLATE_RE`, и в `_PLATE_STD_RE` — раньше выражение было скопировано дословно, и именно из-за этого в C3 ветви разъехались. Теперь правка физически не может доехать в одну копию и не доехать в другую |
| №3 (одинокое «NNкм» не берётся в путевой лист, 47/47) | Жёсткий шаблон «номер + число + км»: число сразу за номером, «км» обязателен, `км/ч` и «(моточасы)» отбиты. Ветка включается ТОЛЬКО когда явных ключей («по одометру», «ГЛОНАСС», «ПЛ») в тексте нет — ключ всегда важнее |
| №4, №5 (интервалы и вторая дата теряются) | Своя строка на каждый интервал со своей датой и своим путевым листом. Включается только когда во ВСЕЙ заявке один ТС: у списка машин непонятно, к какой относится интервал. Ночной интервал относится к дате начала смены |
| №6 (список ТС из комментария клиента) | Комментарий клиента разбирается тем же построчным движком, что и описание: 4522 даёт девять строк вместо одной. Берутся только машины с цифрой пробега или своей датой — иначе «по Н456ОТ ничего не изменилось» заводило бы строку на пустом месте |
| №7 (строка с опечаткой в дате исчезала) | Битая дата больше не уносит с собой ТС и его пробег: строка остаётся со служебным ярлыком «Нет даты» |
| №9 (окно смены игнорируется) | Решение пользователя: время в расчёт не берём (пороги откалиброваны на сутки), но строку помечаем `shift_window` — оператор видит, что клиент спрашивал про смену, а цифра за сутки |
| №10 (похожий номер подбирался молча) | Подбор по несовпадающим буквам выключен: «Объект не найден» + до пяти похожих номеров парка подсказкой оператору. Номер БЕЗ региона по-прежнему находит объект С регионом, латиница/кириллица сопоставляется как раньше |
| №11 (наша цифра пробега врёт) | Два признака: **фантомная петля** (трек кругами без остановок) → «Глушение» + недостоверный пробег; **склейка коротких провалов координат** (до 25 мин) → на чистом треке цифра совпала с операторской до сотых. Длинные провалы не достраиваем и помечаем |
| №12 (пробеговые вердикты на заявке не про пробег) | Заявка без вопроса + наш собственный сводный отчёт → служебный вердикт «В заявке нет вопроса». 63026: было 224 вердикта, стала одна строка |
| №15 (интенты) | Ярлыки «некорректный расход топлива» и «некорректный трек» — ЧИСТО ярлыки: км-вердикт они не отменяют, иначе законное «Глушение» по жалобе «некорректный трек» было бы убито |
| №16, №17, №18 (комментарии клиента) | Из фразы клиента берётся связка «номер + дата + пробег» целиком; если клиент позже поправил цифру — верим последнему его слову (пометка `client_fixed_mileage`) |
| №19 (ярлык рассогласован) | Синхронизация распространена на ЛЮБУЮ расходящуюся пару причинных оценок, не только на «Проверить» |
| №20 (глушение при разрыве трека) | Решение пользователя: если во время разрыва нет и данных о напряжении — это не глушение, вердикт «Проверить». Есть напряжение и данные — «Глушение» законно |
| №21 (устаревший разбор в кэше) | `RULES_VERSION` рядом с записью разбора: при смене версии старый разбор не отдаётся как готовый. OCR (929 записей, 0.91 МБ текста) под инвалидацию НЕ попадает — распознавание от правил не зависит. Платный ИИ от сброса не запускается: промах кэша уходит в бесплатный разбор. Плюс `created_at` теперь обновляется при перезаписи — «разбор от 29.07» больше не врёт про свежесть |

**Признак фантомной петли — точная формулировка.** Существует клетка сетки 100 м, куда трек
возвращался ≥4 раза (проходы разделены ≥2 мин), между возвратами пройдено ≥800 м, длины кругов ±25 %
от медианы, средняя скорость по кругу **≥40 км/ч** (то есть круг пройден без остановок), и серия
накрутила ≥15 км. Две очевидные гипотезы пришлось отбросить: петля 18102 — это круг длиной 12.5 км,
поэтому ни «крошечный охват», ни «доля повторных клеток» её не отличают (1.9 у фантома против 2.3–2.7
у чистых развозных машин). Разделяет **темп**: фантом крутится нонстоп на 97–99 км/ч, а настоящая
кольцевая работа идёт с остановками — максимум 20.9 км/ч на 45 проверенных объекто-сутках.

**Ошибки, найденные регрессом внутри самой починки** (обе — в правках, написанных, но не проверенных
из-за обрыва по лимитам):
1. Разбиение по интервалам смен ломало **Сердобское ПО**: формат `- 26.06.2026 - 64 км - 115 км`
   превращал одну строку в три с ВЫДУМАННЫМИ путевыми листами 64/60/50. Страж: если у даты в сегменте
   больше одной цифры километров — интервалы не разбираем вовсе.
2. Слияние недатированных строк акта плодило фантомные дубли и задваивало числа. Переписано на
   признак совместимости строк.

#### Регресс П5

| Что мерили | Объём | Изменилось |
|---|---|---|
| Разбор текста по корпусу | 6002 текста | шапка `parse_issue` — **0**; строки — **9 текстов**, все многоактные PDF Волжского ПО, где раньше молча терялся заявленный пробег недатированной строки акта |
| Состав строк по кэшированным разборам | 582 заявки | **3 заявки**: 66241 (1→13), 66234 (1→3), 63026 (**224→1**). Ни одно другое ПО не задето |
| Разбор описаний (боевых описаний в кэше нет, брали тренировочный корпус) | 870 образцов | **7 заявок**, все Самарское ПО, +10 строк; ещё у 3 пробег перестал приклеиваться к номеру |
| Вердикты по кэшированной телеметрии | 1269 строк | вердикты **0**, ярлык **1** (синхронизация), новых «Данные верны» **0** |
| Ложные срабатывания «нет вопроса» | весь кэш | признак нашей выгрузки сработал ровно на ОДНОМ документе — том самом. Чапаевское 44 заявки / 434 строки, Барышское 9 / 214, Ульяновское 15 / 22 — **0 изменений** |
| Контрольные объекты гео (живые данные) | 69 объекто-суток | 7793 → **125.42** (цифра оператора, было 109.94); 18102 → «Глушение», цифра помечена недостоверной; `Т734АМ` остался 15.76 с пометкой; 27 суток чистого трека — ложных срабатываний **0** |

**Честно про пределы проверки:** комментарии клиента нигде не кэшируются, поэтому классы 6, 16–18
регрессом по боевым данным не покрываются в принципе — они проверены точечно на текстах заявок и
пробой на ложные срабатывания по обычной переписке. Массовый эффект будет виден только после деплоя.
Признаки петли и провала координат по кэшу тоже не воспроизводимы (сырых пакетов там нет) — они
проверены на живых данных гео.

#### Проверка П5 на проде (`55650c8`, база в `/root/support_agent.db.pre-p5`)

Прогон: 11 заявок по одной, только бесплатные вызовы, сервис `active` после каждого, трейсбеков за
25 минут ноль, самый долгий вызов 2.2 с. **Сошлось 10 из 11.**

| Заявка | Было → стало |
|---|---|
| 66241 | 1 → **13** строк, все 13 пар «путевой лист / заявленный» совпали до сотых |
| 63026 | 224 → **1**, вердикт «В заявке нет вопроса» |
| 4405 | номера без приклеенного пробега, путевые листы 162/36/19/91/240/43 на месте, `region_conflict` исчез; `Е900КЕ` → «Объект не найден» + `similar_plates=['Е900КА763']`, чужая машина не подставлена |
| 4331 · 4322 · 4522 | 1 → **5** (смены, у всех `shift_window`) · 1 → **2** · 1 → **9** (восемь ТС из комментария клиента) |
| 4430 · 4602 | путевой лист **31** из комментария вместо 90 (+ `client_fixed_mileage`) и строка 20.07 на **198**; номер `О135УН56` взят из фразы клиента, 0 → **2** строки |
| 4676 | `Т918ВН56` (разрыв 6.2 ч) «Глушение» → **«Проверить»** с `gap_power_unknown`; у `Т320ХТ56` вердикт и ярлык совпали |
| 4450 | `Н857СМ` → «Глушение» + `phantom_loop`: 3 круга по 12.48 км, накручено 35.71 км, пробег помечен недостоверным |

**Не сошлось: 4461 — 17 строк вместо 18.** Строка `М618ХТ 18км 20.00-08.00 05.07.2026-06..07.2026`
не вернулась, и причина НЕ в битой дате: первая половина `05.07.2026` читается нормально, ветка
«Нет даты» не включается, а ключ дедупа `(М618ХТ, 05.07)` уже занят ДНЕВНОЙ сменой `М618ХТ 196км
05.07.2026`. То есть ночная смена схлопнулась с дневной за тот же день. Это тот же класс, что чинили
в П4 для актов (два путевых листа одного ТС за день — тогда в ключ добавили ФАЙЛ): здесь в ключ надо
добавить ОКНО СМЕНЫ. Потеря не молчаливая — у выжившей строки стоит `duplicate_in_source`.
**Открытый класс, в П5 не закрыт.**

Версия правил штампуется верно: новую получили 12 записей (ровно 12 прогонов), 763 остались
устаревшими и пересчитаются при открытии заявки, 935 записей OCR сохранены — распознавание не
повторяется и токены не тратятся.
